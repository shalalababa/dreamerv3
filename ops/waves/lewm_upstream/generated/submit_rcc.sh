# GENERATED by scripts/ops/wavegen.py from /home/rickybao/projects/dreamerv3/ops/waves/lewm_upstream/spec.yaml -- do not edit by hand.
# Submit this wave's rcc-slurm stages. Re-runnable: run until it reports 0 left.
#   usage: submit_rcc.sh [--dry-run]
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DV3OPS_ROOT="${DV3OPS_ROOT:-/home/rickybao/projects/dreamerv3}"
[ -f "$DV3OPS_ROOT/scripts/ops/lib/common.sh" ] || {
  echo "ERROR: repo not found at DV3OPS_ROOT=$DV3OPS_ROOT" >&2
  echo "       (set DV3OPS_ROOT if the repo lives elsewhere on this machine)" >&2
  exit 2; }
cd "$DV3OPS_ROOT"
source scripts/env.sh

DRY=0; [ "${1:-}" = "--dry-run" ] && DRY=1
MAX_JOBS="${MAX_JOBS:-12}"

# (2) module leakage: --export=ALL carries this shell into every job.
if [ -n "${LOADEDMODULES:-}" ] || [ -n "${_LMFILES_:-}" ]; then
  echo "REFUSING TO SUBMIT: modules are loaded in this shell." >&2
  echo "  LOADEDMODULES=${LOADEDMODULES:-}" >&2
  echo "  --export=ALL would leak them into every job; this has segfaulted before." >&2
  echo "  Open a clean login shell, 'source scripts/env.sh', and rerun." >&2
  exit 2
fi

command -v squeue >/dev/null 2>&1 || {
  echo "ERROR: squeue not found -- run this on an RCC login node." >&2; exit 2; }
cur="$(squeue -h -u "$USER" | wc -l)" || {
  echo "ERROR: squeue failed; refusing to guess the queue depth and risk" >&2
  echo "       submitting past the cap." >&2; exit 2; }
slots=$(( MAX_JOBS - cur ))
echo "slurm: $cur job(s) in queue, cap $MAX_JOBS, $slots slot(s) free"
[ "$slots" -gt 0 ] || { echo "at cap; rerun later"; exit 0; }

queued="$(squeue -h -u "$USER" -o '%j' 2>/dev/null || true)"
submitted=0; skipped=0; left=0


# --- adapt_ax1jppxq1ms0_finger_seed1_ckpt500000 ---
if printf '%s\n' "$queued" | grep -qxF adapt_ax1jppxq1ms0_finger_seed1_ckpt500000; then
  echo "  SKIP already queued: adapt_ax1jppxq1ms0_finger_seed1_ckpt500000"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT adapt_ax1jppxq1ms0_finger_seed1_ckpt500000"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=12:00:00 --job-name=adapt_ax1jppxq1ms0_finger_seed1_ckpt500000 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_ID=adapt_ax1jppxq1ms0_finger_seed1_ckpt500000,WM_RUN=ax1wm_finger_jppxq1ms0_seed1,TASK=dmc_finger_turn_hard,SEED=1,REPLAY='$RUNROOT/axis1_finger/pxq1m/side0',UPDATES=500000,STEPS=1.25e5,AXIS1_EXPL_MODE=task,AXIS1_BASE_CONFIG=pixel_wm,AXIS1_INIT_WM=lewm_distill_finger_s0_seed1,DREAMER_DISABLE_GPUFLAGS=1 \
      "$REPO/scripts/axis1.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- adapt_ax1jppxq1ms1_finger_seed1_ckpt500000 ---
if printf '%s\n' "$queued" | grep -qxF adapt_ax1jppxq1ms1_finger_seed1_ckpt500000; then
  echo "  SKIP already queued: adapt_ax1jppxq1ms1_finger_seed1_ckpt500000"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT adapt_ax1jppxq1ms1_finger_seed1_ckpt500000"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=12:00:00 --job-name=adapt_ax1jppxq1ms1_finger_seed1_ckpt500000 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_ID=adapt_ax1jppxq1ms1_finger_seed1_ckpt500000,WM_RUN=ax1wm_finger_jppxq1ms1_seed1,TASK=dmc_finger_turn_hard,SEED=1,REPLAY='$RUNROOT/axis1_finger/pxq1m/side1',UPDATES=500000,STEPS=1.25e5,AXIS1_EXPL_MODE=task,AXIS1_BASE_CONFIG=pixel_wm,AXIS1_INIT_WM=lewm_distill_finger_s1_seed1,DREAMER_DISABLE_GPUFLAGS=1 \
      "$REPO/scripts/axis1.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- adapt_ax1jppxq1ms0_finger_seed2_ckpt500000 ---
if printf '%s\n' "$queued" | grep -qxF adapt_ax1jppxq1ms0_finger_seed2_ckpt500000; then
  echo "  SKIP already queued: adapt_ax1jppxq1ms0_finger_seed2_ckpt500000"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT adapt_ax1jppxq1ms0_finger_seed2_ckpt500000"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=12:00:00 --job-name=adapt_ax1jppxq1ms0_finger_seed2_ckpt500000 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_ID=adapt_ax1jppxq1ms0_finger_seed2_ckpt500000,WM_RUN=ax1wm_finger_jppxq1ms0_seed2,TASK=dmc_finger_turn_hard,SEED=2,REPLAY='$RUNROOT/axis1_finger/pxq1m/side0',UPDATES=500000,STEPS=1.25e5,AXIS1_EXPL_MODE=task,AXIS1_BASE_CONFIG=pixel_wm,AXIS1_INIT_WM=lewm_distill_finger_s0_seed2,DREAMER_DISABLE_GPUFLAGS=1 \
      "$REPO/scripts/axis1.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- adapt_ax1jppxq1ms1_finger_seed2_ckpt500000 ---
if printf '%s\n' "$queued" | grep -qxF adapt_ax1jppxq1ms1_finger_seed2_ckpt500000; then
  echo "  SKIP already queued: adapt_ax1jppxq1ms1_finger_seed2_ckpt500000"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT adapt_ax1jppxq1ms1_finger_seed2_ckpt500000"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=12:00:00 --job-name=adapt_ax1jppxq1ms1_finger_seed2_ckpt500000 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_ID=adapt_ax1jppxq1ms1_finger_seed2_ckpt500000,WM_RUN=ax1wm_finger_jppxq1ms1_seed2,TASK=dmc_finger_turn_hard,SEED=2,REPLAY='$RUNROOT/axis1_finger/pxq1m/side1',UPDATES=500000,STEPS=1.25e5,AXIS1_EXPL_MODE=task,AXIS1_BASE_CONFIG=pixel_wm,AXIS1_INIT_WM=lewm_distill_finger_s1_seed2,DREAMER_DISABLE_GPUFLAGS=1 \
      "$REPO/scripts/axis1.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

echo
echo "submitted=$submitted skipped=$skipped left=$left"
[ "$left" -eq 0 ] && echo "all submitted" \
  || echo "$left run(s) did not fit under the cap -- rerun this script when jobs drain"

