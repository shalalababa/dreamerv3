# GENERATED by scripts/ops/wavegen.py from /home/rickybao/projects/dreamerv3/ops/waves/lewm_probe_rcc/spec.yaml -- do not edit by hand.
# Submit this wave's rcc-slurm stages. Re-runnable: run until it reports 0 left.
#   usage: submit_rcc.sh [--dry-run]
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DV3OPS_ROOT="${DV3OPS_ROOT:-/home/rickybao/projects/dreamerv3}"
[ -f "$DV3OPS_ROOT/scripts/ops/lib/common.sh" ] || {
  echo "ERROR: repo not found at DV3OPS_ROOT=$DV3OPS_ROOT" >&2
  echo "       (set DV3OPS_ROOT if the repo lives elsewhere on this machine)" >&2
  exit 2; }
cd "$DV3OPS_ROOT"
source scripts/env.sh

DRY=0; [ "${1:-}" = "--dry-run" ] && DRY=1
MAX_JOBS="${MAX_JOBS:-12}"

# (2) module leakage: --export=ALL carries this shell into every job.
#
# The 2026-07-07 incident (a day of rc=139 segfaults ~30s in, at the first JAX
# GPU touch) was caused by `module load cuda/12.2` + `module load
# python/miniforge` + `conda activate` in the submitting shell: their
# PATH/LD_LIBRARY_PATH shadowed the conda env's pinned CUDA wheels.
#
# It was NOT caused by the modules Midway3 loads for every login shell --
# slurm (which PROVIDES sbatch), rcc, gcc, node. Refusing on those made this
# guard unpassable: there is no shell on this cluster without them.
# So: refuse on anything OUTSIDE the baseline, and name it.
MOD_BASELINE="${DV3_MODULE_BASELINE:-slurm rcc gcc node}"
if [ -z "${DV3_SKIP_MODULE_GUARD:-}" ] && [ -n "${LOADEDMODULES:-}" ]; then
  unexpected=""
  IFS=':' read -ra _mods <<< "$LOADEDMODULES"
  for m in "${_mods[@]}"; do
    [ -n "$m" ] || continue
    base="${m%%/*}"
    case " $MOD_BASELINE " in *" $base "*) ;; *) unexpected="$unexpected $m" ;; esac
  done
  if [ -n "$unexpected" ]; then
    echo "REFUSING TO SUBMIT: non-baseline modules loaded:$unexpected" >&2
    echo "  LOADEDMODULES=$LOADEDMODULES" >&2
    echo "  --export=ALL leaks these into every job. Loading cuda/python here" >&2
    echo "  shadowed the conda env's pinned CUDA wheels and segfaulted every" >&2
    echo "  GPU job (2026-07-07, cost a day)." >&2
    echo "  Fix: 'module purge && module load slurm' in a fresh shell, then rerun." >&2
    echo "  Override (only if you are sure): DV3_SKIP_MODULE_GUARD=1" >&2
    exit 2
  fi
fi

command -v squeue >/dev/null 2>&1 || {
  echo "ERROR: squeue not found -- run this on an RCC login node." >&2; exit 2; }
cur="$(squeue -h -u "$USER" | wc -l)" || {
  echo "ERROR: squeue failed; refusing to guess the queue depth and risk" >&2
  echo "       submitting past the cap." >&2; exit 2; }
slots=$(( MAX_JOBS - cur ))
echo "slurm: $cur job(s) in queue, cap $MAX_JOBS, $slots slot(s) free"
[ "$slots" -gt 0 ] || { echo "at cap; rerun later"; exit 0; }

queued="$(squeue -h -u "$USER" -o '%j' 2>/dev/null || true)"
submitted=0; skipped=0; left=0


# --- lewm_probe_e4_s0_seed1 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s0_seed1; then
  echo "  SKIP already queued: lewm_probe_e4_s0_seed1"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s0_seed1"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s0_seed1 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms0_seed1",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s0_seed1.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s0_seed2 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s0_seed2; then
  echo "  SKIP already queued: lewm_probe_e4_s0_seed2"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s0_seed2"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s0_seed2 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms0_seed2",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s0_seed2.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s0_seed3 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s0_seed3; then
  echo "  SKIP already queued: lewm_probe_e4_s0_seed3"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s0_seed3"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s0_seed3 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms0_seed3",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s0_seed3.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s0_seed4 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s0_seed4; then
  echo "  SKIP already queued: lewm_probe_e4_s0_seed4"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s0_seed4"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s0_seed4 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms0_seed4",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s0_seed4.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s0_seed5 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s0_seed5; then
  echo "  SKIP already queued: lewm_probe_e4_s0_seed5"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s0_seed5"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s0_seed5 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms0_seed5",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s0_seed5.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s0_seed6 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s0_seed6; then
  echo "  SKIP already queued: lewm_probe_e4_s0_seed6"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s0_seed6"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s0_seed6 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms0_seed6",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s0_seed6.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s0_seed7 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s0_seed7; then
  echo "  SKIP already queued: lewm_probe_e4_s0_seed7"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s0_seed7"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s0_seed7 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms0_seed7",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s0_seed7.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s0_seed8 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s0_seed8; then
  echo "  SKIP already queued: lewm_probe_e4_s0_seed8"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s0_seed8"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s0_seed8 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms0_seed8",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s0_seed8.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s1_seed1 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s1_seed1; then
  echo "  SKIP already queued: lewm_probe_e4_s1_seed1"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s1_seed1"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s1_seed1 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms1_seed1",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s1_seed1.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s1_seed2 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s1_seed2; then
  echo "  SKIP already queued: lewm_probe_e4_s1_seed2"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s1_seed2"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s1_seed2 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms1_seed2",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s1_seed2.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s1_seed3 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s1_seed3; then
  echo "  SKIP already queued: lewm_probe_e4_s1_seed3"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s1_seed3"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s1_seed3 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms1_seed3",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s1_seed3.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s1_seed4 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s1_seed4; then
  echo "  SKIP already queued: lewm_probe_e4_s1_seed4"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s1_seed4"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s1_seed4 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms1_seed4",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s1_seed4.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s1_seed5 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s1_seed5; then
  echo "  SKIP already queued: lewm_probe_e4_s1_seed5"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s1_seed5"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s1_seed5 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms1_seed5",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s1_seed5.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s1_seed6 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s1_seed6; then
  echo "  SKIP already queued: lewm_probe_e4_s1_seed6"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s1_seed6"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s1_seed6 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms1_seed6",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s1_seed6.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s1_seed7 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s1_seed7; then
  echo "  SKIP already queued: lewm_probe_e4_s1_seed7"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s1_seed7"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s1_seed7 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms1_seed7",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s1_seed7.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

# --- lewm_probe_e4_s1_seed8 ---
if printf '%s\n' "$queued" | grep -qxF lewm_probe_e4_s1_seed8; then
  echo "  SKIP already queued: lewm_probe_e4_s1_seed8"; skipped=$((skipped+1))
elif [ "$slots" -le 0 ]; then
  left=$((left+1))
else
  if [ "$DRY" = 1 ]; then
    echo "  WOULD SUBMIT lewm_probe_e4_s1_seed8"
  else
    sbatch --account="$SLURM_ACCOUNT" --partition="$SLURM_PARTITION" --gres="$SLURM_GRES" --time=04:00:00 --job-name=lewm_probe_e4_s1_seed8 \
      --export=ALL,REPO="$REPO",RUNROOT="$RUNROOT",CONDA_ENV="$CONDA_ENV",MANIFEST="$MANIFEST",RUN_LOGDIR="${RUNROOT}/ax1wm_finger_fpxpxq1ms1_seed8",PROBESET="${RUNROOT}/e4_probesets/fingerpx_v1",OUTPUT="${RUNROOT}/lewm_probe/fpx_s1_seed8.json",EP_BATCH="4",PLATFORM="cuda" \
      "$REPO/scripts/lewm_probe.sbatch"
  fi
  submitted=$((submitted+1)); slots=$((slots-1))
fi

echo
echo "submitted=$submitted skipped=$skipped left=$left"
[ "$left" -eq 0 ] && echo "all submitted" \
  || echo "$left run(s) did not fit under the cap -- rerun this script when jobs drain"

