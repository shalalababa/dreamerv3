# GENERATED by scripts/ops/wavegen.py from ops/waves/turn_easy_poscontrol/spec.yaml -- do not edit by hand.

# Ship the wave dir to an instance, then queue each lane.
#   usage: submit.sh <instance>
set -euo pipefail
N="${1:?usage: submit.sh <instance>}"
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$HERE/../../../scripts/ops/lib/common.sh"
dv3_require_instance "$N"
REMOTE="$RUNROOT/_waves/turn_easy_poscontrol"

# rsync rather than heredoc: no quoting survives ssh reliably,
# and it leaves the exact submitted commands on the instance.
dv3_ssh_dv3 "$N" "mkdir -p \"$REMOTE\""
rsync -az --info=progress2 \
  -e "ssh -p $(dv3_port "$N") ${DV3_SSH_OPTS[*]}" \
  "$HERE/" "root@$(dv3_ip "$N"):$REMOTE/"

echo "-- stage adapt lane 0: 8 task(s) --"
dv3_ssh_dv3 "$N" 'DV3_ABORT_ON_FAIL=0 dv3_queue_or_add "$REMOTE/lane_adapt_0.cmds" 0'
echo "-- stage adapt lane 1: 8 task(s) --"
dv3_ssh_dv3 "$N" 'DV3_ABORT_ON_FAIL=0 dv3_queue_or_add "$REMOTE/lane_adapt_1.cmds" 1'
echo "-- stage random_floor lane 0: 3 task(s) --"
dv3_ssh_dv3 "$N" 'DV3_ABORT_ON_FAIL=0 dv3_queue_or_add "$REMOTE/lane_random_floor_0.cmds" 0'
echo "-- stage random_floor lane 1: 2 task(s) --"
dv3_ssh_dv3 "$N" 'DV3_ABORT_ON_FAIL=0 dv3_queue_or_add "$REMOTE/lane_random_floor_1.cmds" 1'

echo "submitted. verify with: dv3ops status $N"
