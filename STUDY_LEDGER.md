# Study Ledger

One entry per experiment/read: registration → record → verdict →
decision. This is the INDEX; the authoritative documents are the
per-experiment `prereg/` files (immutable after their freeze commit),
the `artifacts/<wave>/` records (RESULTS.md + machine json), and
`analysis/DEVIATIONS.md` (chronological deviations + code
notes/registrations). Plans live in `research_notes/` (gitignored);
the live status board is `research_notes/Roadmap_20260718.md`. See
`PROJECT_INDEX.md` for the file map.

Maintenance rule: append/update the relevant row in the same session
that writes an artifact record. Flags: ⚠ = later qualified;
✖ = instrument-invalidated (see the 2026-07-24 DEVIATIONS entry).

## Paper 1 — causal WM-transfer (ICLR 2027, branch causal-wm-transfer)

| Date | Experiment | Registration | Record | Verdict → decision |
|---|---|---|---|---|
| 07-02 | Gate 0 (pilot probe sets, cup/finger/reacher) | note App. A | `artifacts/gate0_20260702/GATE0_DECISION.md` | pilots pass → probe-set protocol frozen |
| 07-07 | Phase 5a (reward-free WM transfer, unblinded) | `analysis/PREREG_phase5a.md` | `artifacts/phase5a_20260707/` | cup reward-free transfer large → Axis-1 program launched |
| 07-10 | Phase 6 / Axis-1 controlled buffers | plan v3/v4 | `artifacts/phase6_axis1_20260710/` (+ `axis1_2026*` AUC snapshots) | occupancy-controlled q1 pairs established |
| 07-11 | E2 Gate F; E3 precheck; expl smoke | per-file | `e2_gatef_*/DECISION.md`, `e3_precheck_*/PRECHECK.md` | gates passed → E3 wave authorized |
| 07-11/12 | E1 reacher replication (driver-level) | corrective prereg + amds | `artifacts/e1_reacher_final_20260712/` | registered driver-level read done |
| 07-13 | P0 corrective refit (reward-free finger Q1) | `PREREG_axis1_corrective_*` | `artifacts/p0_axis1_corrective_20260713/` | **NULL → pivot fired** (plan v4, Act-1-only) |
| 07-13 | W0 fully-paired n=16 interaction | corrective Amendment 2 §A | `artifacts/w0_n16_interaction_20260713/` | **CONFIRMED +98.7\*, perm p=.0027** |
| 07-14 | E4 stratified error + Goodhart sprint | `PREREG_e4_stratified_error_20260714`, `PREREG_goodhart_sprint_20260714` | `artifacts/e4_goodhart_20260714/` | mechanism = reward-predictability of transferred features; Goodhart continue-but-weaker |
| 07-16 | W1 (E3v2 within-collector) + W2 + W3 | registered reads | `artifacts/w123_e3v2_20260716/` | **W1 REPLICATES +84.0, 14/14, p=.00012, de-confounded**; apt −5.1 = 3rd reward-free null; W2 cup +93 directional-only; W3 Q2 null → scaling pilot authorized |
| 07-17 | P3 gradient-path factorial | `PREREG_p3_gradient_path_20260714` | `artifacts/p3_wave_20260717/` | primary rgo−sgb +50 NOT confirmed at n=8; **shuffle collapse FIRES** (binding necessary); rgo +65 own-CI>0; vgo dead; E4 rew-NLL LEVEL separates arms |
| 07-17 | vgo wiring audit | — (audit) | `artifacts/vgo_wiring_audit_20260717/AUDIT.md` | case (b) replay-grounded → P-B2 shifts to attenuation form |
| 07-17 | TD-MPC2 cross-family n=8 + Goodhart ensemble + D1 pilot | `PREREG_tdmpc2_crossfamily_20260716` | `artifacts/tdmpc2_goodhart_d1pilot_20260717/` | [B_aware−B_free] +9.5 ns; ensemble does not fix top-1 regret (demote-leaning); D1 pilots 6/6 |
| 07-17 | **P3 Amendment 1, pooled n=16** | `PREREG_p3_amendment1_20260717` | `artifacts/p3_amend1_20260717/` | **PRIMARY FIRES +51.5 [+9.0,+94.6]; rgo +56.7\*; sgb +5.2 tight null → reward-gradient path CONFIRMED sufficient carrier** |
| 07-17 | Theory predictions frozen | `PREREG_theory_predictions_20260717` | (spectral model, `research_notes/Theory_SpectralTransfer_20260717.tex`) | P-A1 landed (Amend-1); P-B1 stands (stamping); P-B2 pending vgo-x; P-B2 attenuation NOT supported 07-26 (vgo absence-like at 3×; weakest reading only); P-B4 strong REFUTED (Opt-C corrective); P-B5 sign-consistent underpowered (07-26 scaling S1d; E4 leg supports representation half); P-B6-lite CONFIRMED 07-26; P-C2 premise-failed 07-24 → swamping regime CONFIRMED 07-25 (P-SW1) |
| 07-18 | Stamping | `PREREG_stamping_20260717` | `artifacts/stamping_20260718/` | primary does NOT fire → **P-B1 strong form STANDS** |
| 07-18 | Scaling Option C (volume rider) | `PREREG_scaling_pilot_20260717` | `artifacts/scaling_optc_20260718/` | ⚠ realized design ≠ registered (side inversion; DEVIATIONS 07-18); P-B4 not adjudicated; behavioral anomaly recorded |
| 07-19 | Option C corrective | `PREREG_scaling_optc_amend1_20260718` | `artifacts/optc_corrective_20260719/` | **C2 FIRES: P-B4 strong form REFUTED** |
| 07-18 | Synth Phase B | `PREREG_synth_phaseb_20260717` | `artifacts/synth_phaseb_20260718/` | collapse-only (interaction null; shuffle −226.6\*); Phase C not authorized |
| 07-18/19 | Synth diagnosis + re-diagnosis | `PREREG_synth_diagnosis_20260718`, `PREREG_synth_rediag_20260718` | `artifacts/synth_diagnosis_20260718/`, `synth_rediag_20260719/` | in-subspace refuted at model level; adapt lottery confirmed → B″ authorized |
| 07-23 | Synth Phase B″ | `PREREG_synth_phasebpp_20260720` | `artifacts/synth_phasebpp_20260723/` | NULL +30.5 [−18.7,+100.2] → **synth interaction leg CLOSED** (shuffle-collapse-only exhibit) |
| 07-19 | TD-MPC2 F1 (n=16 pooled) | `PREREG_tdmpc2_amend1_20260718` | `artifacts/tdmpc2_f1_20260719/` | null at doubled power; P-C1 PARTIAL; **family-scope limitation registered** (headline restricted to reconstruction-based WMs); G-F2 GO |
| 07-22 | TD-MPC2 F2 (E4 mechanism port) | `PREREG_tdmpc2_f2_20260720` | `artifacts/tdmpc2_f2_20260722/` | **P-F2a FIRES +1.717, lottery 0 → never-included-like**; G-F3 NO-GO; TD-MPC2 track complete |
| 07-23/24 | E4 passes: Amend-1 fits + Opt-C volume + sgb gap | E4 prereg (descriptive) | `artifacts/e4_amend1_optionc_20260723/` (+§A′) | rgo mechanism REPLICATES (1.07 vs 1.21); volume buys stability not level; **sgb gap closed 07-24: separation replicates at n=16 both arms (2.03 vs 1.07 hi side)** |
| 07-19 | Pixel X1 pair build | `PREREG_pixel_repl_20260719` | `artifacts/pixel_x1_pair_20260719/PAIR.md` | pair built → X2 submitted |
| 07-24 | **Pixel X2 read** | `PREREG_pixel_repl_20260719` | `artifacts/pixel_x2_20260724/` | **interaction does NOT fire** (+3.6 [−29.9,+40.9], p=.87; both simples null; all four cells on a common AUC floor; audit 32/32 clean) → **G-X3 NO-GO**; P-C2 premise-failed (monotone form fails; swamping regime = surviving branch); pixel leg = registered scope boundary; swamping diagnostic REGISTERED same day |
| 07-25 | **Orthogonal-objective test** (full grid, seeds 1–16) | `PREREG_orthogonal_obj_20260723` | `artifacts/orthogonal_obj_20260725/` | validity gate passed (45.3 ≥ 20); **primary rgo−sgb +1.0 [−16.1,+17.0] does NOT fire ⇒ objective-specific support** (same fits: +45.0\* on the ORIGINAL objective; ratio .023); band leg LANDED; strict headline reading stands (scope does not widen); local read ≡ cluster json |
| 07-25 | **Pixel swamping diagnostic** | `PREREG_pixel_swamping_20260724` + Amend 1 (07-25 E4 repair after `KeyError: 'image'` crash — measure keyed off model enc/dec spaces, proprio numbers bit-identical, read frozen pre-outcome) | `artifacts/pixel_swamping_20260725/` | **P-SW1 SWAMPING-CONSISTENT FIRES**: task-arm in-regime rew-NLL 23.3 [19.4, 27.3] nats ≫ 2.0 bar (floor 0.67 informative; side-robust — all 16 runs ≥ 2.0; s0 24–58 / s1 4.0–7.3); reward info ABSENT from pixel trunk ⇒ **pixel boundary = predicted structure (g > G2 regime), mechanism-complete**; membership→transfer link unchallenged; G-X3 stays NO-GO, no further pixel arms |
| 07-26 | **vgo extended-fit discriminator** | `PREREG_vgo_extended_20260723` | `artifacts/vgo_extended_20260726/` | **does NOT fire** (diff-in-diff −2.2 [−35.2,+40.2], p=.93; vgo gain +5.8 ns ≈ sgb generic +8.0 ns) ⇒ **P-B2 attenuation form NOT supported — vgo absence-like at 3×**; value gradients are NOT a carrier even with 3× time (sharpens the reward-gradient sufficiency claim); no further extended-fit arms without new theory; **PAPER-1 EXPERIMENTAL PROGRAM COMPLETE** |
| 07-26 | **Scaling Option B + 12m E4 pass** | `PREREG_scaling_pilot_20260717` (+ compcapacity predictions committed pre-read) | `artifacts/scaling_optionb_20260726/` | **PRIMARY three-way includes 0** (−30.8 [−154.5,+83.4]) ⇒ **capacity does not substitute at 12× — interaction SCALE-ROBUST** (12m interaction fires +126.5\* 7/8 seeds; robust variant vs honest W0 baseline +27.8 ns, same branch); **P-B6-lite CONFIRMED** (apt@12m +12.7 ns — 4th reward-free null, now at 12×); P-B5 sign-consistent underpowered; **P-E1 HOLDS** (no forbidden pattern; 12m = regime A ⇒ 25m targets A→B/task-lo rise); P-E2/P-E3 premise-idle; E4@12m: task hi rew-NLL 1.30 IN the 1m membership band, lo 5.8 out; d_errin ≈ 0 (arm-invariance again); apt rew-NLL absent-by-design (gate) |
| 07-25 | **Unfrozen-adaptation calibration** | `PREREG_unfrozen_calib_20260723` | `artifacts/unfrozen_calib_20260725/` | **FIRES, AMPLIFIED ×4.3**: unfrozen rgo−sgb +267.2 [+206.7,+331.4] (p=.0078 = n=8 min, every seed positive, d_z=2.77, both sides fire) vs frozen same-seed +62.5; level changes rgo +235.2\* / sgb +30.4\* ⇒ reward-legible support = better INITIALIZATION, frozen-readout was the CONSERVATIVE protocol; **band leg lands in the strong form — all four band conditions now adjudicated** |
| 07-25 | **Spectral domains read** | `PREREG_spectral_domains_20260724` + Amend 1 | `artifacts/spectral_domains_20260725/` | **P-SM1 NOT confirmed** (hi side REVERSED: rank cup 492 vs finger 455; lo holds; all 4 buffers own-θ, no fallback engaged) ⇒ spectral DOMAIN account refuted as registered; cup/finger = unexplained scope condition; theory E5 cup section loses measurement support; arm-level results untouched per frozen map |
| 07-30 | **U2 stamped unfrozen** (48 jobs) | `PREREG_unfrozen_u2_20260727` | `artifacts/unfrozen_stress_u234_20260730/` | **PROTOCOL-ROBUST**: P-U2 [B_srd−B_sid] −1.3 [−25.5,+21.0], no flip — the installed stamp is inert even under full fine-tuning ⇒ **"inclusion ≠ usefulness" = TWO-PROTOCOL fact**; UNREAL/RaMP refutation strengthens; P-B1 standing improves; descriptor: sid controls gained most from unfreezing (+32.9*/+26.8*), stamped cells less; config audit 48/48 |
| 07-30 | **U3 orthogonal unfrozen** (32 jobs) | `PREREG_unfrozen_u3_20260727` | `artifacts/unfrozen_stress_u234_20260730/` | **PROTOCOL-ROBUST**: P-U3 rgo−sgb on spin +5.3 [−13.6,+23.6], pooled level 67.9 ≥ 20 gate — no flip ⇒ **objective-specificity = INITIALIZATION fact** (two-protocol standing); strict headline strengthens; rgo0 level +36.4* from unfreezing but not differential by arm; config audit 32/32 |
| 07-30 | **U4 pixel unfrozen 2×2** (32 jobs) | `PREREG_unfrozen_u4_20260727` | `artifacts/unfrozen_stress_u234_20260730/` | **FLOOR-CENSORED** (registered guard): P-U4a relief +1.7 [−10.6,+13.4] does not fire — pixel cells stay at the X2 floor (76–88 vs frozen 77–83) even fine-tuned, unlike proprio (+235/+30) ⇒ interaction unadjudicable; frozen pixel floor binds; no further pixel-unfrozen compute pre-deadline; G-X3 NO-GO unchanged; P-SW1 untouched; config audit 32/32 |
| 08-02 | **Pretrained-encoder pixel arm** (16 frozen-enc fits + 16 adapts + E4) | `PREREG_pe_pixel_20260730` | `artifacts/pe_pixel_read_20260802/` | **NO-RELIEF (registered honest negative)**: P-PE1 in-regime rew-NLL 23.03 [20.14, 25.68] ≡ the swamping baseline 23.34 [19.41, 27.29] — removing the encoder from the gradient competition entirely moved reward-NLL by NOTHING; P-PE2 no lift off the X2 floor (−8.5 [−26.0, +9.6]); P-PE3 premise-idle ⇒ the swamping account's causal lever FAILS its first intervention (measured fact P-SW1 stands, NOT re-adjudicated); U4's pretrained-encoder decision resolved — pixel floor survives encoder pretraining; integrity sweep 16 PASS w/ training witness (counter-skip trap excluded); no further pixel arms queued (account revision first) |
| 08-04 | **Recon-detached pixel arm (rde)** (16 trainable-trunk fits w/ `recon_grad False` + 16 adapts + fpxpx calibration + E4; ONE read; manifest 154/154; freeze 87196fe3 ancestor of run head 0a3774cc, tree clean; all 5 instrument shas byte-identical; smoke gate 3/3 machine asserts incl. ARM-path `recon_grad False` + deter_std 0.0229 > 0; **disclosed deviation: registered persisted config-audit log missing from bundle — registered fail-closed script executed locally at read on the bundle's 16 fit configs, 16/16 PASS, log archived in the artifact**; COLLATE echo = overridden rde csv; counters 500000/500000 ×16) | `PREREG_rde_pixel_20260802` | `artifacts/rde_pixel_read_20260804/` | **PARTIAL-RELIEF (registered middle branch): P-RD1 in-regime rew-NLL 7.47 [5.74, 9.55] — CI entirely below the baseline's lower bound 19.41 but far above the 2.0 swamping bar** ⇒ removing reconstruction's gradient competition (trunk trainable, decoder on sg) moves reward-NLL by ~15.9 nats (~74% of the nats-to-bar, descriptive) — **the FIRST intervention in the pixel arc that moved reward-NLL at all** (pe moved it by nothing) — but does NOT restore inclusion; latent-alive witness ALL 16 PASS (no COLLAPSE qualifier; threshold 0.0167, min fit 0.0297); P-RD2 NO behavioral lift off the X2 floor (−9.6 [−22.5, +3.5]); P-RD3 premise-idle. Registered reading: **directional support only, no headline change** — competition account NOT retired (that was the NO-RELIEF branch) and NOT confirmed; P-SW1 not re-adjudicated; the two levers now DISSOCIATE (features-lever dead, competition-lever moves representation but not behavior). Post-read exploratory (labeled, verdict-untouchable): strong side asymmetry — deter_std s0 0.03–0.18 vs s1 0.43–0.49; P-RD2 deficit concentrated in s1 (60.7 vs baseline 82.6; s0 ≈ baseline). No follow-up compute queued by this branch — **Paper-1 registered compute COMPLETE** |
| 08-04 | **EXPLORATORY trunk-breadth probe** (labeled post-read, verdict-untouchable; `probing/feature_pr_probe.py` — h0-posterior feature covariance spectra over the frozen probeset, verbatim witness extraction; 48 runs = rde+X2+fpxpx ×16; manifest 54/54; **witness_match TRUE 32/32 gated** — extraction ≡ frozen deter_std witness on real data; prediction stated in-chat pre-outcome) | — (exploratory; no registration) | `artifacts/feature_pr_exploratory_20260804/` | **NARROWING CONFIRMED, side-resolved: rde deter-PR less than half of X2 BOTH sides (paired −8.2*/−8.1*) at better legibility; head-input space (feat2tensor, what the reward head consumes) collapses 44→9 on s1 (−34.9 [−36.1, −33.5]) — exactly the side carrying the rde behavioral deficit (AUC −21.9) — while s0 keeps breadth (+4.8 ns, stoch-dominated) and keeps its AUC ⇒ breadth loss and competence loss co-localize across sides while legibility improves on both: transfer needs breadth, not just legibility (the narrowing account of P-RD2's null).** Frontier exhibit: fpxpx broad-illegible → X2 broad-swamped → rde narrow-legible. Descriptives: X2 pooled 23.34 is side-bimodal (41.2/5.5); rde s0 6/8 seeds near-rank-2 deter converging to an identical 8.33-nat solution, the 2 deter-recruiting seeds WORSE (19.9/22.5); corr(deter_std, pr_deter)=−0.08 (scalar witness ⊥ breadth). Figure `fig_feature_pr.png` (+ `research_notes/figures_theory_20260804/`). Non-causal, n=8/cell; rde verdict untouched |

Standing Paper-1 decisions: headline = "support must be legible to the
objective" (17 Jul editorial); claim band 40–55% — **all four conditions
adjudicated 07-25**: n=16 ✓ + TD-MPC2 ✗ (explained boundary) +
unfrozen calibration ✓ (**AMPLIFIED ×4.3** — frozen-readout was the
conservative protocol) + orthogonal test ✓ (**objective-specific**);
**Goodhart DEMOTED** (18 Jul, re-confirmed
22 Jul — negative/boundary exhibit only); 9a un-embargoed (Amend-1).

| 08-08 | **FULL-RECORD REVIEW RESOLUTION** (response to the 12-lens 07-Aug review; every BLOCKING/MAJOR verified — ALL CONFIRMED, incl. exact reproduction of D1 overlaps finger q1s1 7/60 / cup 1+8/60 / pixel 8+5/20, D8 −0.811, D11, D14 all 4 snapshot rows, Part-III statistics line-for-line) | — (labeled post-read response; no verdict re-executed) | `artifacts/review_response_20260808/` (RECORD + d1_overlap + zero_compute + stats_readjudication + scripts); record corrected IN PLACE w/ dated marks + §13 appendix; disposition map `research_notes/paper1_wm_transfer/reviews/Review_Resolution_20260808.md` | **Key outcomes:** (1) **D1 probe-holdout FALSIFIED as claimed** — §2.7 corrected; pixel leak-excluded sensitivity rde 7.47→8.26 / pe 23.03→24.07 (verdicts unchanged); `--holdout` guard added to all 5 search modes (selfcheck PASS); proprio recompute queued (instance). (2) **Spectral instrument was 97.7% collector latents** ⇒ P-SM1 re-graded instrument-invalid (domain account untested); `spectral_v2_20260808` shipped; volume/f_R mediators + breadth 4.84-PR caveated (proprio-block 0.736). (3) **Stats re-adjudication:** percentile CI at n=8 = ~86–90% interval (sim); U1a → SUGGESTIVE (perm .086, sign .727); rgo-n8 wording retired (pooled n=16 +56.7 robust is citable); carrier +51.5 fails BH (5 survivors: W1/W0/Phase5a-occ/P0/unfrozen-level) + fails Pocock (two-look α≈15%) ⇒ **estimand split adopted: rgo>sgb LEVEL contrast is the strong claim, interaction-carrier is marginal**; power-honest nulls = W0-apt (TOST .005) + orthogonal (.003); stamping/vgo not equivalence-licensed; W0 batch effect p=.048 disclosed; superadditivity withdrawn (winner-cursed); floor-censoring labels retired (pixel 80.9 ≡ proprio-apt 81.3, Welch .95). (4) **Zero-compute publishes:** random-policy floor 83.48±15.10 (all reward-free cells inside ⇒ null = NO LEARNING); apt-unfrozen 0.54×/0.61× of scratch w/ CIs entirely negative; log-interaction 1.749× [1.24,2.49] p=.009 (pixel log-null 0.943) — claim licences drafted `PREREG_paper1_reframe_claims_20260808` (**awaits [YOU] freeze-commit**); volume 2×2 completed n=12 (C1 +23.3→−61.1 sign flip; "occupancy saturates" retired; reward-free volume +21.8 [+0.2,+44.4] CI now carried); rew_nll_out panel (rl most-confidently-wrong out-of-regime ⇒ D6 confirmed). (5) **Fig 2a was WRONG (R1: plotted U3 spin runs as rgo/sgb) — rebuilt** from unfrozen_calib: rgo-unfrozen 348.9/380.4 ≫ scratch 147.7, sgb/apt at floor. (6) **Goodhart evaluator double-symlog (D18) confirmed + fixed** — track suspended pending re-collate. (7) sid-is-stamped (D5), rl one-directional (D6/D7), synth by-construction (D20), TD-MPC2 7-confound (D21), pixel ¼-budget/base-rate/capacity (D3/D4/D19), replay-context + 12m zero-context rule-out (D9), 3 side inversions (D10) — all disclosed in record. (8) Related-work §9.7 REWRITTEN (5 published frozen-transfer positives; objective-taxonomy defence: transferable reward-free methods are successor-structured); practical corollary re-scoped vs Zhang RLC'24; HarmonyDream must-cites + s/w_r theory-instrument mismatch (`ln(1+s)²f` not `s²f`) recorded pre-any-s-wave. **STANDING RULES added: permutation-primary + BCa for all future reads; readers verify n_ep_100k vs modal; new searches pass --holdout.** Measure-pass queue (9 items) + wave candidates (#15 random-init control first) in the resolution doc — [YOU] calls |

| 08-08 | **REVIEW-RESOLUTION EXECUTION WAVE** (user GO: "everything helpful except expensive-low-value"; slate in `reviews/Review_Resolution_20260808.md`) | 5 new preregs (uncommitted, ONE batch reviewer running): `PREREG_randominit_control_20260808` (#15, 8 adapts, unfrozen arm = existing scratch, from_checkpoint=''+frozen_wm verified checkpoint-independent) · `PREREG_inprotocol_external_20260808` (#20, 10 adapts on the 5 pretrain_p2e donors — Plan2Explore in-protocol) · `PREREG_orthogonal_poscontrol_20260808` (#18 turn_easy leg, 16 adapts on existing rgo/sgb fits; floor-confound branch registered = U3 downgrade path) · `PREREG_tm2_mpcfalse_20260808` (#24, 32 adapts, `tdmpc2_adapt --mpc` flag added — readout de-confound of the family boundary) · `PREREG_swave_theory_correction_20260808` (**freeze BEFORE any scaled buffer**: instrument-form `ln(1+s)²·f`, s×w_r pairing mandatory, own-label scoring mandatory, dose crossings {1, 2.45, 4.46} × w_r {1,10,100}) | shared frozen reader `analysis/review_waves_read.py` (4 subcommands, permutation-primary, modal-n_ep + dup guards, branch-crossing selfcheck PASS) + instruments: `probing/ridge_probe.py` (#9/#10/#12 — OOF ridge + AUROC + recal-NLL + witness gate, selfcheck PASS), `relabel_replay` `scale` kind + `transform-probeset` own-label overrides (selfcheck PASS incl. support-preservation + determinism), `target_angle_audit.py` (smoke PASS) | **d_errin BY-KEY (#4) EXECUTED from existing summaries (zero compute): aggregate arm-invariance hides a per-key reward-gradient fingerprint — `dist_to_target` out−in ≈ −0.018 for EVERY non-reward-gradient arm (apt/sgb/vgo/sh/rl/sid/srd) vs ≈ 0.000 for task/rgo on the leak-free lo side** (labeled exploratory; first obs-space trace of the carrier; sharpens not weakens "aggregate modelling is not the carrier") → `artifacts/review_response_20260808/d_errin_by_key.json` + record §4.9 addendum. NO'd: #25 dwell (unless #17 nulls), #28, #23 12m carriers. Staged GO: #17 cup quadrant, distractor-dose, #16, #22, #26, #21-conditional. **BATCH REVIEW (ONE reviewer, same day): 4 BLOCKING / 5 MAJOR / 8 MINOR / 1 NIT — ALL APPLIED, selfchecks re-PASS.** B1 multi-domain csv contamination (reader domain-filter + csv-list merge + fixture); B2 tm2 audit dump hardcoded mpc=True (now echoes cfg.mpc — contamination would have been undetectable); B3 ridge_probe crashed on reward-free ckpts (no rew params — gated on reward_aware); B4 orthpos donors DELETE-SAFE + cleanup already run (donor preflight + refit-fallback amendment path + survivors re-protected in KEEP-PENDING); M5 P-RI1 upgrade needs perm-ns AND |Δ|≤15.1; M6 tm2 wave = **64** adapts (n=16 assert in reader); M7 p2e donors 500K-step; M8 orthpos floor now MEASURED (5 rndte turn_easy random runs added — turn_hard band does not transport) + significantly-negative branch; M9 swave constant → (ln11/ln2)²≈11.97×; minors incl. --expect_ep majority-truncation guard, qc-fail exclude-not-crash, transform-probeset byte-sha verify. **Cluster preflights REQUIRED pre-freeze: p2e ckpts, rgo/sgb seeds1-4 donors, tm2wm ×64** **DATA-LOSS DISCLOSURE (08-08 pm, user-confirmed): (a) `adapt_ax1uzfq1s*_finger_seed*_ckpt500000` GONE not recoverable — U1 verdicts stand on archived bundle, ckpt-level re-analysis foreclosed, #27 conditional on surviving apt uzf snapshots; (b) rl+sh WMs GONE ⇒ measure pass #11 DEAD, D6 (rl own-labels) PERMANENTLY OPEN — record corrected to "never measured, no longer measurable"; (c) `target_position` never existed in cup buffers (finger_turn_hard observable) ⇒ target-angle audit finger-only. No registered wave touches lost items; freeze-commit set unaffected. **SAME-DAY RESPONSE (user GO): `PREREG_rlsh_ownlabel_refit_20260808` — rl/sh REPLICATION wave (32 fits on the P3 grid, label processes deterministically reproduced relabel-seed-0; own- vs true-label E4 to NEW dir e4_finger_v1refit; P-RS1/RS2 paired own−true contrasts = D6 population-level closure; S2 = d_errin fingerprint out-of-sample replication; NO adapts, NO reviewer — rides just-reviewed instruments, skip disclosed; re-fit names re-protected KEEP_PENDING; preflight q1 chunks; explicit replication-not-recovery wording — Midway3 non-determinism). FRAMING DECIDED (user: maximize upside): DUAL-LEAD — negative-transfer empirical hook + inclusion≠usefulness mechanistic thesis (phenomenon→mechanism); #15/#20 outcomes adjust wording strength only, never the structure (record §13). **LOSS 3 (storage-agent verdict): ax1wm_cup_* ALL fitted cup WMs removed WITHOUT archive (cup ridge #12/P-C3 errored) — but axis1_cup buffer tree (5.2G) + 15 cup collectors + d1pilot_cup_e1 SURVIVE archived ⇒ `PREREG_cup_refit_20260808` REGISTERED same day: 32 regenerated fits (task+apt, byte-identical buffers, 11-Jul-corrective protocol) + ridge/E4 to NEW dirs; P-C3 parity-vs-finger rule frozen FULLY PROSPECTIVE (no ridge number exists in either domain); preflights = restore sha verify + cup_v1 probeset manifest-identity; KEEP_PENDING re-protection; NO adapts/reviewer. Cup ridge pass re-sequenced AFTER regeneration; finger ridge passes unchanged (they are the finger side of the P-C3 contrast). **FULL LOSS INVENTORY (08-08 late, storage agent; 2/4 ridge tasks had failed on missing files): UNRECOVERABLE = finger q1 MAIN ARMS (task+apt all seeds!), rgo/sgb 9-16, rl/sh/vgo, all cup WMs, finger q2 fits+adapts, ALL U1 uz adapts, descendant adapts; ARCHIVED byte-identical = rgo/sgb/sid/srd0/srd1/s12/fs12/s25/fs25/pxpx seeds 1-8, tm2wm ×64 + tm2_data, 9 collector families (walker/reacher/cup — NOT finger), axis1_cup buffers, d1*/r3_reacher; NEVER LOST = fpxpx, axis1_finger, r3_local, all bundles/manifests/artifacts ⇒ NO registered result affected. RESPONSE: `PREREG_finger_refit_20260808` (32 fits task+apt on never-lost axis1_finger/q1; #9 apt-trunk ridge rule frozen PROSPECTIVE: present-but-unusable vs absent-or-nonlinear, task = calibration arm; P-C3 finger side; anchors = restored rgo/sgb 1-8; KEEP_PENDING added); cup prereg finger-side reference amended pre-commit; ALL ridge passes now post-regeneration; #18/#24 preflights → verified archive restores (byte-identical, no amendment needed); #27 RETIRED-UNRECOVERABLE; #20 donors (pretrain_p2e_finger) STATUS UNKNOWN — in neither list, check before freeze; pepx/rdepx also unknown (#10 panel) → **BOTH CONFIRMED PRESENT same day (user check): #20 preflight PASSES, #10 panel on originals — freeze-commit fully unblocked, no amendment needed** |

## Paper 2 — EVPI / value-of-computation (off ICLR path)

**⚠ 2026-07-24 INSTRUMENT INVALIDATION**: the shared D1 real-op labeler
carried two defects (stale-carry candidate branches; policy-RNG not
common across branches) + audit found a third (xpol trajectory
prevact). ALL D1 decision-value verdicts below marked ✖ are withdrawn
as evidence (faithful reads of an unintended estimand); frozen
RESOURCE decisions stand. DEVIATIONS 07-24 entries = tracked record.

| Date | Experiment | Registration | Record | Verdict → decision |
|---|---|---|---|---|
| 07-10 | Latent-UQ Stage 0 | Gate-D0 App. A | `artifacts/latent_uq_stage0_20260710_221232/DECISION.md` | attractor bias found (disagreement → density-tracking at convergence); phase change + cross-policy boundary |
| 07-14 | Stage 1A (density-residual instrument) | `PREREG_gate_d1_stage1a_20260714` | `artifacts/latent_uq_stage1a_20260714/` | registered battery PASSES (10/10 flip + 4 cross cells) → renamed **density-deconfounded disagreement residual** (diagnostic, not per-state gate; audit-verified leakage-free) — STANDS |
| 07-18 | Gate D1 (Stage-1B labels, 24+24 unfreeze) | Stage-1A prereg criteria | `artifacts/gate_d1_20260718/` | ✖ FAILS 0/4 → 24+24 frozen (resource decision stands; verdict uninterpretable) |
| 07-23 | Route-A R2 decision probe | `PREREG_gate_d1_r2probe_20260722` + Amend 1 (imag demoted pre-read) | `artifacts/gate_d1_r2_20260723/` | ✖ NULL 16/16 → **Route A CLOSED** (resource decision stands); imag-op defect confirmed independently (retired — stands) |
| 07-23 | Actionability ladder | `PREREG_d1_ladder_20260723` | `artifacts/d1_ladder_20260723/` | ✖ no compression signature → ensemble arm stays gated (off-by-default stands) |
| 07-24 | Shift-consequence probe | `PREREG_d1_shift_20260723` + Amend 1 | `artifacts/d1_shift_20260724/` | ✖ neither arm fires (xpol also carries the prevact defect) |
| 07-24 | Instrument correction + relabel | `PREREG_d1_relabel_20260724` (+ audit additions) | → 07-25 read | labeler `d1fix_20260724` built + registered; 18 relabel passes on existing shift pilots |
| 07-24 | Relabel Amendment 1 — d1pilot cohort | `PREREG_d1_relabel_amend1_20260724` | → 07-25 read | Gate-D1 pilots confirmed alive (same 1e5 maturity) ⇒ +18 passes; replication family + pooled-12 headline; frozen d1s primary UNCHANGED |
| 07-24 | Relabel Amendment 2 — schema-drift loader fix | `PREREG_d1_relabel_amend2_20260724` | → 07-25 read | amend-1 smoke crashed (`model_obs` absent from 18-Jul configs); `load_config` backfills absent keys from defaults; backfill logs on the real campaign show ONLY registered keys (7-key subset early, 8 later — checkout drift, all inert) |
| 07-25 | **R3 consumer-competence factorial REGISTERED** | `PREREG_r3_competence_20260725` | → 07-29 read | {cup,finger}×{e1,e4}×seeds 31–38, early(2.5e4)+late(1e5) maturities, base arm, oracle_all; P-R3a opp>0.2 / P-R3b gap>0 / P-R3c maturity-buys-competence; reacher conditional-GO (all-or-nothing cohort); power pre-sized (n=32 detects gap 0.42 = half observed); driver `scripts/r3_local.sh` (dosed-smoke GATE before labels), read `analysis/r3_read.py` selfcheck PASS |
| 07-25 | **Corrected-instrument relabel READ** (both cohorts, ONE execution) | relabel prereg + Amend 1 + Amend 2 | `artifacts/d1_relabel_20260725/` | **neither arm fires in ANY family** (d1s: xpol +0.24 [−0.33,+0.77]; phys −0.19 [−0.34,−0.06] — negative, no fire under one-sided rule, sign flips in replication; pooled-12 both null) ⇒ **corrected negative at 12 clusters — the consequence leg fails ON THE CORRECTED INSTRUMENT**; attenuation objection discharged up to power (t-intervals disclosed); defect-impact: old labels inflated base/phys (+0.20/+0.15 → −0.01/−0.20); **opportunity/achieved: opportunity large everywhere (xpol finger +6.3), achieved ≈ 0 ⇒ implementation gap = R3's target, parameterized**; next = R3 registration |
| 07-29 | R3 Amendment 1 — reader seed-grid repair | `PREREG_r3_amend1_20260729` | commit bbe86983 (pre-read) | frozen reader had `EXPECT_SEEDS`=1–8 vs registered 31–38 (crash before any value touched; selfcheck structurally blind — fixture built FROM the constant); value-blind repair + 4 integrity guards (unregistered-seed, finiteness = oracle_all signature, m_real bounds, n_states=200), decision rules verbatim-unchanged; 3-agent value-blind bundle audit clean (64/64 labels exact dials/doses, smoke gate 4/4 dosed, 32/32 two-phase continuation verified) |
| 07-29 | **R3 consumer-competence READ** (ONE execution, RCC) | `PREREG_r3_competence_20260725` + Amend 1 | `artifacts/r3_competence_20260729/` | **P-R3a FIRES** (opp +1.247 [+0.700,+1.908] > 0.2) **+ P-R3b FIRES** (gap +1.293 [+0.723,+1.987] ≈ 3× the pre-sized detection bar); **P-R3c null** (+0.014 [−0.177,+0.215]) ⇒ **CONSUMER-COMPETENCE FAILURE registered: opportunity exists, is not harvested, and maturity does not close the gap** — 25-Jul shift negative = competence ceiling, NOT information ceiling; Paper-2 headline = opportunity/competence decomposition; domain-robust (cup gap +0.917\*, finger +1.669\*; finger achieved −0.157 CI<0 = harvest slightly counterproductive); opportunity survives dose (e4 +0.98\*) and maturity (late +1.30\*); floors 25%/19% not limiting; local verification re-execution bit-identical; 24+24 frozen / Route A closed / imag retired reaffirmed |
| 07-31 | **R3 reacher third-domain READ** (ONE execution, local; manifest verify OK 14,554 files == committed pin; code snapshots ≡ frozen; selfcheck PASS pre-read) | `PREREG_r3_reacher_20260729` (freeze 596a7f93) | `artifacts/r3_reacher_20260731/` | **REPLICATES — P-RRa FIRES** (opp +0.828 [+0.565,+1.114] > 0.2) **+ P-RRb FIRES** (gap +0.813 [+0.554,+1.097]); **P-RRc null** (+0.047 [−0.023,+0.121]) ⇒ **the opportunity/competence decomposition is a THREE-DOMAIN fact (cup, finger, reacher); the conference-band task axis LANDS**; maturity-does-not-buy-competence replicates per domain; opportunity survives dose (e1 +0.99\*, e4 +0.66\*) and maturity (early +0.68\*, late +0.97\*); pooled achieved +0.015 [−0.038,+0.071] tight null; **floors 1/32 (3.1%) — the registered sparse-floor risk did NOT materialize** (gate bar 50%); 24+24 / Route A / imag invariants reaffirmed |
| 07-31 | **R3 candidate-aware ladder READ** (ONE execution, local CPU 21m30s; parent freeze 6271c770 + pre-read Amendment 1 commit 5a16c728 triggered by the independent review; reader ≡ HEAD at execution; in-read digest gate matched frozen 02efe6c5…) | `PREREG_r3_ladder_20260730` + `PREREG_r3_ladder_amend1_20260731` | `artifacts/r3_ladder_20260730/` | **NO PER-STATE SIGNATURE (registered flat branch): the implementation gap is NOT state-legible at this information level — policy-level constant, not a per-state opportunity map**; no compression signature (L2 ≥ L3/L4 in the e1 cells — scalars already carry the little legibility there is) and no candidate signature ([L4−L3] ≈ 0 everywhere, max +0.040 with CI lower bound +0.000); weak sub-bar legibility real (L2 ≈ +0.16\* both e1 cells; early_e4 L4 +0.163\*) but all rungs < 0.2; late_e4 flat at every rung ⇒ **competence-repair proceeds with its registered DEFAULT targeting**; amendment VALIDATED on real data (pooled diagnostics non-evidential: early_e1 L1 sign-flip +0.100\* within vs −0.130 pooled; late_e4 L3 −0.014 within vs −0.248 pooled — the confound was live); nan runs (all-floor) 5/10/1/7 ⇒ live clusters 11/6/15/9, floors concentrate in e4+early; 48 registered looks + 40 pooled diagnostic points; json sha 3cff90dc… |
| 08-02 | **Competence-repair READ under Amendment 1** (ONE execution, local; bundle `local_results/r3_repair_20260802_143054/` manifest 0-mismatch; code shas ≡ committed finals incl. labeler 6561eff9…; REP_GATE/REP_DETGATE OK, 32/32 passes; model sha be802f1e… matches the frozen pin; state-identity violations 0; selfcheck PASS pre-read) | `PREREG_competence_repair_20260730` + `PREREG_competence_repair_amend1_20260801` | `artifacts/repair_read_20260802/` | **NOT-REPAIRABLE-FROM-OBSERVABLES: P-REP1 +0.070 [−0.092,+0.245] straddles 0** (n=32; sensitivity leg n=31 excl smoke cell +0.074 — NOT fragile); achieved_rep +0.011 / in-pass probe −0.060 / committed cross-era −0.039, all ≈0; chooser change rate 83.3% (the LORO model re-picks constantly — it just adds no value); residual gap fraction 99.3%; cross-era drift context exactly as the audit predicted (probe-vs-committed agreement 32.6%, opportunity max\|Δ\| 97.0 — gates nothing) ⇒ **a LINEAR probe on stored observables (deter, action, qfull, udyn) does not close the competence gap; representational account strengthened (support must be legible cross-link); scope-limited by registration — NOT "no repair possible"** |
| 08-02 | **Cross-consumer (xc) READ under Amendment 1 — within-pass dual-chooser** (ONE execution, local; bundle `local_results/r3_xc_20260802_135102/` manifest 0-mismatch; code shas ≡ committed finals; XC_GATE/SMOKE/DUALGATE OK; 64/64 dual labels; integrity violations 0; cands_max_absdiff 0.0 — in-process RNG replay EXACT where cross-job floats drift O(100), vindicating the within-pass redesign; selfcheck PASS pre-read) | `PREREG_r3_xc_amend1_20260802` (parent `PREREG_r3_amend2_20260730` retired unexecuted) | `artifacts/r3_xc_read_20260802/` | **MIXED — off-map CI excursion: P-XC1 late-eval +0.028 [−0.216,+0.244] straddles (matches the registered HEADS-IRRELEVANT prediction); P-XC2 early-eval −0.155 [−0.311,−0.032] ENTIRELY NEGATIVE where the registered fire direction was + ("mature heads rescue") ⇒ verdict MIXED per the frozen map: disclosed, NO headline claim, interpretation deferred.** Direction domain-consistent (cup −0.146\*, finger −0.164 straddling same sign); chooser disagreement 75–85% (swap real, no SUSPECT-NO-OP); opportunity matched across sides (1.37/1.36). Mature heads transplanted onto an early agent REDUCE achieved value — consistent with heads-features-occupancy co-adaptation (interface mismatch caveat registered symmetric to the HEAD-DEFICIT caveat); any interpretive follow-up (e.g. interface-controlled swap) = NEW registration; regardless-clauses reaffirmed (committed reads stand, labels never re-read, 24+24 frozen, Route A closed, imag retired) |
| 08-02 | **XC2 excursion decomposition** (EXPLORATORY post-read on the adjudicated xc bundle; plan fixed in chat pre-execution; d_pair anchor matches the read exactly both sides) | — (exploratory; cannot alter the MIXED verdict) | `artifacts/xc2_decomposition_20260802/` | **The excursion's significant carrier is the PROBE-PICK term: real_term −0.152 [−0.269,−0.052] run-clustered (harm story confirmed); now_term +0.004 ns (self-consistency story rejected at run level). DIRECTIONAL: late→early damages (−0.152\*), early→late is value-neutral (+0.018 everywhere) — generic swap noise can't do this (now_term IS side-symmetric: non-floor +3.0/+2.2 both sides = optimizer's-curse decorrelation, mechanical).** Floors 93%/88.5% (17/32 early runs constant-opp); non-floor 7% (opp≈19.6) carries d_pair −4.27 = real −1.27 vs now +3.00; floors DILUTE (excursion would be ≈−0.30 without them). Concentration: finger/e1 holds the 4 worst runs (real_term to −1.15); per-domain real_term finger −0.240\*, cup −0.063. ⇒ interface-controlled swap (aligner-mediated transplant) now WELL-POSED as a NEW registration (separates interface-mismatch vs value-knowledge-mismatch — the registered caveat's two branches) but **DEFERRED (user 2 Aug: revisit at Route-B §6 drafting or on reviewer pressure; no default GO)**; meanwhile the anatomy folds into Route-B §6 as disclosed MIXED + excursion, no new compute; nothing here is confirmatory |
| 08-03 | **TM2-R3 TD-MPC2 cross-family replication** (ONE read; manifest 370/370; RCC head 481aced9 clean; all 5 instrument shas byte-identical; SMOKE_OK + API smoke + 4 dosed smokes reader-enforced; 32 runs TRAIN_DONE, 64/64 labels; FILL audit executed at read — 64/64 planner blocks exact, num_q 5 / latent_dim 512, steps 25000/100001 ×32; **disclosed deviation: the registered pre-labels FILL amendment file was never created — data bundled, audit discharged at read, no decision quantity touched**) | `PREREG_tm2_competence_20260730` | `artifacts/tm2_r3_read_20260803/` | **CROSS-FAMILY REPLICATION (strongest registered branch): P-TM2a opportunity +0.546 [+0.325,+0.808] FIRES (bar 0.2) + P-TM2b gap +0.530 [+0.315,+0.787] FIRES — the 512-sample 6-iteration MPPI planner leaves the one-real-step oracle opportunity on the table (pooled achieved +0.016 [−0.036,+0.074], a tight null: it harvests essentially NOTHING); P-TM2c maturity null (−0.034) = the dv3 maturity null reproduced.** Same shape as DreamerV3 in every registered secondary: dose GROWS opportunity (e1 +0.304 → e4 +0.789), both domains fire same-direction (cup gap +0.777\*, finger +0.283\*), floors pass (cup 0%, finger 15.6% < 50%). Effect ≈42% of the dv3 gap, above the ≈0.42 pre-sized bar ⇒ **decomposition = TWO-FAMILY regularity incl. a categorically stronger consumer class; HARVESTED-BY-PLANNER boundary did NOT materialize; Paper 2's strongest generality leg lands; the user's venue rule "ICML if TD-MPC2 repl fires" has its condition MET (call stays ~Dec)** |

| 08-09 | **PAPER-2 DUAL-REVIEW RESOLUTION** (internal 12-lens 07-Aug + external GPT_Diagnosis 09-Aug — independent convergence; every checked claim CONFIRMED: 8 code [V], 9 data [V], 3 Stage-1A [V]) | — (labeled post-read response; no verdict re-executed, no frozen reader touched) | `artifacts/review_response_p2_20260809/` (RECORD + p2_verify_A/B jsons + scripts); record corrected IN PLACE + §13 appendix; disposition map `research_notes/paper2_evpi/reviews/Review_Resolution_20260809.md` | **HEADLINE ESTIMAND CONSTRUCT-INVALID: G = ONE stochastic realization (follower samples; --rollouts feeds D0 signals) ⇒ opportunity ≡ max−mean (1.247 vs 1.268); choosers at chance (rank 4.508/4.5; ±0.025 of mean); candidate-permutation null REPRODUCES headline (+1.233 [+0.700,+1.865]); CROSS-FIT (select on committed pass, evaluate on repair re-pass) COLLAPSES it: within +1.3014 ≡ registered → cross −0.105 [−0.290,+0.066], low-drift half +0.011; g_all_boot +2.881 (noise doubles "opportunity"); variogram flat.** Re-grades: P-R3a/b, P-RRa/b, P-TM2a/b → CONSTRUCT-INVALID; P-R3c → NOT ESTIMABLE (84% exact-zero pairs); doubling → noise-account prediction; **D1: TM2 planner NEVER the consumer (m_now = plug-in argmax; planner = follower only) ⇒ 08-03 row's cross-family claim + ICML-condition-MET are SUPERSEDED — venue condition NOT met**; D2 repair chooser = 4-DoF tilt (belief latent tiled out) ⇒ NOT-REPAIRABLE re-scoped; D3b env UNSEEDED (drift root-cause story corrected; quarantine decisions stand); D3c delta_real stdout leak disclosed; Stage-1A = comparator-flip (raw +0.272/+0.434 → resid +0.267/−0.166 [V]); ladder → UNDERPOWERED (target reliability 0.086); XC2 asymmetry → dilution artifact; bars 0.2/0.42 provenance corrected. SURVIVES: instrument+methodology contribution; **identified dissociation (value ensemble CANNOT rank own candidates −0.017 ns vs probe score +0.023* CI>0; non-floor achieved +3.39* with plug-in below random −2.76*)**; TM2 bias-corrected residue +0.34 [+0.19,+0.51]; mechanism arc reduced (pcorr(D,E|ρ) positive-attenuating, phase 8/10). Must-cites: Berseth 2508.01329, Brennan 2007, Smith-Winkler 2006, Fenwick 2008, Muppidi. **[YOU]: framing call (dissociation / instrument-crisis / rescue — rec (a)+(b) composed + wave regardless); W1 adjudication-wave GO (repeated-rollout select/eval split + duplicate-candidate null + TM2 planner-as-consumer + actor-as-consumer, ~1-2 GPU-d, seeded env, ONE reviewer); freeze-commit.** Route-B writing PAUSED at framing layer. Ledger backfill queue: doubling/second-k×2/repair-quarantine/xc-detprobe rows + DEVIATIONS August entries. **08-09 late (user GO "all three" + mechanism directive): FRAMING DECIDED (a)+(b) composed, W1 regardless, surprise/top-venue priority. MECHANISM MINING (labeled; p2_mech_C/D jsons): (1) "cannot rank" = LOCAL FLATNESS, leak-free — ensemble candidate spread ≈5% of own head noise BOTH families (does not discriminate, not misranks); (2) NEW BEYOND BOTH REVIEWS — TM2 duplicate-action groups (true signal ≡0) give Spearman(real_scores,g_all)=+0.47 [+0.39,+0.57] = the shared-rng_mark leak witness (r_real ∈ G[m]) ⇒ dissociation's probe arm (+0.023*/+0.130*) leak-inflated unknown degree; TM2 distinct-only probe +0.117* / ensemble +0.034 ns; (3) top-1% states carry 51%/80% of pooled opportunity. W1 CONSTRAINTS FROZEN pre-instrument (PREREG_w1_adjudication_constraints_20260809): R≥8 independent-mark repeats + split-selection; duplicate-null as GATE; actor+planner ACTUAL-consumer branches; r_real storage + independent probe marks ⇒ leak decomposition (3-way outcome map incl. "ground-truth reward outranks the learned value stack" branch); env seeding mandatory; global redaction. [ME] next: labeler instruments + wave prereg + reader; ONE reviewer then [YOU] freeze-commit constraints file FIRST. **→ 08-09 night W1 BUILD COMPLETE (user GO, reviewer approved): d0/oracle_labels.py W1 path (`--w1_repeats/--w1_dup_cand/--env_seed`; _w1_label_state — repeat marks base+(r+1)·1e5 / probe base+5e4 disjoint; deployed-ACTOR branch = the sample the default path discards; r_real stored via return_rewards; dup_cand; seed_env_task walks wrapper chain to dm_control task RNG; `_w1` stdout redaction; default path wrapped under else, byte-identical) + tdmpc2_oracle_labels.py W1 path (seeded torch marks via w1_mark — offsets don't apply to opaque state dicts; MPPI-PLANNER branch = actual follower_act at decision state = the D1 fix; op_real return_rewards; Dv3TaskEnv env_seed override) + BOTH selfchecks extended w/ the two fixture classes the program never had (stochastic-follower: within-repeat max bias VISIBLE + split-selection kills it; duplicate-null: exact zero) — ALL PASS + labeler→reader E2E schema compat PASS; frozen reader analysis/w1_read.py (split-select estimator, dup GATE exact-zero refusal, 3-way leak verdict DISSOCIATION-SURVIVES / GROUND-TRUTH-REWARD-ONLY / LEAK-WAS-ALL, BCa+permutation per standing rule; selfcheck: planted signal/noise/leak/value fixtures all classify correctly); PREREG_w1_wave_20260809 (68 passes: 32 dv3 late + 32 tm2 late + 4 dup gates; env_seed 20260809; registered against-interest expectations incl. dv3 no-fire prediction). ONE Fable batch reviewer over {2 preregs, 2 labelers, reader} → **2 BLOCKING / 5 MAJOR / 5 MINOR + NITs, ALL B+M FIXED same-day, all selfchecks re-PASS: B1 dv3 actor branch sampled from a DOUBLE-ASSIMILATED belief (policy(carry_post, obs) re-assimilates obs_t — the D1/D3 instrument-vs-claim gap recurring one level down!) → actor = act0, the sample d0_eval already drew from the deployed belief and the default path discards; carry-sensitive fixture added (fixture actor must equal d0_eval's zeros, not a fresh sin(ctr) draw); B2 reader spearman fabricated ranks on ties (argsort-of-argsort; sparse-reward all-zero r_real states would inject index-order bias into the P-W1c verdict) → midranks + constant-input NaN + tie fixtures; M1 TM2 consumer = COLD-START planner ≠ deployed → t0=False w/ warm-start pstate (cold start stays registered for candidate branches only); M2 Spearman(r_real, Ḡ_odd) is TAUTOLOGICAL (r_real is bitwise G's first step — same restored transition; de-shared marks ≠ de-shared transition) → registered TAIL form Spearman(r_real, Ḡ_odd−r_real) + mutant leg proving the naive form fires on leak-only fixtures; M3 dup gate was blind probe-side (the ρ=0.47 witness channel!) → ptp(r_real)==0 ∧ ptp(real_scores)==0 per state; M4 reader pins (env_seed/repeats/states/horizon + filename↔run_id identity, fail-closed); M5 constraint-3 scope note (P-W1b excluded from gate, reasoned). Reviewer also VERIFIED-CORRECT: mark discipline collision-free; within-invocation same-mark CUDA determinism 761/761 duplicate groups bit-identical in existing TM2 labels (DUP_TOL=0 defensible — new evidence produced in review); split estimator sound; BCa/perm correct; redaction complete; no cleanup glob eats w1_*; dv3 constraint-6 genuinely closed. Minors: w1_labels KEEP_PENDING, realization-conditional disclosure, stale comment, _own() in seed_env_task. Smoke stage now includes one dup_cand cell per family. WAVE FREEZE-READY: [YOU] freeze-commit → smoke → 68 passes → ONE read/family** **08-09 late-2 (Paper-1): s×w_r WAVE BUILT on the frozen 08-08 constraints — `PREREG_swave_wave_20260809`: FULL 3×3 rgo grid (s∈{1,2.45,4.46}×w_r∈{1,10,100}, lo side f=.054, 8 seeds/cell, IN-WAVE (1,1) baseline refit — immune to destroyed-fit/archive anchors) + 2-cell vgo repair leg (s∈{1,4.46}, w=1; s-axis is the value-path lever, valnorm none) = 88 fits + 88 adapts + E4 own-label passes; naming sw{r|v}<si><wi>q1s0 glob-checked CLEAN + RUN_RE-parses; `AXIS1_WR` sbatch plumbing added (additive, task-mode-only, empty-default byte-identical; loss_scales.rew leaf verified); registered buffer commands (scale-kind transforms of q1 → q1_s245/q1_s446, refuse-overwrite) + probeset overrides (finger_v1_scale245/446); rules frozen: P-SW1 s-membership own-label / P-SW2 w_r-membership / P-SW3 behavioral vs in-wave baseline BH-corrected ×8 / P-SW4 constraint-6 dissociation map / P-SW5 vgo repair; power honest (behavioral MDE80 ≈50-57 vs archived anchors rgo 97.0±35.0 n8 / pooled 99.5±47.4 n16 / vgo 82.4±25.3); KEEP_PENDING added (runs + buffers). Reader `analysis/swave_read.py` = two-stage (frozen before read). → **REVIEW DONE (ONE reviewer, user-approved): 2 BLOCKING / 5 MAJOR / 7 minor+NITs, ALL APPLIED same-day, all selfchecks re-PASS. F1 (BLOCKING, verified independently): symexp_twohot achievable-NLL FLOORS differ by scale (bin spacing 20/127; floor(1)=0.6735 / floor(2.45)=0.3981 / floor(4.46)=0.5286 nats per rewarded frame; zero labels exactly on-bin) ⇒ cross-scale own-label contrasts carried a deterministic seed-variance-free bias in the fire direction — every NLL rule now runs FLOOR-CORRECTED (− floor(s)·n_in_rew/(n_in_rew+n_in_norew) per run); selfcheck floor-kill leg proves floor-only difference no-fires corrected while naive form fires (~6σ). F2 BOTH-MOVE branch registered verbatim; F3 paired/sign-flip wording struck (unpaired as implemented); F4 label-provenance machine checks (collate now carries additive override_stem/kind/scale columns from summary.json; reader refuses scope mismatches; registered config-audit sweep for loss_scales.rew per run); F5 exact E4 measure/collate commands registered (default ov- naming; e4_finger_v1sw claim dropped); F6 runroot_cleanup KEEP-precedence NOW IMPLEMENTED in the delete loop (grep -v -x -F -f keep-list — the comments claimed it, the code never did it; pre-existing overlaps incl. rl/sh/cup/finger re-fit protections were all exposed!); F7 constraint-6 bullet-1 reconciliation sentence; M1 hash pin 1a36a65a; M2 MDE 49–66 (was overstated 50–57); M4 seed-99 smoke exclusion step; M5 SW5-adjudicates-P-B2-only scope; M6 seeds 1–8 pinned; M7 transform-probeset overwrite refusal. Reviewer verified-correct: AXIS1_WR end-to-end (Flags.parse fail-loud, rew in task keep-set only), s=1 identity exact, override path changes labels only, no name collision w/ archived rgo, BCa/BH correct, KEEP_PENDING slash-paths honored. **WAVE FREEZE-READY: [YOU] freeze-commit → buffers/overrides (record shas) → smoke swr21 seed99 + config audit → REMOVE smoke pre-collate → 176 jobs → 3 collates + counter producer + config sweep → ONE read**** |

Paper-2 plan: `research_notes/other research/EVPI_Plan_Revision_20260724.md`
(v4). Constants (early −0.208\*/late×e4 +0.313\*) = hypotheses to
re-test, not results. 24+24 permanently frozen; imag op retired.

## Paper 3 — composition×capacity flagship (own venue clock)

| Date | Experiment | Registration | Record | Verdict → decision |
|---|---|---|---|---|
| 07-29 | **Volume-anomaly replication P-E4a + diversity secondary** (ONE read, cluster-side) | `PREREG_volume_repl_20260726` | `artifacts/volume_repl_20260729/` | **PRIMARY FIRES: fresh v400s1−v200s1 +331.0 [+169.3,+502.7]** (> the original +228.6 — no winner's curse; pooled-12 +279.8\*; s0 fresh +194.8\* same direction) ⇒ **anomaly REAL — support breadth enters the theory as a λ-structure input distinct from occupancy**; diversity direction HOLDS (diversity_pr v400s1 10.63 > v200s1 9.23 under the pinned `spectral_v1_1_20260724` gate, while generic spectrum_pr goes the other way and v400's occupancy is 3.4× lower) ⇒ P-E4b diversity leg engages; non-spectral-account branch not triggered; conditional-on-occupancy regression form stays unregistered; local re-execution bit-identical |
| 08-03 | **High-f_R falling-limb wave** (ONE read; manifest 151/151; instrument shas ≡ amendment finals; both f-gates PASS: side1 0.7248, side0 0.4088 included) | `PREREG_highfr_wave_20260802` + amend1 + amend2 (freezes `39a15880`/`be60afbe`/`080aa7b2`) | `artifacts/highfr_read_20260803/` | **SATURATION (registered branch) — P-HF1 does NOT fire: hi-f − v200s1 = +49.7 [−8.4, +102.8]**; monotone-rise refutation does not fire (chain 187.7→244.6→237.5 non-monotone); P-HF2 membership INTACT (side1 rew-NLL 1.178 [1.119,1.237] ≤1.5) but diversity_lower FALSE ⇒ dissociation NOT adjudicated; P-HF3 weak HOLDS (argmax interior f=0.4088). Disclosed riders: CI excludes the registered predicted band −88..−108; **mediator never moved — diversity_pr(side1) 10.26 > ref 9.23 (this pool's high-f tail is BROADER, not starved)**; both curated cells above the natural buffer (+49.7/+56.9, straddling) with source_l1=1.0 tier-pure composition (mid vs high) — no f_R attribution licensed ⇒ trade-off form neither supported nor refuted; flagship breadth axis gains NO falling side; grid-densification option does NOT trigger |
| 08-04 | **Breadth-causal wave (q1d)** (matched-f_R diversity pair, 16 fits + 16 adapts + E4; ONE read; manifest 176/176; freeze 7bbf3eb1 → run head a2d19c72 ancestry ✓; instrument shas ≡ frozen finals; **bundle div_pairs.json BIT-IDENTICAL to registered identity 48032a75…**; all six HALT gates reader-enforced PASS: f 0.30358/0.30350 |Δf|=8e-05, div sep 4.8407 ≥ 3.0 straddling ref 9.23, proprio-block sep 0.7359 ≥ 0.5, n=200 both, pins hold) | `PREREG_breadth_theory_20260803` (481aced9, pre-feasibility) + `PREREG_breadth_wave_20260803` (7bbf3eb1) | `artifacts/breadth_read_20260804/` | **P-BD1 INFORMATIVE NULL (registered branch): hi-div − lo-div = −9.8 [−89.9, +72.2] — no breadth effect at an instrument-verified 4.84-PR separation at exactly matched f_R and volume; effects > +72 AUC excluded ⇒ frozen consequence FIRES: the λ-input reading of the volume anomaly loses its principal support — the anomaly requires a non-breadth account.** P-BD2 ordering FAILS (253.7/187.7/243.9) but registered reading rule = FAIL-uninformative: both curated cells +56/+66 above the natural anchor — the f_R wave's curation offset (+49.7/+56.9) REPRODUCED on two fresh cells (curation lift now a 4-cell regularity, exploratory). P-BD3 both cells member (0.93/1.05 ≤ 1.5; no inclusion-leak, no hi-anomaly) — the null is not a broken-cell artifact. Registered scope: full index latent-majority (trace 0.38/0.47), visible-block dose 0.736 PR (gate 0.5, pre-registered). Consequences: P-E4a stays correlational — **theory's causal ledger asymmetric: g/composition-side supported (rgo proprio + rde pixel), λ/breadth-side = registered causal null**; flagship breadth axis demotes to descriptive; **Paper-3 committed compute COMPLETE** |

## Paper 4 — algorithmic collusion (MARL/econ venue, own clock, Paper-4 chat)

| Date | Experiment | Registration | Record | Verdict → decision |
|---|---|---|---|---|
| 07-25 | Pilot stages 1–3 (baseline reproduction, measurement calibration, Design-B viability) | design memo `Design_Collusion_Pilot_20260724.md` | `artifacts/collusion_stage1_20260725/`, `collusion_stage23_20260725/` | stage 1 GO (Δ 0.831 in published range, fp 68/100; failure-to-learn minority ⇒ fp-conditional measurement); distance label recommended (undercut saturates + inverts); RQ1 resolved via `coverage_late`; Design B VIABLE (float-exact 20/20) but deletion confounds volume (composition increment −0.107) ⇒ graded volume-controlled interventions required |
| 07-30 | Confirmatory-prereg calibration | (calibration, pilot seeds only) | `artifacts/collusion_calib_20260730/` | terminal reference frozen (exo degenerate: mean .981, 96%>0.9); ladder d.25 inert / d1 fp-cond −0.134; **Design-A smoke: DISSOCIATION found (forbid λ=1 ⇒ Δ +0.987 w/ fingerprint 0/10, degenerate attractor) ⇒ P-CLA primaries re-specified pre-freeze**; 2 adversarial review passes (10+11 findings, 2 blocking reader-corruption defects) all fixed |
| 07-30 | **Confirmatory sweep READ** (ONE execution; freeze `42bbe487`; manifest 16/16, snapshots ≡ freeze) | `PREREG_collusion_confirm_20260730` | `artifacts/collusion_confirm_20260730/` | **ALL FOUR PRIMARIES FIRE — P-CLB1 −0.104 [−0.165,−0.046] + P-CLB2 −0.592 [−0.724,−0.447]: punishment-phase experience is a composition-specific driver of BOTH profit level and punishment structure at matched update mass (dose ladder monotone, mid-dose fires); P-CLA1 −0.650* + P-CLA2 +0.135*: forbidding exploratory undercuts collapses the fingerprint (0.65→0.00) while Δ RISES (0.852→0.987) ⇒ experimentally induced Abada–Lambin failure-to-learn regime — punishment-generating exploration necessary for GENUINE collusion, not for supra-competitive prices. All gates pass (exact 100/100, n_fp=76, manip −0.485*). ⇒ RQ2 composition leg LANDS; RQ3 coupling wave AUTHORIZED; two-outcome measurement argument load-bearing** |

## Paper 5 — uncertainty field / No Free Information (own clock, Paper-5 chat)

| Date | Experiment | Registration | Record | Verdict → decision |
|---|---|---|---|---|
| 08-01 | **NFI Level-1 pilot** (EXPLORATORY; harness `uncfield/`, 4-model planted selfcheck battery PASS; 1 adversarial harness review "trustworthy after fixes", 4 BLOCKING fixed pre-read, pilot1 killed unread) | design note `Pilot_NFI_Design_20260801.md` + Amendment 1 (gitignored; pre-outcome) | `artifacts/nfi_pilot_20260801/` | **FLAGSHIP SIGNATURE FIRES: ensemble EXPLOIT-SURVIVES-CIG+PBIM (3/4 members; conjunction exploits 13/61/0/61)** — planner finds evidence-neutral cycles (referee-certified, incl. the designed duplicate-pair loop at 31× true gain and the zero-information noisy-TV loop) farming predicted info through BOTH prefix-conditioned EIG and drift-adjusted realized-ΔH; member heterogeneity real (m2 = clean-accountant control); holonomy-predicts-farming NOT supported (commutator residue stays diagnostic) ⇒ next: mechanism analysis + sensitivity sweeps, then claim-freeze audit + registration; NOT a registered claim yet |
| 08-02 | **NFI mechanism analysis** (instrumented replay of 8 cycles/member on the frozen pilot2 ensemble) | — (exploratory follow-up) | `artifacts/nfi_mechanism_20260802/` (⚠ **AMENDED same day, post-review — read through the prepended amendment**) | **Anatomies (1)+(2) SURVIVE adversarial review** (formula = exact Kalman entropy drop; coherent-adapter calibration exactly 1.000; denominators non-degenerate; record bit-reproduces 421/421): (1) update-operator over-contraction 1.4–11.6× own-heads license (cleanest m0/m1/m2; m3 = heads-vs-referee dominant); (2) TV fictitious info — but +0.84 = carried EIG PROMISE not realized contraction (eig-only; fires 2/4 members). **Anatomy (3) dichotomy AMENDED AWAY**: m0 off-support exhibit inverts under matched-walk baselines (walk drift); leakage real for m1/m3 only (static components — relaxation excluded); duplicate-double-count WITHDRAWN (exhibit c451 non-neutral non-exploit; c447/c451 name conflation corrected; c447 = 50/50 split); no thresholdable cycle-level discriminator ⇒ self-consistency ratio = model-level diagnostic candidate only |
| 08-02 | **CEI Level-1 pilot** (equivalence-pair construction + 3 registered kill probes; selfcheck PASS) | design in module docstring (exploratory) | `artifacts/cei_pilot_20260802/` (invalidation note prepended) | ✖ **VERDICT WITHDRAWN same day — harness review "redesign needed"**: TV-sensor shadowing meant NO behavioral separation ever occurs (separation curve = plain node-3 hitting time, bit-exact); ψ deducible from the action log alone (coverage bookkeeping — impossibility tautological as built); fixed patrol = selective-labels carve-out (v2-audit criterion 2) + registered Failure C. Survives: ε=0 ψ-gap pair analytically sound; repaired sense-loop verified to produce real divergence ⇒ **CEI v2 redesign required** (U-controlled movement + failure non-deducible from coverage bookkeeping + explicit access model + A/B/C assessment); theorem-phase go/no-go NOT licensed by this record |
| 08-02 | **CEI v2 pilot** (calibration-falseness redesign: coherent liar R̂₇=0.01 vs true 1.0, committed uncertainty-greedy policy; **reviewed BEFORE read** — "fix before run/read", B1 movement→collection-intensity re-scope + B2 deadlock fix + B3 hazard redesign + N1–N6, core construction verified-and-held; selfcheck re-PASS) | `Design_CEI_v2_20260802.md` §§7b–7c (pre-outcome + review record) | `artifacts/cei2_pilot_20260802/` | **SURVIVES post-review: passive identification bounded (U_false 1 s7 read, logLR ∈ [−0.33,+0.12] across seeds, ψ=0.5 vs U_true 19 reads, logLR +506..+1048, ψ=0; in-probability: P(1-read LR>log19)=1%); ε-separation cost = 1/hazard with h∼ε^4.6 (733→199k steps over ε .5→.15; one late read decisive at ~46 nats) vs targeted audit 7 steps flat (gap 10²→3×10⁴, unbounded); E4 audit-cost curve m* 3/7/21/51/173 for R̂/R .01–.7; L2 blind spot: coherent liar passes replay audit at 1e-17 vs parity operator flagged at .083 with ψ dissociation OPPOSITE** ⇒ theorem phase licensed (bounded-LR impossibility at L1; intervention cost O(diam+m*(γ)); L2 blind spot scoped to (R̂\|known-dynamics) family); v2.1 coupled arm (route control) parked with prototype facts |
| 08-02 | **NFI sensitivity sweeps** (8-job queue on RCC: ymode sample ×3, train {1k,10k,30k}, dataseed {1,2}, hid {32,128}; bundle manifest-verified; **instrument reviewed BEFORE read** — "fix before read": verdict leg CLEARED (classify/aggregation verbatim-shared with pilot, axes verified genuine, resume-guard summary bit-reproduced), self-consistency column EXCLUDED (B2: 34/40 cells null/mistargeted), mechanism record amended same day, sweeps.py empty-ensemble hole patched) | — (exploratory; the "just undertrained" decider) | `artifacts/nfi_sweeps_20260802/` | **STRONG OBJECTION REFUTED: 37/40 members ≥ CIG-ONLY across every axis (CIG-farming persists at 10× training); flagship CIG+PBIM conjunction robust across obs-mode (3/3 sampled seeds), fresh data (2/2 dataseed pilots), and capacity-UP (hid128 = only unanimous 4/4 flagship) — but TRAINING-DOSE-SENSITIVE: flagship members 0/4 @1k → 3/4 @3k → 1/4 @10k → 1/4 @30k = realized-ΔH farming is a mid-training phenomenon (attenuates, does not vanish); exploit GROWS with capacity (hid32 = only NO-EXPLOIT member)** ⇒ headline must scope the conjunction by training dose; claim-freeze lit audit UNBLOCKED; optional follow-ups: per-member ratio re-search; hid128×30k interaction |
| 08-02 | **NFI defense-fidelity arms** (γ-discounted Ng-form rescore, REQUIRED + CIG-faithful disagreement kernel; both built+selfchecked same day; **arms review "fix before read" — machinery certified bit-exact but BOTH registered read quantities replaced; amended protocol REGISTERED pre-read, memo §6.3**; kernel re-run w/ walk-matched twins) | memo §§1-C1, 5.3, 6.3 (protocol governs) | `artifacts/nfi_gamma_kernel_20260802/` | **γ-arm (gates 0.0 ×8): the exploit is a RETURN-PREFERENCE phenomenon, not an unbounded-stream one — telescoped preference for the farming future is γ-INVARIANT in sign (m0 +3.64/+0.66/+0.12, m1 +8.13/+0.83/+0.08, m3 +2.72/+0.72/+0.19 at γ=1/.995/.99) while the reward STREAM self-limits at rate (1−γ)·(entropy displacement): m0/m1 exploit streams fully taxed at γ≤.995, but member-heterogeneous — 34/61 of m3's conjunction cycles farm level-neutrally and their streams SURVIVE γ=.99 outright. Kernel arm: registered THIRD outcome — estimator-insensitive-in-regime (matched-twin per-action surpluses one narrow band, twins beat exploits as often as not; no Prop-2(ii) decay; open-loop divergence dominates) ⇒ C1's principle-vs-estimator scoping now EMPIRICALLY grounded; refutation reading forbidden** ⇒ both arms publishable both ways; remaining pre-freeze arm = P1 (PRIME penalty, §6.2 spec) |
| 08-03 | **NFI registered reads** (per frozen `PREREG_nfi_confirm_20260802`, freeze aa0d2efd; stamps byte-identical to freeze on all decision-path files; bundles manifest-verified) | `prereg/PREREG_nfi_confirm_20260802.md` | `artifacts/nfi_registered_reads_20260803/` | **P-N1 PRIMARY FIRES 8/8 (criterion ≥6/8): both fresh-data pilots ensemble CIG+PBIM, 6/8 members full conjunction — carried farming confirmed PROSPECTIVELY on novel seeds. Interaction: hid128×{10k,30k} = 0/4 conjunction ⇒ capacity amplifies the mid-training peak but STEEPENS the fall. C4 does NOT fire (ρ=−0.021, p=.55, N=30 at floor; ratio stays discussion-level — flags incoherence, doesn't rank-predict counts). P1: penalty attenuates but never removes (≥1 member farms at EVERY grid point; m3's 95 negative-ale senses = penalty-free); GATE RE-ADMISSION CONFIRMED 3/3 (m1 back to full unpenalized count); λ* ordering CONFIRMED on m1 (legit 0.176 vs duplicate 0.697 — legit dies 4× first)** ⇒ defense story complete: all three archetypes farmed-through/attenuate-only; remaining = writing + scoop re-scan + OpenReview pass |
| 08-02 | **NFI claim-freeze literature audit** (24-agent workflow: 4 deep-readers CIG/PBIM/Caron/BiasedDreams + 4 claim-collision searches + fresh-lit scoop scan + adversarial refuters on every serious collision + synthesis; refuters corrected 7 claimed collisions downward) | — (audit; memo = pre-registration input) | `research_notes/NFI_ClaimFreeze_Audit_20260802.md` (gitignored memo) | **ZERO KILLS, ZERO SCOOPS — all four claims freeze, NONE as worded**: C1 rescope (both defenses implemented principle-faithful but estimator-unfaithful — CIG scores parameter-info w/o any belief object, PBIM's guarantee is a terminal correction we never encounter ⇒ claim = "survives the transplanted principles, outside the proven scope of the published estimators"; **REQUIRED pre-freeze: γ∈{.99,.995} Ng-form recompute from saved beliefs — deficit straddles EPS_PRED, numerically threatens the flagship count**; CIG-faithful kernel arm strongly recommended); C2 rescope (dissociation is ours; Pan et al. owns capability-axes template; gradient-steps-not-data qualifier mandatory); C3 rescope (split m3 out of the sane-heads sentence; **Bayer et al. 2021 conditioning gap = biggest citation risk**, capacity contrast runs OPPOSITE to amortization-gap lit); C4 blocked-as-worded (Chen 2605.06915 May-26 priority on the diagnostic concept; "self-consistency ratio" name OWNED by Schmitt ICML-24 → rename "Bayes-license ratio"; claim only if the queued ratio re-search predicts exploit counts, else discussion-level); battery adequacy: PRIME (18 Jul, aleatoric-penalized IG) must be re-scored or carved out; LPM + learnable-novelty = registered scope sentences; **relabel EXPLOIT-SURVIVES-CIG+PBIM → principle-level names**; RECOMMEND freeze+register NOW (don't wait for hid128×30k); re-scan scoops after 4 Aug + OpenReview pass pre-submission |
| 08-07 | **NFI family-generality factorial BUILT + REVIEWED** (breadth insurance vs the "one synthetic family" objection; 3 new cells lg_lstm / dc_gru / dc_lstm at the anchor config; NEW discrete-chain world `uncfield/dcfield.py` — 8 two-state chains, deterministic emissions, exact 256-state joint Bayes referee, runtime observation-independence certificate + MC-replay selfcheck 1.8e-15, EXACT-zero baits; planner world-parameterized w/ thresholds/verdicts inherited VERBATIM; additive LSTM in learnedwm; dc battery: joint-null + marginal-surrogate-null NO-EXPLOIT, constant-rate positive CIG+PBIM 220, saturating TRANSIENT 435; family-1 path verified BITWISE-unchanged twice — reviewer committed-HEAD diff 0.0 e2e vs saved 1-Aug npz, + independent member-0 recompute all counts/top-rate 0.0; Dreamer-scale probe REJECTED by design (no referee ⇒ noisy-TV collapse). ONE Fable reviewer "fix before freeze/run": **1 BLOCKING** (draft registered "steady dh farming impossible in bounded world" — FALSE at the 38-loop window, budget bound is ≈69 loops; reviewer's delayed-saturator fires 172 steady conjunctions inside a valid ≤5.5-nat path ⇒ reworded to quantitative empirical prediction + pre-specified stronger-result branch) + **2 MAJOR** (transient-conjunction pinned as same-cycle eig∧transient computed by runner AND recomputed by reader — raw pbim/transient tiers shown move-only-drift-contaminated in smoke; dc-episode collect race ⇒ pid-atomic write + serial dc task) + 3m/4n — ALL applied same day, selfcheck+smoke re-run green (smoke verdicts byte-identical pre/post-fix) | `prereg/PREREG_nfi_family2_20260807.md` (**pending [YOU] freeze-commit**) + frozen reader `uncfield/family2_read.py` (selfcheck reproduces smoke verdicts from raw npzs; threshold-direction + conjunction-AND mutants killed) | `scripts/uncfield_family2.sbatch` (array 0-1; G-HOST selfcheck gate wired as step 0) | **P-F2 primary = ensemble ≥ CIG-ONLY in EACH new cell (breadth wording frozen both branches); P-F2b conjunction-location secondary (lg steady ∧ dc none) w/ bounded-entropy prediction on record; awaits freeze → RCC run → bundle → ONE read** |
| 08-07 | **NFI family-generality READ** (frozen reader `family2_read.py` sha `1671dcae…`; bundle manifest-verified 21 files ≡ committed copy; freeze `8f6b4de8` → executed at descendant `da800742`, decision-path diff EMPTY, ancestor-verified; stamps uniform + thresholds pinned; `dirty:true` on RCC stamps DISCLOSED — bounded by the empty diff + G-REPRO all-12-member re-derivation from raw npz rates; G-DATA dc episode shas identical) | `prereg/PREREG_nfi_family2_20260807.md` | `artifacts/nfi_family2_read_20260807/` | **P-F2 PRIMARY FIRES 3/3 cells, 12/12 members ≥ CIG-ONLY — the frozen breadth wording is LICENSED (four cells, two exact-referee families, two architectures, no retuning). P-F2b does NOT fire — the STRONGER-RESULT branch: dc steady conjunction 2/8 members (dc_gru m1 + dc_lstm m3, both member-verdict CIG+PBIM) farming referee-certified EXACTLY-ZERO cycles (duplicate-pair + the coin-flip TV channel at eig .254/dh .150 and .093/.106 nats/loop) — the flagship conjunction reappears in the bounded discrete family; lg_lstm 0/4 steady (conjunction dose-curve is architecture-dependent); transient tier ZERO everywhere (the bounded-entropy transient-shift prediction registered as a MISS — review-B1 arithmetic vindicated: 69-loop budget ≫ 38-loop window). Descriptives: lg_lstm m2 tv_max 0.788 = strongest TV farmer in the study; no dc member exceeds the 5.545-nat world budget (conjunction via entropy cycling, not runaway contraction)** ⇒ NFI generality leg COMPLETE; writing: carried leg = universal, conjunction = seed/architecture/dose-scoped per frozen C1/C2 |
| 08-09 | **FULL-RECORD + DRAFT REVIEW → response wave** (12-lens Opus review `reviews/Review_FullRecord_and_Draft_20260809.md`; decisive findings INDEPENDENTLY VERIFIED from the arrays this session) | — (review + response; amendments prepended to 5 records) | `artifacts/nfi_review_response_20260809/` | **VERIFIED: (1) family-1 flagship conjunction = drift-credit ARTIFACT (raw ΔH counts 0/0/0/1 vs published 13/61/0/61; 52/61 of m3's flagged cycles had entropy RISING; median credits +0.054..+0.141 ≈ the 0.08 bar; pilot ensemble on raw = CIG-ONLY; sign sentence in record was inverted); (2) family-2 conjunctions drift-clean (raw≡adj 5/5) but WINDOW-TRANSIENT (new re-score: 0/5 hold at loops 2–5, 0/5 at 37–40; registered values reproduce ≤1e-7) ⇒ NO window-robust realized-ΔH claim in either family — conjunction demotes to window-scoped descriptive; (3) capacity claim inverted in magnitude (mean both 42.0→33.8→14.2; tier count seed-confounded); (4) CEI "within 4%" near-optimality RETRACTED (power@628=0.943; optimum 653; gap = 1.65× BH slack × 1.93× REAL fixed-vs-sequential inefficiency — SPRT≈339 inside Thm 3's own class; = correction #15, on review #13's own suggested upgrade) → Theory v3.2. UNTOUCHED: carried leg (8/8, 12/12, 37/40, all arithmetic reproduces, family-2 prereg provably pre-outcome). NEW ANALYSES: predicted-vs-true ranking (top-1 FICTITIOUS in 3/4 anchors, whole top-10 evidence-free, ≈1.04 nats/loop true regret); carried window-robustness member-heterogeneous (5/8 dc members persist at 40 loops; dc_lstm m2 bit-stable = true steady farming); UNTRAINED-filter control: random-init nets also reach CIG-ONLY (lg 96 / dc 34 cig) ⇒ tier membership doesn't separate trained from untrained — claim "training does not remove", never "training creates"; pooled 68/72 = .944 [.866,.978]; EPS sensitivity no cliff. COMPLIANCE REMEDIATION: confirm-wave frozen reader RETRO-BUILT (`uncfield/confirm_read.py`) — ALL recorded outcomes reproduce incl. perm-p 0.5477 exactly (λ*_legit definitional nuance pinned: 0.170 registered-def vs recorded 0.176 all-8-loop variant, ordering unaffected); G-STAMP dirty gate registered (`PREREG_nfi_family2_amend1_20260809`) + implemented + clean-HEAD reproduction control + re-read; reader fixture battery kills the 17 surviving mutants incl. the CIG_LEVEL primary flip; 3 record errors amended (95/816; m1's 129 negative-ale senses restored; "everywhere" false, 5/30 cells ≤1) + p1_rescore ancestor-stamp disclosure** ⇒ manuscript restructure per review §9 (writing chat): flagship = carried leg + ranking/regret + exactly-zero exhibits; conjunction/capacity/dose rescoped; TV λ* negative restored; diagnostic renamed 3rd time (Imran 2507.17951 BCC priority)  **9-Aug-late addenda (§§3e,6 of the response record): raw-ΔH recovery wave LANDED (manifests/uncfield_raw_20260809.sha256; verdict_match 56/56 — every sweep verdict incl. P-N1's 8/8 re-derived from the models, review §15 residual closed; drift artifact GENERALIZES: 1462 adjusted vs 102 raw conjunctions, 453 sign flips, Spearman(n_adj,−drift)=0.805, raw survivors 14/56 members mostly y_mode=sample/zero-drift; sweep tables → dual raw+adjusted) + ideation-L1 global calibration (ρ(carried,true) median 0.10 factorial / −0.05 sweeps, negative 34/72, exploit basin up to 47% of library, measure-zero kill-gate 0/72 ⇒ headline strengthens; deep-enumeration DP demoted to optional).**  **+ ideation-L2 control EXECUTED (§7): full-Cholesky z-head ensemble on pilot2 episodes/seeds — s4 marginal farming falls (24→3) but correlation mis-learned (wrong sign 2/4) and exploit SURVIVES the joint log-det objective (m0 31/46 neutral s4 cycles) ⇒ representational adequacy relocates, not repairs; full-library dual-view sweep = scripts/uncfield_fullcov.sbatch (optional RCC).**  **+ L2 full-library sweep (§7b): CIG-ONLY 8/8 member×view, joint objective no-repair at scale, top-1 fictitious 8/8 (axis-aligned channels), drift ≈0 under full head; §7 'falls' finding CORRECTED (batch-unstable — RCC retrain ≠ local batch; cross-machine CPU training NOT reproducible; local batch-A artifacts overwritten by rsync, numbers preserved in record); calibration kill-gate now 0/80.**  **+ v2-DELTA REVIEW (#16, yield 16/16, §8): draft restructure COMPLIED (no demoted claim smuggled back; new stats all reproduce) but 11 wrong numbers incl. BLOCKING capacity-tier mislabel (printed 1/4→3/4→4/4 is the demoted conjunction tier; carried is 3/4→4/4→4/4 — verified) + 2 governance holes; record+code side RESOLVED same session (§6b member fix, distinct-model denominators 64/60, gate hardenings validated w/ real-read re-run no-HALT); draft actions 1–12 → writing chat.**  **Amend-2 registered (PREREG_nfi_family2_amend2_20260809): frozen-reader hardening brought under governance — strictly-stricter gates, verdicts verified identical, process disclosure inside.**  **+ CEI MANUSCRIPT REVIEW (#17, yield 17/17, adjudicated 10 Aug, §9): theorems all survive attack (Thm 3: 9 structural + 13 adaptive auditors) but Thm 4 UNPROVED (blocking) + 3 page errors + wrong-paper citation + 4 abstract qualifier drops; retraction containment + Ma&Zhao attributions CLEAN; source conflict RESOLVED — draft right, FullRecord wrong (653/0.943 not 668/0.9446, verified); RelatedWork liu2026 corrected; draft actions → writing chat.**  **12 Aug assessment-response wave (artifacts/nfi_assessment_response_20260812/): held-out table DONE (trained ≈ Bayes floor + exploit verdicts; untrained 4× off — the 'normal metrics, failed audit' exhibit); SE prereg drafted (planted N/D/C channels, D=duplicate flagship, Distractor-wrapper reuse, disag objective exists in repo); CEI exposure note drafted (Prop 1: behavioral leakage EXACTLY zero for arbitrary policies — sharper than the assessment's proposal; coupling axis = the generalization); 2 Fable reviewers in flight.**  **12 Aug late: both instrument reviews adjudicated same-day (#18 SE prereg 4B/8M/9m → v2; #19 exposure note → v0.2 with the reviewer-found BUDGET THEOREM: KL ≤ κ·E[N_j] under arbitrary coupling + arbitrary policies — CEI Thm-3 restrictive assumptions deleted from the impossibility direction; behavioral and coupling side-channels both provably zero; yield streak 19/19).**  **12 Aug pm: SE instruments BUILT (planted.py selfcheck PASS + wiring + sbatch reusing p2e path; smoke = user action); exposure note v0.3 O1–O4 discharged (proofs appendix + exposure_example.py selfcheck PASS); second-pass theory reviewer in flight.**  **12 Aug night: second-pass theory review (#20, 20/20): budget theorem SURVIVES; H1′ independence hypothesis added (reviewer counterexample: quantile-coupled randomization ⇒ KL=+∞ under letter of H1–H3 — a REAL scope boundary); O2 superseded by chain-rule proof; exposure note v0.4 READY for CEI manuscript integration; exposure_example.py audited clean.**  **12 Aug late-night: SE wave FREEZE-READY — se_probe built+reviewed (#21, 5B/6M/7m adopted; rollout-pairing fix flipped smoke P-SE2 to p=.010; const=diagnostic-only; ≥7/8-run cross-seed fire rule + BH registered; se_read = pre-read frozen reader); D-niche search CLEAR (LIMIT-2: CIG=temporal-redundancy differentiation, DreamerV3-XP=relevance); prereg v2.2 zero residual FILLs; awaiting [YOU] freeze commit + 8-run submission (seeds 10–17). Reviews #18–21 same-day-adjudicated, yield 21/21.**  **12 Aug (post-freeze fc85fdee, runs training): PRE-READ INSTRUMENT BATCH (record §9) — Amendment A1 (PREREG_nfi_scale_exhibit_amend1: additive per-family SE2 fields the frozen probe lacked for a §5-registered reporting obligation; selfcheck re-PASS with IDENTICAL numbers = bitwise-inert witness); se_mask.py built (§4 secondary: batch-permutation N / source-substitution D, dup0 hard-asserted bitwise no-op, sign-flip directional + inflation-side diagnostic, BCa; selfcheck PASS); se_read.py FROZEN READER built (≥7/8+binomial+BH fire rules, conservative invalidity w/ denominator pinned at 8, bit-identical float32 provenance gate verified exact on real smoke output, θ₁/Fisher/BCa/D-ladder/fit-counters/outcome-cell); review #22 (ONE Fable): se_mask+A1 FREEZE-READY, se_read 1B/5M/9m ALL ADOPTED — B1 = M4-snapshot clobber of <run>/se_probe would silently put the primary read on the wrong checkpoint ⇒ final-ckpt hard gate + snapshot passes rerouted to se_probe_snap<pct>; M4 run-identity via config.yaml train seeds (probe seed is 0 everywhere; duplicates fatal); M5 missing-run denominator mutant killed; M6 fixture provenance de-circularized (stored p from se_probe's own perm_null). Registered-rule note documented pre-read: a LONE 7/8 channel dies under BH (needs ≥3 channels at p≤.0375 or 8/8). Both selfchecks re-PASS. Yield 22/22. Awaiting bundle → registered read.** |
| 08-07 | **CEI theory v3 → v3.1** (matching lower bound added as Thm 3; L2 characterization RENUMBERED Thm 4 — map in file header; theorem-freeze lit prep CLOSED: Ma&Zhao 2608.01378 targeted read — orthogonal neighbor, no L2 axis, our regime named open in their SI §S10; Srivastava 2602.16984 = the real must-differentiate (exogenous vs our endogenous blind spot); Berk-Nash slate verified w/ Esponda-Pouzo 2021 Lemma 3(i) closest; handoff `theory/CEI_RelatedWork_Notes_20260807.md`). ONE Opus math reviewer "fix before circulating": **1 BLOCKING** — the v3 proof's divergence-decomposition step INVALID for exchangeable reads (conditional per-read divergence carries data-dependent mean-mismatch; adaptive selection on it violates the intermediate bound — counterexample at z≈+220, force-iff-\|y₁\|>c ratio diverges) ⇒ REPLACED by reviewer-derived **latent-augmentation proof** (X hypothesis-free ⇒ conditionally-i.i.d. reads ⇒ Wald equality ⇒ data processing; bound counts TOTAL reads n+M_j, loses <1 read via J_n identity) + **4 MAJOR** (intervention model stated; γ=0.2 passive term corrected to n=16 ⇒ E[M]≳190; **"factor ≈3" = pure Bretagnolle–Huber slack — exact optimal test needs m=653, E4's 628 is within 4% of true optimum, UB/LB ratio stable ≈3.2 across δ — near-optimality UPGRADE**; side conditions added) — all + 8m/2n applied same hour; v2 KL-direction slip caught & fixed (0.0941/0.2169, the file's own convention, TIGHTER trade-off); scripts + report archived `theory/review_cei_v3_20260807/` | — (theory doc, not a run registration) | `research_notes/paper5_uncertainty_field/theory/Theory_CEI_20260804.tex` (v3.1) | **CEI theorem set COMPLETE: exact-KL impossibility + audit-cost UB + matching LB over ALL adaptive auditors + L2 blind-spot characterization, with the audit shown near-optimal (4%) — remaining = manuscript (writing chat), EP-2021 lemma-number verification at writing time** |

## Registered / running, no read yet

- **R3 reacher third-domain replication** (REGISTERED 07-29; **READ
  07-31: REPLICATES — see the Paper-2 table;
  `artifacts/r3_reacher_20260731/`**):
  `PREREG_r3_reacher_20260729` → `analysis/r3_reacher_read.py`
  (selfcheck PASS; imports the frozen r3_read machinery, import surface
  pinned). 16 fresh two-phase runs + 32 oracle_all passes on the idle
  instance (`DOMS=reacher` pilots → `SMOKE_DOM=reacher` smoke →
  labels); machine-checked reacher smoke gate at driver AND reader;
  registered FLOOR gate (>50% all-zero-G ⇒ inconclusive, not failure);
  P-RRa opp>0.2 / P-RRb gap>0 / P-RRc maturity; verdicts REPLICATES /
  NO-OPPORTUNITY / HARVESTED / FLOOR-LIMITED; n=16 detects gap ≈0.59
  vs observed cup/finger +1.29. Supersedes the executed R3 prereg's
  conditional-GO clause; cup/finger primaries never recomputed.
- **R3 candidate-count doubling test M=8→16** (REGISTERED 07-29, to
  submit after freeze-commit; closes editorial objection-6 residue):
  `PREREG_r3_doubling_20260729` → `analysis/r3_doubling_read.py`
  (selfcheck PASS incl. _load fixture leg + dial-identity guards from
  meta on both sides). 1 smoke + 32 late-cell M=16 passes on the
  existing R3 checkpoints (`--actions 16`, all other dials pinned);
  CELL-level pairing only (candidate sampler shares the agent RNG ⇒
  trajectories diverge — disclosed by design); P-DBLa monotone
  consistency (CI entirely <0 ⇒ ANOMALY, quarantine) / P-DBLb
  materiality vs 0.6465 (= 50% of the committed gap 1.29304688,
  truncated conservatively); T4 signed-bias theory anchor verified
  (Prop biassign). **08-01 READ (ONE execution, local): CONSISTENT +
  MATERIAL TRUNCATION DISCLOSED** — inc_opp (opp16−opp8) +0.634
  [−0.210, +1.439] n=32 (no anomaly; CI upper > 0.6465 ⇒ the M=8
  truncation can exceed half the gap — quantified in the paper;
  conservative direction unaffected). Decisive secondaries: **gap16
  REFIRES bigger** (+1.893 [+1.141, +2.715] vs committed M=8 gap
  1.293) while **inc_ach ≈ 0** (+0.081 [−0.117, +0.255]) — doubling
  the candidate set grows opportunity but the consumer harvests none
  of it ⇒ the headline gap GROWS under candidate-set refinement
  (mechanism-coherent with opportunity-without-competence); per-domain
  inc: cup +0.25 ns, finger +1.02 ns wide. Bundle
  `local_results/r3_doubling_20260801_212204/` (32+smoke m16 labels);
  → `artifacts/r3_doubling_20260801/r3_doubling.json`.
- **R3 candidate-aware ladder REGISTERED 07-30/31** (Paper-2
  objection-4 residue; registered-DESCRIPTIVE, never decisional):
  `PREREG_r3_ladder_20260730` → `analysis/r3_ladder_read.py` (selfcheck
  PASS; sha256 ea4271d5…). L0–L4 on the committed R3 labels (substrate
  byte-gated by frozen `COMMITTED_LABELS_DIGEST` 02efe6c5…); NEW L4 =
  frozen 7-feature candidate-geometry/Q-dispersion set (old ladder's
  candidate-geometry exclusion LIFTED as an explicit registered scope
  change); nested-LORO ridge, 16 clusters/cell, 48 looks counted;
  heuristics: compression signature (carried) + candidate signature
  (new) — consequence resource-only (repair targeting). ONE local CPU
  read after freeze-commit. Solo pre-freeze review (reviewer wave lost
  to session limits — see DEVIATIONS): exact version pin + digest gate
  added. **AMENDED 07-31 pre-read** (`PREREG_r3_ladder_amend1_20260731`
  → reader sha aa776f1c…, selfcheck PASS 7m08s): the deferred
  independent review (2 reviewers + refuters, 12 agents, value-blind)
  CONFIRMED the pooled rung metric confounds between-run/domain
  structure with per-state legibility (LORO mean-reversal −0.24…−0.65
  on null w/ CIs<0; domain shortcut +0.30 on zero signal) ⇒ metric =
  mean per-run held-out Spearman (pooled demoted to point-only
  diagnostics; explicit all-floor nan policy, ~3–4 nan runs/cell
  expected); new machine-checked heterogeneity + p=512 legs; +4
  registered disclosures (act_greedy_dist ≡ 0 by construction — L4 has
  6 live G features; Power-section erratum; lambda_per_fold
  diagnostics; nanpercentile policy). Read still unexecuted at
  amendment (value-blindness intact); executed after the amendment
  commit — **READ 07-31 late: NO PER-STATE SIGNATURE (flat branch);
  repair default targeting stands; amendment validated on real data —
  see the Paper-2 table row.**
- **Stage-0 second-k robustness REGISTERED 07-30/31** (the Stage-0
  memos' own unexecuted caveat; gates externalizing TRACKS_DENSITY):
  `PREREG_stage0_secondk_20260730` → `analysis/stage0_secondk_read.py`
  (selfcheck PASS; sha256 3c297e18…) + one-line dumper patch
  (`probing/latent_uq.py` meta gains ep_batch). k2 ∈ {5,20}, 10 p2e
  cells all-or-nothing; substrate NOT on disk (anchors discarded at
  dump time) ⇒ registered EXISTENCE GATE (gate JSON w/ replay chunk
  counts) + SUBSTRATE-GONE branch; two-mode integrity-guard ladder
  (tolerance / fallback rank-floor 0.9) selected by a k=10 CALIBRATION
  dump (bf16 + categorical sampling ⇒ cross-backend drift is real;
  --platform cuda pinned); P-K1 robust iff 20/20 TRACKS_DENSITY,
  AMBIGUOUS counts as flip. 2 reviewer passes (2 BLOCKING: broken
  selfcheck float-equality asserts; prereg/reader design divergence
  from an interrupted fixer — reader design adopted, prereg rewritten
  to match). **31 Jul EXECUTION: existence gate PASSED (SUBSTRATE OK,
  10/10 runs, replay chunks 1520–1760) but the k=10 calibration hit the
  registered QUARANTINE branch (guard_mode=invalid: 7 tolerance
  failures AND err_position/err_steps/err_velocity rank corr
  0.74/0.84/0.81 < floor 0.9) ⇒ full passes correctly refused, no
  verdict. AUDIT
  (`artifacts/stage0_secondk_quarantine_20260731/AUDIT.md`): committed
  dumps were computed on Vast (/workspace); re-dump ran on RCC — dials
  and probeset sha identical, true_* arrays matched (pipeline
  faithful), only model-path arrays diverged ⇒ cross-MACHINE
  backend/env change, the registered drift mechanism beyond the rank
  floor. NOT evidence against TRACKS_DENSITY (frozen consequence map).
  Next: Vast-matched smoke retry (gate retry, no amendment needed) or,
  if the env is unreconstructable, quarantine-terminal ⇒ permanent
  single-k qualifier.** **08-01 VAST-MATCHED RETRY + ONE READ (executed
  ON the instance): K-SENSITIVE.** Smoke `--smoke_check` PASSED on the
  matched env (guard_mode=fallback, the registered branch — 5 tolerance
  failures but rank floor met; RCC had refused outright); full passes
  10 cells × k∈{5,20} × 5 ckpts, integrity 0 failures/1450 arrays;
  verdict **18/20 TRACKS_DENSITY, 2 flips both finger @k20**
  (seed2, seed5 AMBIGUOUS; all k5 and all cup verdicts track) ⇒ frozen
  consequence: every external TRACKS_DENSITY statement carries the
  k=10 qualifier + enumerates the flipped cells. Record
  `artifacts/stage0_secondk_20260801/READ_RECORD.md` (bundle
  `local_results/stage0_secondk_20260801_212204/`; **02 Aug provenance
  pulled + VERIFIED, chain closed**: json bytes = instance-recorded
  sha 75ab1bd2…, json verdict ≡ synced stdout, instance reader sha
  3c297e18… = registered freeze sha = local file, instance git head
  003d7b82 = committed ancestor on branch; disclosure: the recommended
  twice-run probe was not executed — partially covered by same-session
  smoke pass + read).
- **TM2-R3 TD-MPC2 cross-family replication REGISTERED 07-30/31**
  (Paper-2 LONG POLE; the ICML band-mover):
  `PREREG_tm2_competence_20260730` → labeler port
  `probing/tdmpc2_oracle_labels.py` (selfcheck PASS; 8eeb5949…; planner
  warm-start snapshot + torch-RNG CRN replace belief carries; smoke
  stdout estimand redaction) + `probing/tdmpc2_r3_train.py` (official
  OnlineTrainer subclass, early ckpt at 2.5e4) + compat dose patch
  (exact main.py distractor mirror; byte-identical for existing
  callers) + `scripts/tm2r3.sbatch` (4 stages, checkout pin, grid
  validation) + `analysis/tm2_r3_read.py` (selfcheck PASS; 00e83fc4…;
  r3_read import pins, FLOOR gate, dosed-smoke authenticity + task
  pins). Grid: {cup,finger}×{e1,e4}×seeds 51–58×{2.5e4,1e5} = 32 online
  runs + 64 oracle_all passes ≈ 130–230 GPU·h. P-TM2a/b/c mirror
  P-R3a/b/c; HARVESTED-BY-PLANNER = registered family-boundary branch.
  2 reviewer passes 0-blocking; all MINOR/NOTE hardening applied.
- **R3 Amendment 2 — cross-checkpoint consumer REGISTERED 07-30/31**
  (parent 8b3380ce re-entry clause): `PREREG_r3_amend2_20260730` →
  `--consumer_checkpoint` overlay in `d0/oracle_labels.py` (full
  selfcheck PASS; 3613f488…; counter snapshot/restore ⇒ control and
  swapped passes state-identical — per-state pairing; regex
  '^(rew|con|valens\d+)/'; no-op/partial-overlay guard; consumer-arm
  stdout REDACTION (value-blindness leak fixed pre-freeze); config
  comparer) + `analysis/r3_xconsumer_read.py` (selfcheck PASS;
  b4952946…; pairing QUARANTINE gate, smoke authenticity, max_steps
  pin) + `scripts/r3_xc_local.sh` (existence gate refuses to erase a
  prior SUBSTRATE_GONE marker). 128 passes on the R3 run dirs (kept in
  runroot_cleanup KEEP_PENDING); P-XC1 head-harm / P-XC2 head-deficit,
  prediction HEADS-IRRELEVANT (per P-R3c + reacher). 2 reviewer passes
  ×2 rounds (2 BLOCKING fixed: stdout estimand leak; stale disclosure —
  reacher read now named known-at-freeze). **02 Aug DETERMINISM PROBE
  FAIL (`artifacts/r3_xc_detprobe_20260801/`, diagnostic — no
  registered gate ran, no wave label exists): control-config overlay
  pass run twice, identical dials — episode/step EXACT but g_all
  max|Δ|=95.0 and m_now flips 64/200 (32%) ⇒ the registered two-pass
  pairing (pairgate atol 1e-5) is UNEXECUTABLE on this substrate; the
  128-pass wave is NOT submitted (would have QUARANTINED at the
  pairgate). Same substrate fact as the repair parent. Path forward
  queued for GO: xc Amendment 1 = within-pass dual-chooser pairing
  (one pass computes g_all once, evaluates control + overlaid choosers
  on shared features; 64 passes, half cost; labeler extension + amended
  reader + dated amendment before any wave label). Disclosure:
  `--consumer_checkpoint` has now run in CONTROL config only (one cell
  ×2, labeler seed 0 ≠ wave seed 1); no swapped pass, no
  cross-checkpoint statistic exists; probe touched integrity
  quantities only (stdout redaction active).** **02 Aug xc AMENDMENT 1
  BUILT on user GO (pending freeze-commit; ONE reviewer run same day):
  `PREREG_r3_xc_amend1_20260802` (sha ef9e31b4…) + dual-extended
  `d0/oracle_labels.py` (`--dual_chooser`, version `_xc2`; DualHeads
  cached head swap w/ witness read-back + identical-heads refusal;
  per-state gates: candidate identity ≤1e-4, env-reward CRN ≤1e-6,
  RNG-consumption identity; RNG schedule identical to a non-dual pass;
  default paths byte-identical, full selfcheck PASS incl. new xc8 dual
  legs + all pre-existing legs; sha b437a6a7…) + amended
  `analysis/r3_xconsumer_read.py` (64-file `_dual` grid, exact `_xc2`
  pin — `_xc1`/plain files trip; consumer-must-be-other-maturity pin;
  QUARANTINE re-scoped to within-pass integrity; SUSPECT-NO-OP honesty
  valve; value-blind `--dualgate` replaces pairgate; selfcheck PASS;
  sha 81793426…) + amended driver `scripts/r3_xc_local.sh` (stages
  gate→smoke[2 dual]→dualgate→labels[63]; sha 772ef3cd…). Estimand =
  the parent's achieved contrast realized within-pass
  (d_pair = [g_all[m_real_x]−g_all[m_now_x]] −
  [g_all[m_real]−g_all[m_now]]); decision rules/prediction/consequence
  map inherited verbatim; ≈0.55–0.6× parent compute (~70–150 GPU·h).
  Regression: repair_read + r3_read selfchecks still PASS (labeler
  default paths untouched). **Review DONE 08-02 (ONE reviewer, Opus 5):
  0 BLOCKING / 3 MAJOR / 5 MINOR / 8 NIT — logic/inheritance/ordering/
  value-blindness all confirmed machine-true (incl. jax swap semantics
  against agent internals, RNG bookkeeping, guard-exemption of the
  device ops, three-way probe exclusion, dualgate blindness); all 3
  MAJORs were selfcheck-strength gaps, ALL FIXED + verified caught on
  mirrors: M1 CI-endpoint fire rules now discriminated by
  nondegenerate-CI legs (both endpoint mutants killed); M2 the RNG
  replay now exercised with a REAL counting oracle (schedule-invariance
  final-counter equality + over-consuming-shadow trip; reset-deletion
  and guard-deletion mutants killed); M3 DualHeads itself now executed
  (jax-free identical-heads-refusal leg + jax-backed two-store swap +
  lossy-store witness trip; both guard-deletion mutants killed). MINORs
  applied: m_now/m_real ranges restored to the QUARANTINE class,
  DUAL_CANDS_ATOL import-pinned to the labeler, dual_allow_identical
  stamped + pinned False in DIALS, shadow-executes-CONTROL-cands leg
  (threshold-straddling sub-atol drift; cands_x mutant killed),
  process-count guard; NITs: SystemExit conversions, flag dependency,
  shadow array shape checks, dead fields dropped, side_diffs reuse,
  prereg wording (labeler-vs-reader enforcement split; probe seed-0).
  Post-review shas: prereg 05cd8772…, oracle_labels 6561eff9…,
  r3_xconsumer_read 2908a78c…, driver 772ef3cd… (unchanged). All four
  selfchecks + driver dry-run PASS. COMMIT-READY.** **08-02 COMMITTED
  (4decd451) + wave RAN + READ — MIXED (off-map XC2 negative
  excursion); see the Paper-2 table row 08-02 and
  `artifacts/r3_xc_read_20260802/`.**
- **Competence-repair intervention REGISTERED 07-30/31** (the R3
  consequence map's mandated NEW registration; the headline follow-up):
  `PREREG_competence_repair_20260730` → `d0/train_consumer_model.py`
  (LORO per-run ridge on cmfeat1 observables; selfcheck PASS
  deterministic across PYTHONHASHSEED; 2405c44e…; live ALLOWED_KEYS
  whitelist + source-scan) + `--consumer_model` chooser (cast-first f32)
  + `analysis/repair_read.py` (selfcheck PASS; bec84fc6…; determinism
  gate episode/step/m_now/g_all vs the COMMITTED labels as per-state
  control — zero control passes; frozen COMMITTED_LABELS_DIGEST +
  trainer-source sha + 3-way model-sha protocol) +
  `scripts/r3_rep_local.sh`. 32 repaired passes (~32–64 GPU·h);
  P-REP1 paired achieved CI>0, P-REP2 materiality 0.6465 (point label);
  verdicts REPAIRED / PARTIAL / NOT-REPAIRABLE-FROM-OBSERVABLES /
  HARMFUL. 2 reviewer passes ×2 rounds (1 BLOCKING fixed: salted-hash
  selfcheck nondeterminism). **08-01 EXECUTED → registered QUARANTINE
  (determinism gate; instrument outcome, no verdict computed, 31/32
  passes never run, no read executed).** Existence gate + trainer
  PASSED (32 LORO models, sha `be802f1e…` recorded pre-labeling);
  smoke detgate tripped (episode/step ok, m_now/g_all drift); audit:
  chooser EXONERATED (no-consumer re-pass drifts identically) — root
  cause = **labeling stack is not job-to-job deterministic on midway3**
  (same-day same-code pair: deter max 1.36, g_all per-state rank ρ
  0.02, m_now flips 48/200; committed-pass bitwise reproduction was
  never a property this substrate had). Bundle
  `local_results/r3_repair_quarantine_20260801_210414/`; audit record
  `artifacts/r3_repair_quarantine_20260801/AUDIT.md`. Recommended
  path: within-pass shadow-pairing amendment
  (`g_all[m_real] − g_all[m_real_probe]` inside each rep pass; model
  reusable; zero extra compute). **xc wave EXPOSED
  to the same hazard (pairing gate atol 1e-5 vs measured cross-job
  drift ~30–46) — one-cell same-job reproducibility probe required
  before submitting; TM2 not exposed (within-pass asserts).**
  **08-01 late AMENDMENT 1 REGISTERED (user GO):**
  `PREREG_competence_repair_amend1_20260801` (aefe13da…) — parent
  committed-paired primary RETIRED unmeasurable-on-substrate; amended
  primary = per-state within-pass [`g_all[m_real] −
  g_all[m_real_probe]`] (g_now cancels; both choosers already stored
  by the frozen labeler), same P-REP1/P-REP2 (bar 0.6465, bridge
  disclosure) + verdict map; gates: episode/step state identity vs
  committed ⇒ QUARANTINE, within-pass identities (oracle_all, m_real
  max ghat, NEW m_real_probe max real_scores + finiteness), NO
  cross-job float gate anywhere; sensitivity leg excl. the
  pre-amendment smoke cell + registered FRAGILITY DISCLOSURE
  (materiality is a point threshold — deterministic flip leg in
  selfcheck); driver byte-unchanged (comments stale by design; smoke
  stage re-mints the detgate marker); model-restore rule (never
  retrain — sha mismatch would crash the read). Reader amended
  (selfcheck PASS; c92b2fd7…). ONE reviewer (Opus 5): 0 BLOCKING /
  4 MAJOR / 7 MINOR / 4 NIT; all MAJORs fixed pre-commit — drift
  figure corrected to ≈50% (`m_real` 101/200; 25% was `m_now`), the
  three audit scripts ARCHIVED VERBATIM
  (`artifacts/r3_repair_quarantine_20260801/audit_scripts/`; none
  loads chooser fields from the rep file; smoke npz git-pinned
  dd3138e7 sha 0d549b6b…), selfcheck source-pinning e2e numerics +
  episode-half legs (d_pair-from-ghat and episode-drop mutations both
  verified CAUGHT on a mirror); MINORs applied (scoped
  no-cross-job-float wording, ghat/real_scores finiteness guards,
  non-read precedent clause for within-pass gate aborts, audit_nocm
  file disclosure, model-restore rule); skipped by design:
  branch-varying JSON schema NIT, mechanical one-read guard
  (procedural, parent-inherited). **08-02 detgate re-run OK + 31 passes
  RAN + READ — NOT-REPAIRABLE-FROM-OBSERVABLES (P-REP1 straddles, not
  fragile); see the Paper-2 table row 08-02 and
  `artifacts/repair_read_20260802/`.**

- **Unfrozen stress waves U1–U4** (REGISTERED 07-27; motivated by the frozen-protocol audit `research_notes/Audit_FrozenProtocol_20260726.md`): `PREREG_unfrozen_u{1,2,3,4}_20260727` → `analysis/unfrozen_stress_read.py` (four subcommands, selfcheck PASS) + `orth_spin_unfrozen` config. **U2/U3/U4 READ 07-30** (see Paper-1 table: robust / robust / floor-censored — no flips anywhere; `artifacts/unfrozen_stress_u234_20260730/`). **U1 READ 08-01 (ONE execution, local): PROTOCOL-ROBUST — the strongest branch.** P-U1a interaction +145.0 [+22.9, +276.5] n=8 PERSISTS unfrozen; P-U1b apt occupancy +10.7 [−7.1, +26.5] — the reward-free null HOLDS under fine-tuning ⇒ hi-occupancy reward-free support is not merely frozen-illegible, it is not an initialization advantage either; headline + null family generalize beyond the frozen protocol. Unfrozen levels: task +225/+221 vs frozen (both sides). Bundle `local_results/unfrozen_u1_20260801_212204/` (32/32, exclusions all foreign waves) → `artifacts/unfrozen_u1_20260801/unfrozen_u1.json`. **U1–U4 family COMPLETE: no flips anywhere — the frozen-protocol scope objection is fully discharged.** Frozen registrations untouched under every branch — flips would have changed generality language only.
- **Pretrained-encoder pixel arm REGISTERED 07-30** (GO'd; the swamping account's lever prediction): `PREREG_pe_pixel_20260730` → `analysis/pe_pixel_read.py` (selfcheck PASS; sha256 34380e0f…) + code: `agent.frozen_enc` (optimizer module filtering), `offline_fit` partial-init w/ counter reset (the skip trap), `AXIS1_INIT_WM` plumbing, `scripts/check_frozen_enc.py` integrity gate (training witness via OFFLINE_FIT_PROGRESS; committed selfcheck; sha256 00acbdff…). Design: 16 stage-2 pixel fits (task-mode full, enc loaded '^enc/' from seed/side-matched fpxpx donors + frozen) + 16 frozen-readout adapts + E4 pass (COLLATE to a NEW csv). P-PE1 graded (CI<2.0 INCLUSION-RESTORED / <19.41 PARTIAL / else NO-RELIEF vs pinned 23.34 baseline); P-PE2 lift vs committed X2 cells; P-PE3 conditional occupancy split. Smoke gate + 16-run integrity sweep with persisted log. 2 adversarial reviews (0 blocking; all 4 MINOR hardening items applied incl. the counter-skip witness + subshell-safe sweep gate). Donors protected in runroot_cleanup (KEEP-PENDING). **08-02 READ (ONE execution): NO-RELIEF — see Paper-1 table row 08-02 + `artifacts/pe_pixel_read_20260802/`.** Bundle `local_results/pe_pixel_20260802_165601/` (manifest 152/152; git head bcc73387 = local commit, all 7 code shas byte-identical, zero drift; integrity sweep 16 PASS 0 QUARANTINE — training witness 500000/500000 counters-reset on all 16, excluding the counter-skip masquerade). Independent anchor: hand-mean of the 16 h0 `rew_nll_in` = 23.0324 ≡ reader point. Interpretation flag (non-verdict, in RECORD): the donor encoder is REWARD-FREE trained, so the null is also readable as a 4th reward-free-transfer null (legibility-coherent) rather than against spectral competition per se — separating those needs a task-trained-donor arm, NEW registration, NOT queued. fpxpx donors' KEEP-PENDING condition now met — release = user cleanup decision (releasing forecloses donor-dependent pe follow-ups). *(Superseded same day: the rde registration below re-holds the donors as its calibration reference.)*
- **Recon-detached pixel arm (rde) REGISTERED 08-02** (pending freeze-commit; user GO conditional on story value — both live branches end the pixel arc decisively): `PREREG_rde_pixel_20260802` (sha 502c36c2…) → `analysis/rde_pixel_read.py` (selfcheck PASS; sha 6a845655…) + code: `agent.recon_grad` (decoder trains on sg(latents) — trunk shaped by rew/con/repval/dyn only; the exact complement of pe), `AXIS1_ARM=rde` case, additive `deter_std` in `stratified_error` (measure moments + collate column; cross-wave-safe: additive only, all pending readers consume by column name), fpxpx cleanup hold transferred to the rde calibration pass. Design: 16 pixel fits `ax1wm_finger_rdepxq1ms<side>_seed<f>` (X2-task-identical except recon_grad False, NO donors) + 16 adapts + fpxpx calibration E4 pass (per-run `--output e4_fingerpx_v1cal` — swamping summaries never touched) + E4 pass to NEW csv. P-RD1 graded as P-PE1 (same pins 23.34/[19.41,27.29]/2.0/1.5/0.6713) **plus the latent-alive witness: deter_std ≥ 0.10×median(fpxpx calibration) on ALL 16 fits, else a numeric fire reads DEGENERATE-MASQUERADE** (registered trap: collapsed trunk → marginal reward head → false sub-2.0 NLL; pe was immune via recon-trained donors, rde is not); P-RD2/P-RD3 = P-PE2/P-PE3 with rde modes. Verdict map: COMPETITION-CONFIRMED / INCLUDED-BUT-USELESS / DEGENERATE-MASQUERADE / PARTIAL-RELIEF / NO-RELIEF(+COLLAPSE-QUALIFIED disclosure) — NO-RELIEF retires the causal competition account (both lever directions failed) and fixes the pixel boundary as a modality-level scope fact. Design-rationale disclosure: task-trained-donor variant REJECTED (near-tautological given P-SW1). Local freeze verification archived (`artifacts/rde_pixel_reg_20260802/`): image→trunk grads 114/7528 → **0.0 exact** under the flag, dec unchanged, reward path bit-identical, moment math exact. Smoke gate + config-audit sweep + calibration ordering registered. **Review DONE 08-02 (ONE reviewer Opus 5, user-approved): 2 BLOCKING / 6 MAJOR / 6 MINOR / 5 NIT — ALL B+M fixed + re-verified**: B1 cleanup-glob deletion hazard (`ax1wm_finger_rd*`/`adapt_ax1rd*` in DELETE-SAFE matched every rde run ⇒ digit-anchored `rd[0-9]*`, machine-verified vs rediag names); **B2 trivial-floor false fire — the fire region [0.6713, 2.0) contained the best-constant-predictor level, so a marginal head (the MOST LIKELY degenerate outcome) read COMPETITION-CONFIRMED and the pre-review selfcheck's Case-1 fixture was itself an instance ⇒ floor made DECISIONAL (fire needs CI entirely < 0.671345852414428) + NOT-BETTER-THAN-CONSTANT registered outcome**; M1 COLLAPSE-QUALIFIED extends to PARTIAL; M2 negative-lift + M3 NO-RELIEF-with-lift disclosure branches; M4 four surviving mutants (inverted fire bound, partial-bound-vs-point, lift-point-vs-CI, calib mean-vs-median) killed by new fixtures — six mutants total CAUGHT on mirror copies; M5 config audit fail-closed + asserted PASS count; M6 smoke rerouted through axis1.sbatch (ARM-case + --export exercised) w/ machine asserts. MINORs: Chan centered-moment combiner (unit-tested; shipped function imported by the harness), harness rev 2 (training=True, all-8-component bit-identical comparison), witness scope + cross-arm-margin disclosures, audit +expl.mode/model_obs; NITs: full-precision pins (23.338872993169502 [19.409267325402837, 27.285688932142705]), member_band ≤, calib --horizons 1, COLLATE-echo check. Final shas: prereg 70d52a30…, reader 864f0556…, stratified_error c382a6e1…, cleanup 922df763… (agent/configs/axis1 unchanged from pre-review). Both selfchecks + harness re-PASS. **Pending [YOU] freeze-commit.**
- **From-scratch baseline anchor REGISTERED 07-30** (GO'd; descriptive class — no decision rules): `PREREG_scratch_anchor_20260730`. 8 jobs, `pilot.sbatch MODE=goal` (plain dmc_proprio, zero code change), RUN_ID `adapt_scratch_finger_seed<k>_ckpt0` → collates as mode `scratch` (no frozen reader consumes it; 10 readers verified mode-pinned). Purpose: absolute-scale anchor for Fig 2a; %-of-anchor reporting never load-bearing. Reviewed (0 blocking; env.sh sourcing + staging checks added). **31 Jul LANDED: 8/8 complete + QC-pass (112 episodes each; config audit 8× from_checkpoint ''/frozen_wm false; bundle `local_results/scratch_anchor_20260731_170618/` manifest-verified). Anchor: AUC50k 113.2 (sd 42.0), AUC100k 147.7 (33.2), AUC125k 174.0 (39.2), final10 319.9 (151.7; seed-8 late collapse to 0.0, descriptive). % anchoring entered `Paper1_ResponseCurves`: W1 +84.0 = 57% of anchor AUC100k, Amend-1 +51.5 = 35%; task_s1 267.9 = 1.81×; reward-free plateau 0.53–0.68× (denominators only, protocols differ — no claim licensed). Fig 2a build blocked on a scores-only pull of per-arm adapt scores.jsonl (none local). **08-01: pull landed (`local_results/fig2a_scores_20260801_122853/`) + Fig 2a BUILT post-U1/25m-reads (`research_notes/figures_paper1_20260730/fig2a_anchoring.png` + data json + archived generating script): unfrozen-protocol arms (frozen-era per-episode scores no longer on scratch; U1/U2 no-flip reads license the substitution, captions must say "unfrozen") + scratch anchor; recomputed scratch AUC100k 147.68 matches the registered anchor exactly; unfrozen task clears scratch 2.3–3.3×, every reward-free arm sits at/below the scratch curve.**
- **High-f_R falling-limb theory predictions FROZEN 07-30** (pre-buffer; GO'd wave, registration to follow): `PREREG_highfr_theory_20260730` — P-HF1 falling limb / P-HF2 support-starvation dissociation (membership intact + diversity_pr lower) / P-HF3 interior maximum, + registered failure modes (monotone rise refutes the breadth-patch trade-off form). Derivation note `Theory_HighFR_Prediction_20260730.md` (disk-only). No high-f_R buffer exists; 25m + U1 not consulted.
- **High-f_R falling-limb wave REGISTERED 08-02** (pending freeze-commit; adjudicates `PREREG_highfr_theory_20260730`): `PREREG_highfr_wave_20260802` (pre-review sha256 acd6d7c9…) + `probing/curate_frew.py` (selfcheck PASS; pre-review sha256 6251c958…) + `analysis/highfr_read.py` (selfcheck PASS; pre-review sha256 876a2bb7…) — post-review finals at the end of this bullet. Design: quad `axis1_finger/q1f` — two curated 200-episode buffers from the frozen episode index (side0 target f_R 0.60, side1 target 0.80; deterministic contiguous-window rule, sides disjoint, NO coverage matching — breadth is measured not controlled; frozen builder materializes; SHORT ⇒ halt + dated amendment); task arm seeds 1–8 per side at the identical default Axis-1 protocol as the v200/v400 cells (16 fits + 16 adapts) + E4 h0 pass (`finger_v1`, GLOB `ax1wm_finger_hfq1f*`). Gates: `spectral_v1_1_20260724`; side1 f∈[0.60,0.85] else the read refuses; side0 f∈[0.55,0.65] + sep≥0.08 else excluded non-fatal. PRIMARY (sole confirmatory, P-HF1): mean[ax1hfq1fs1]−mean[ax1v2q1v200s1] AUC100k two-sample seed-cluster bootstrap B=10K rng0, FIRES iff CI entirely <0. Branches: monotone-rise ⇒ breadth-patch trade-off REFUTED (revision required); straddle+no-diversity-separation ⇒ saturation (uninformative). P-HF2: side1 fits' h0 in-regime rew-NLL CI ≤1.5 member / ≥2.0 inclusion-alternative (pixel-swamping constants) × diversity_pr(side1) < 9.230033291492585 (frozen v200s1). P-HF3 weak: argmax of matched-200-ep grid {v200s0 .054 n6, v200s1 .3232 n12, side0, side1} not at highest f (grid correction disclosed: freeze's matched-volume clause selects v200s0, not v400s1). Comparators = archived volume_repl bundle rows (12/6, hard-asserted). Power (restated post-review against the registered POOLED-12 comparator, mean 187.72 sd 77.18 — the against-interest cohort): MDE ≈69 vs predicted −88..−108. **Review DONE 08-02 (ONE reviewer, Opus 5): 2 BLOCKING / 5 MAJOR / 12 MINOR / 7 NIT — ALL BLOCKINGs+MAJORs fixed + verified**: B1 E4 command dropped invalid HORIZONS=0 (h0 emitted unconditionally by `stratified_error`, 0 crashes it — reviewer machine-verified); B2 side0 made never-fatal end-to-end (qc-fail/incomplete cohort ⇒ EXCLUDED with recorded reason, not abort); M1 three decision-rule mutants (point-vs-CI membership, chain-missing-lo, refute-on-ci[1]) now killed by new selfcheck legs — verified caught on mirror copies; M2 reported-mean≡members invariant added (window-shift + argmin-tie + doubled-tol mutants all caught; tie fixture rebuilt float-exact dyadic); M3 spectral side/domain pin (swap trips); M4 source-composition rider registered (f_R inseparable from collector composition as-realized; manifest mixture+source_l1 disclosed pre-fit; P-HF2 carries mechanism attribution); M5 known P-HF2 comparator level disclosed (archived v200s1 E4 h0 mean 0.827, deep in-band). MINORs applied: pooled-12 power restatement, P-HF3 near-tautology + monotone-decreasing + 2-point-chain disclosures, verdict wording "max-not-at-highest-f", collation+read commands registered verbatim, domain/milestone row filter, spectrum_pr registered descriptive, F_V200S0 provenance comment, halt rule spelled out, floor-valve scope note. Final shas: prereg bbc8f25a…, curate_frew 49b7cf6f…, highfr_read 77def16e…; both selfchecks PASS. **08-02 FREEZE-COMMITTED (39a15880) + registered curation ran on RCC → SHORT (`artifacts/highfr_short_20260802/`, provenance verified — shas match committed finals, head = the freeze commit): side1 OK via registered fallback at f=0.7293 (in-gate, inside the freeze's range); side0 target 0.60 unreachable disjointly (post-removal best window 0.4130 — the pool's high-occ tail is ~200 episodes deep) ⇒ REGISTERED HALT pre-fit, no buffer/fit/outcome exists. Amendment 1 QUEUED for GO: re-target side0 → 0.41 (gate [0.36,0.46]); side1/primary/decision rules unchanged; grid {0.054, 0.3232, ≈0.413, ≈0.729}.** **08-02 AMENDMENT 1 BUILT on user GO (pending freeze-commit): `PREREG_highfr_wave_amend1_20260802` (sha 98e1e122…) + amended `analysis/highfr_read.py` (F_LO_RANGE→[0.36,0.46] + fixtures/range legs; selfcheck PASS; sha dc474355…); `curate_frew.py` UNCHANGED (targets are CLI args). side0 re-registered as MID-GRID point (never load-bearing, as before); separation gate vacuous under amended ranges (retained as belt); deterministic curation outcome knowable in advance (side1 reproduces 0.7293 exactly, side0 lands 0.4130 at dev 0.003). Registered scope note: GRID DENSIFICATION IS OUT — a 0.60-mean window exists only OVERLAPPING side1; adding non-disjoint mid cells (or new collection) is an outcome-contingent NEW registration, warranted only if the falling limb fires AND a peak-location estimate is wanted for the flagship (the freeze claims no peak location). NO fresh reviewer (two-constant amendment on the just-reviewed instrument, per standing reviewer policy).** **08-02 AMENDMENT 1 FREEZE-COMMITTED (be60afbe) + curation OK in-gate (side1 0.7293 fallback reproduced; side0 0.4098 dev 0.0012 — a CLOSER window than the SHORT diagnostic's 0.4130 max-mean window; Amendment 1's ≈0.4130 prediction corrected in Amendment 2, no decision consequence) → BUILD FAILED pre-fit: FileNotFoundError on donor chunk; preflight needed=2046 missing=26 (chunks dated 07-01; scratch purge of never-materialized donor chunks — this wave's tail picks episodes no prior curation ever built) ⇒ AMENDMENT 2 BUILT (pending freeze-commit): `PREREG_highfr_wave_amend2_20260802` (sha bdd27a92…) + `curate_frew.py` `--require_chunks` availability filter (pool = buildable episodes, covering-chunk rule ≡ builder's load_span, eid numbering preserved — index never rewritten; excluded eids + missing files recorded in pairs json; targets/tol/fallback/SHORT/gates unchanged, re-armed on filtered pool; selfcheck REPRODUCES the field failure e2e incl. interval-overlap oracle + FileNotFoundError leg + filtered pair builds clean; PASS; sha 4b14eb47…). Builder + reader UNCHANGED. Record `artifacts/highfr_chunkgap_20260802/`. NO fresh reviewer (per standing policy). Donor-longevity flag: wave becomes donor-independent post-build, but grid-densification option needs the donors — copy off scratch to keep it open.** **→ AMENDMENT 2 FREEZE-COMMITTED (080aa7b2), wave ran clean, READ 08-03 — SATURATION, see Paper 3 table (`artifacts/highfr_read_20260803/`).**
- **Breadth-causal wave GO 08-03** (user; **reviewers pre-approved in advance** for this wave): the missing causal leg of the theory's λ/support input — two 200-ep cells MATCHED on f_R (target 0.32, the v200s1 level) and maximally SEPARATED in diversity_pr. Theory predictions FROZEN pre-feasibility (`PREREG_breadth_theory_20260803`, sha256 eb31b3a2…; committed before any feasibility scan/search/buffer exists): P-BD1 primary hi-div − lo-div > 0 (CI>0) at matched f_R + matched volume; P-BD2 ordering lo < v200s1 ≤ hi (anomaly attribution); P-BD3 membership intact BOTH cells (≤1.5 band, anchor 0.827) w/ registered inclusion-leak alternative; failure modes: informative NULL (λ-input reading loses principal support — anomaly needs non-breadth account), REVERSED (λ-monotonicity refuted), **manipulation-validity HALT gates (min instrument diversity separation + max |Δf| + target band) REQUIRED in the wave registration — the high-f_R mediator lesson made an explicit gate**; composition rider inherited (source_l1 disclosed pre-fit; no pure-diversity coefficient). Feasibility instrument BUILT same day (`probing/curate_frew.py` sha256 931bdbf7…, additive modes, frew legs regression-PASS): `scan-moments` = one chunk pass → per-episode rewarded-frame sufficient statistics EXACTLY in diversity_pr's feature space (obs_keys+symlog+f64), availability rule built in (episodes touching purged chunks recorded+excluded ⇒ buildable by construction); `search-div` = deterministic PR-greedy (objective-aligned own-PR seeds — greedy never evicts its seed, caught by the planted-family selfcheck) + least-damage occupancy-repair exchange, corridor + min_rew drops recorded. Selfcheck PASS incl.: pooled-moment PR ≡ materialized-frames participation_ratio (1e-8) AND ≡ frozen spectral instrument on BUILT buffers (1e-6, the binding path); planted narrow/broad families recovered with forced repair + mixed occ classes; determinism; SHORT; chunk-availability avoidance. Run-name family `bdq1d` (fits `ax1wm_finger_bdq1ds{0,1}_seed*`, adapts `adapt_ax1bdq1ds{0,1}_*`) glob-checked CLEAN vs runroot_cleanup DELETE-SAFE. Ordering: [YOU] freeze-commit → RCC login-node scan+search (value-blind) → IF qualifying pair: wave registration + ONE reviewer (pre-approved) → freeze → build → spectral gates → 16 fits + 16 adapts → E4 → ONE read. Donor-copy URGENT (purge active; this wave needs the pool's chunks). **→ 08-03 THEORY FREEZE COMMITTED (481aced9) pre-feasibility → FEASIBILITY STRONG: 7424 scanned (288 unavailable), pair occ 0.3038/0.3037 (matched 1e-4), search diversity_pr 7.495 / 12.336 (separation 4.841, STRADDLES ref 9.23); feature-space audit: dim 524 ≡ frozen instrument obs_keys incl. dyn/deter → WAVE REGISTERED (`PREREG_breadth_wave_20260803` + frozen `analysis/breadth_read.py`; quad q1d, modes ax1bdq1ds0/1, seeds 1–8, archived v200s1 anchor) → REVIEW DONE (ONE Opus 5): 2 BLOCKING / 4 MAJOR / 8 MINOR / 5 NIT, ALL B+M FIXED + machine-verified. B1 = diversity_pr is 97.7% `dyn/deter` COLLECTOR LATENTS the fitted WM never models (not in obs_space; replay-carry overwritten; probeset builder excludes /-keys) — a fire would have licensed "support breadth" off a WM-invisible quantity ⇒ block-pr decomposition (moments+replay modes, ≡ instrument 1e-9 on built buffers) + OBSERVABLE-SUPPORT GATE proprio-block PR sep ≥ 0.5 (HALT+reader) + pre-freeze block disclosure + "observable support" vocabulary. B2 = rebuild-onto-existing-quad silently doubles a side and f/PR gates are duplication-INVARIANT (the 2-Aug chunkgap incident's rebuild path would have passed all gates, burned 32 jobs, died at the read) ⇒ test-!-e + check-pairs preflight (sha identity + decision-OK + member-chunk re-check) + n_episodes==200 HALT witness + registered recovery rule. M3 wave identity sha-pinned (div_pairs.json committed w/ registration); M4 f-band fixtures de-confounded (message-verified); M5 P-BD3 = gross-collapse screen only (finger reward ≡ regime indicator ⇒ trivial member; historical NLLs 0.75–1.18 never near 2.0; nll_out descriptives); M6 P-BD2 curation-offset rule (+50/+57 prior; FAIL-uninformative/HOLD-strong). 12/12 mutants CAUGHT (clean control PASS); both selfchecks re-PASS. Shas: wave-prereg interim 91722494…, reader 73cc1c24…, curate_frew f58a6513…. NEXT: instrument commit → RCC round-trip (archive pairs+meta into `artifacts/breadth_reg_20260803/`, moments-mode block-pr on the pair, replay-mode on the reference) → fill --expect_sha + fold block numbers → [YOU] freeze-commit → preflight/build/gates/submit.** **→ 08-03 ROUND-TRIP EXECUTED: observable-support pre-check PASSES — pair proprio-block PRs 3.0359/3.7718, separation 0.736 ≥ 0.5; n_rew matched to 0.03% (sample-size confound dead); proprio ordering STRADDLES the reference (3.036 < 3.250 < 3.772) at ~half the full index's relative swing; latent-majority by trace (shares 0.38/0.47) = what the gate guards; **reference replay-mode pr_full 9.230033291492585 BIT-EXACT vs the frozen pin — live real-data validation of block-pr ≡ spectral_measure**. Wave identity pinned: div_pairs.json sha 48032a75… in check-pairs --expect_sha + freeze_shas. FINAL shas: wave-prereg fc24f4e0…, reader 73cc1c24…, curate_frew f58a6513…. READY: [YOU] freeze-commit → preflight → build → gates → 16+16 → E4 → ONE read.**
- **Paper-3 confirmatory 25m point** (REGISTERED 07-26, to submit after freeze-commit): `PREREG_compcapacity_confirm_20260726` → `analysis/compcapacity_read.py` (selfcheck PASS). Primary = task-lo rise (25m−12m, s0 task cells, two-sample seed bootstrap, CI>0 fires); GUARD = forbidden pattern (apt rises while task-lo floored ⇒ P-E1 refuted); 32 jobs at `size25m` (existing config block) on `q1_ctx25` (build via resize tool, deter 3072 stoch 32×24) + E4 pass `ax1wm_finger_*s25*`; power: detects rise ≈33 vs full transition 139; no blank slots — realized values go in the artifact. **08-01 READ (ONE execution, CLUSTER-SIDE by the staged driver; adopted — reader/collator/prereg shas verified byte-identical to local frozen files; 32/32 done): PRIMARY DOES NOT FIRE** — task-lo rise −12.2 [−45.7, +17.7] (t-sensitivity agrees) ⇒ regime A persists through 25m, transition bracket widens to [12m, >25m] (G1 crossing above 25m), bounded null, no ordering violation; **guard clean** (apt rise +7.0 ns — forbidden pattern NOT triggered, P-E1 not refuted); composition interaction PERSISTS at 25m (+113.8 [+68.9, +155.4], 8/8 pos; b_task_25m +119.2*; task-hi change −32.2 ns). E4 membership pass still running (feeds the THEORY read P-E2/P-E3, not this one — frozen CLI consumes auc.csv only). Record `artifacts/compcapacity_25m_20260801/` (json + code shas); bundle `local_results/compcapacity_25m_full_20260801_214345/` (scores-only per sync policy — small by design). **02 Aug USER RESOURCE DECISION (standing, like 24+24): capacity axis CAPPED at 25m — no >25m runs will be registered** (25m = 4×5090 for a full week; single-wave wall-clock at that scale is the affordability limit) ⇒ the A→B transition is permanently right-censored; bracket [12m, >25m] is FINAL; the flagship factorial redesigns to composition × capacity ≤ 25m with **capacity-robustness of the composition effect** as the flagship claim (interaction persists at every tested size; +113.8* at the cap) and the right-censored transition reported as a bounded scaling fact. Theory adjudication (P-E1–E4) unaffected — it consumes existing waves only. **02 Aug E4 membership counterpart LANDED (registered descriptive; bundle `local_results/e4_25m_20260801_225152/`, provenance verified — code shas match local, head committed ancestor; 128/128 rows):** task@25m,s0 in-regime rew-NLL (h=0) **mean ≈ 8.06 nats (per-seed 3.77–10.81) vs 12m reference 5.77** — the membership form did NOT move toward the band ⇒ **regime A persists through 25m on BOTH metrics (AUC flat + membership not approaching): the bounded null is metric-consistent**; apt rows rew-NLL absent-by-design (reward_aware gate as disclosed); no decision rule touched (csv + record in `artifacts/compcapacity_25m_20260801/`); P-E2/P-E3 theory read stays future (Scaling-B completeness blocker open).
- ~~Volume-anomaly replication P-E4a~~ → READ 07-29 (see Paper 3 table).
- **Scaling-B completeness blocker**: latest exclusions snapshot (07-25 orthogonal bundle) lists 4 `ax1(f)s12*` adapt runs missing scores.jsonl (`fs12q1s0_seed8`, `fs12q1s1_seed7`, `s12q1s1_seed2`, `s12q1s1_seed7` — set CHANGED from the X2 snapshot; seed6 completed since). Re-run or explain before the Scaling B read.
- **Comp×capacity theory predictions** (registered 07-24, adjudicated by Scaling B + 12m E4 + volume replication): `PREREG_compcapacity_theory_20260724` (P-E1 ordered un-nulling / forbidden pattern; P-E2 task-lo-first; P-E3 metric split; P-E4 diversity-as-λ); derivations in `research_notes/Theory_CompCapacity_Addendum_20260724.tex`; MUST be committed before the Scaling B read.
- **08-07 REALIZED-TRAINING DISCLOSURE (dated addendum, no verdict re-adjudicated)**: recovery of the prereg-required realized values (triggered by the Paper-3 design review, finding M5) surfaced **heterogeneous walltime truncation in BOTH capacity waves — 25m: 22/32 fits full, 10 truncated 111,900–347,250 (primary cell task-s0 7/8, task-s1 6/8, apt 9/16); 12m: 15/32 full, 17 truncated 104,550–431,425 (task-s1 = 1/8 full)**; structural cause = axis1.sbatch `latest_ckpt()` (loads newest done ckpt at ANY step) + idempotent fit-skip on any done marker ⇒ killed fits flowed silently into adapts + E4; the registered uniform-reduced-count fallback was NOT what happened and nothing was recorded. **Labeled sensitivity (B=10K rng0): dose–outcome DECOUPLED (pooled within-cell corr +0.019 @12m / −0.309 @25m — truncated fits at-or-above full in 6/8 cells); interaction@25m full-only +85.8 [+24.7, +145.4] (vs +113.8 all — survives); primary full-only −16.3 [−54.5, +16.4] (unchanged)** ⇒ registered verdicts stand; decision D4 = NO top-up re-runs; paper carries the disclosure in C1/C3/C4 + figure captions. Also recorded (review B1, labeled descriptive w/ partial-unblinding disclosure): **task-hi@25m membership rew-NLL mean 3.10, 6/8 > 1.5 bar (12m hi: 1.30 in-band) — the hi side drifts OUT of band at 25m, NOT dose-driven** (extremes are full-trained fits) ⇒ metric-consistency wording re-scoped to AUC + LO membership; ordering hi<lo preserved at every size. STANDING RULE: every future wave read verifies fit counters vs total_updates before consuming adapt/E4 outputs. s12 completeness blocker (07-25 note above): **stale — discharged 07-26** (Option-B RESULTS: exclusions EMPTY, all 32 complete). → `artifacts/compcapacity_25m_20260801/REALIZED_TRAINING_20260807.md` + `realized_training_sensitivity_20260807.json`
- ~~Paper 4 pilot / calibration / confirmatory sweep~~ → READ 07-30, ALL FOUR PRIMARIES FIRE (see the Paper 4 section table).

## Cross-cutting audits (2026-07-24, DEVIATIONS entries)

Labeling stack: 3 defects fixed pre-freeze (xpol prevact, distractor OU
snapshot, FromDM._done). Training side: clean (arm sg wiring exact;
reward hard-excluded from the model; valens/disag measurement-only;
caveat: con-head grads reach trunk — constant target in DMC,
arm-invariant, do not change mid-study). Upstream surfaces: clean
(offline_fit replay read-only by construction; replay_context
foreign-latent transient is arm-invariant; return math canonical; AUC =
online wrapper-emitted reward; E4 err_diff = out−in; Stage-1A residual
out-of-fold). No known open defects on the decision path.

## 2026-08-10 read batch (nine registered reads + aux pack; this session)

All 13 result bundles sha256-manifest-verified before any read; every
prereg confirmed freeze-committed pre-outcome (1a36a65a / 18a774fb /
825de241 / 00d6ce15). Readers selfchecked before each ONE execution.

- **PROVENANCE DISCOVERY (Amendments 1–2, `prereg/PREREG_refit_reads_amend{1,2}_20260810.md`)**: registered fit-counter preflight FAILED (restore-overlay stale counters + duplicate-job races) → substituted measured-checkpoint-step witness (96/96 at 500000). Weight-hash census: **the 08-08 loss inventory was WRONG for finger q1 (all 16 task + 10/16 apt fits = restored ORIGINAL July weights, bitwise) and cup task s0 (8/8 original)**; genuinely fresh = 6 finger apt, cup task s1 + apt, all rl/sh. The finger replication rider caught it via bitwise NLL identity — which also establishes: `stratified_error` h0 evaluation is **bitwise job-to-job reproducible on identical weights** (the ~7e-4 drift is ridge-probe-path-specific). Verdicts unaffected (substrate mostly ORIGINAL = stronger than registered).
- **Finger q1 regeneration read** (`artifacts/finger_refit_read_20260810/`): **#9 = ABSENT-OR-NONLINEAR** — task calibration passes (pooled AUROC 0.935 [0.908,0.952]), apt trunk carries NO linearly-decodable reward signal (pooled 0.401 [0.386,0.426]; in-regime 0.458 straddles 0.5) ⇒ registered wording shift: "not linearly present at all" (away from "present but illegible"); no archived verdict flips. **Finger Δ_dom = +0.534 [+0.483,+0.557]**, perm p=.008. POST-READ flag: apt pooled AUROC significantly BELOW chance in both substrate classes (anti-predictive ranking) — unregistered mechanism lead.
- **Cup regeneration read** (`artifacts/cup_refit_read_20260810/`): **P-C3 = AMBIGUOUS by the registered letter** — cup Δ_dom +0.0015 [+0.0009,+0.0028] (excludes 0 by ~0.001 at near-ceiling levels: task 0.9990, apt 0.9974) vs finger +0.534; parity-with-violation pattern is descriptively overwhelming but the CI∋0 clause fails ⇒ no P-C3 wording licensed; candidate one-line TOST equivalence-band amendment flagged (user call). Cup s1 rider: fresh draws population-consistent with the archived panel (4th-decimal agreement).
- **rl/sh own-label read** (`artifacts/rlsh_ownlabel_read_20260810/`): **P-RS1 (rl) FIRES — LEARNED-BUT-UNALIGNED** (own−true −0.0408 [−0.0502,−0.0335], p<1e-4; D6 closed at population level). P-RS2 (sh) = registered anomaly (own ABOVE true +0.0236, p=.025; post-read structure: in-regime own advantage masked by out-regime dominance). S1 not claimable (own-in CIs 1.59–1.91 > 1.5 bar). **S2 d_errin fingerprint replicates out-of-sample both codes** (−0.0165/−0.0166 vs archived −0.018).
- **s×w_r read** (`artifacts/swave_read_20260810/`): **BOTH-AXES-INERT — the corrected a²-term REFUTED in the tested range** (P-SW1 −0.470 p=.229; P-SW2 −0.648 p=.158; P-SW3 zero BH survivors of 8; P-SW5 +0.127 wrong-signed p=.067; counters 88/88, config audit 88 clean, twohot-floor correction applied). Third straight λ/energy-side registered negative (after P-HF1, P-BD1) — the causal story stays g-side.
- **TM2 mpc:False read (#24)** (`artifacts/tm2_mpcfalse_read_20260810/`): **P-MF1 NULL** (+2.05 [−32.4,+35.5], p=.910; aware simple +0.70 p=.966) ⇒ family boundary SURVIVES the readout de-confound; "reconstruction-based WMs" wording stands, one of seven D21 confounds retired. Post-read color: F1's aware-simple advantage (+35.3*) vanishes without the planner — TD-MPC2's adaptation value was expressed through MPPI.
- **orthpos read (#18 turn_easy)** (`artifacts/orthpos_read_20260810/`): **P-OP1 FIRES** — rgo−sgb on turn_easy +112.4 [+53.4,+194.0], exact p=.0078, 8/8 positive; pooled level 253.9 above the measured rndte floor band [127.7,211.2] ⇒ positive control passes; the U3 orthogonal null is about ORTHOGONALITY, not floor. Detectability gradient: identical (+45.0*) → adjacent same-channel (+112.4*) → orthogonal (+1.0 ns).
- **random-init control read (#15)** (`artifacts/randominit_read_20260810/`): **P-RI1 upgrade FIRES** — rif 70.67 vs pooled apt-frozen 81.30 (diff −10.6, perm p=.129 ns, |Δ|≤15.1 bar met) ⇒ **frozen reward-free transfer is indistinguishable from an untrained network** (null family upgrades to the no-learning form); rif inside the random floor band. Disclosure: report-only bootstrap CI [−21.2,−0.4] marginally excludes 0.
- **p2e in-protocol read (#20)** (`artifacts/p2e_inprotocol_read_20260810/`): **P-X1 NULL** (p2e-online frozen 90.5 vs apt 81.3, p=.28 — apt is a fair family representative; limitation-7 converted to result) + **P-X2 FIRES BELOW SCRATCH** (p2e-online unfrozen 82.8 vs scratch 147.7, −64.9 [−92.6,−39.2], p=.0032) ⇒ **the negative-transfer claim C-R2 extends to Plan2Explore**, the canonical published reward-free method.
- **Aux pack** (`local_results/paper1_aux_20260810_215605/`): **spectral-v2 re-measure: P-SM1 CONFIRMED under the valid instrument** (cup reward-direction rank 21/31 vs finger 45/47 both matched sides; emitted verdict: "the domain contrast is a λ-spectrum fact") — replaces the instrument-invalid v1 leg. **Target-angle audit (finger only)**: sides' target-angle distributions near-uniform, KS .095 < .136 crit, |corr(occ, sin/cos)| ≤ .064 ⇒ no placement confound; occupancy wording needs no qualifier. **D1 closure: NOT executable locally** — pack shipped the probeset npz + the x2 errors.npz PATH LIST but not the payloads; recompute must run cluster-side (script archived in `artifacts/review_response_20260808/scripts/`). s×w_r counters + config audit consumed by the swave read.
- **apt-drift arm: RETIRED** (user 08-10, no data produced; no registration consumed). **Goodhart fixed-code re-collate (D18)**: was BLOCKED on missing files — the restored/regenerated fits now satisfy its prereqs; queued back to the instance pack.
- **Cup parity TOST successor read (user GO 08-10, same session)** (`prereg/PREREG_cup_parity_tost_20260810.md` → `analysis/cup_parity_tost.py`, selfcheck PASS, ONE execution; VALUE-AWARE registration disclosed — no new compute, same bundle/gates as the executed P-C3 read): **P-C3-EQ ESTABLISHED** — cup Δ_dom +0.0015, 90% BCa CI [+0.0009,+0.0025] inside the frozen ±0.10 band, both TOST one-sided p=.0042 (resolution floor), finger 95% CI [+0.483,+0.557] entirely ABOVE the band edge (stricter bar than the original >0) ⇒ licensed wording: cup parity within ±0.10 AUROC with finger exceeding the band — the parity-with-violation dissociation lands equivalence-grade, cohering with same-day P-SM1. Original P-C3 AMBIGUOUS record untouched (`artifacts/cup_parity_tost_20260810/`).
- **#21 task-arm dose wave BUILT 08-10 (preflight PASSED by user — E3 dose chunks survive; pending ONE reviewer + freeze-commit)**: `prereg/PREREG_dose_task_20260810.md` + frozen `analysis/dose_task_read.py` (selfcheck PASS: planted log/linear/step shapes recovered decisively w/ correct change-point; flat grid → GRID-UNINFORMATIVE; occupancy/counter gates trip; CI∧BH conjunction enforced) + `submit_all.sh` AXIS1_DOSE_TASK=1 path (task-arm td names; ax1d* stays registered reward-free; default path byte-identical) + cleanup KEEP_PENDING `ax1wm_finger_td[0-3]_seed*`/`adapt_ax1td[0-3]_*` (machine-verified CLEAN vs all 94 DELETE-SAFE + 5 DELETE-AFTER globs, positive control matches). Design: 4 levels (nominal occ 0.054/0.131/0.249/0.323, ±0.02 manifest gate) × 8 seeds task fits + 32 adapts + E4 default-dir pass → NEW csvs. Rules: G1 in-wave replication gate (level3−level0 CI>0 ∧ p<.05, else GRID-UNINFORMATIVE); **P-DR2 primary = LOSO-CV shape comparison linear vs ln(f) vs step (change-point over interior gap midpoints, df disclosed; decisive at ≥5% CV-MSE margin, else TIE)**; P-DR1 secondary activation contrasts (BH q=.05 ∧ CI, MDE≈69 disclosed — interior nulls weak); P-DR3 descriptive E4 membership curve. Composition rider disclosed (occupancy manipulations change composition; the unconfounded axis was the s-wave, read INERT). Dual fit-witness registered (counters + measured-ckpt step, 10-Aug lesson).
- **#21 REVIEW DONE + ALL FIXES APPLIED 08-10 (ONE reviewer Opus 5, user-approved): 1 BLOCKING / 11 MAJOR / 7 minor-NIT — streak intact.** B1 = the shape primary was a STEP-DETECTOR: reviewer simulation at the wave's own noise (sd 77.18) showed the fixed-5%-margin CV comparison fires THRESHOLD on a PURE NULL ~19% and on graded-linear truth ~16% of the time (step's free min over 3 cuts + uncalibrated margin) ⇒ **fix = fold-internal change-point (inner LOSO per training fold) + permutation-calibrated decisiveness (within-seed level-label permutations, B=1000 rng0, 95th-pct margin crit); independently re-simulated post-fix: FLAT null 2/40 decisive (nominal 5%), LINEAR→false-THRESHOLD 1/40, STEP power ~45%**. Majors: M3 NON-MONOTONE activation branch added (only-level1 previously emitted a false LATE string); M4 dual fit-witness was VACUOUS (e4 collate has no checkpoint column; summary checkpoint = dirpath, stepless) → real --ckpt_steps input + registered producer one-liners (incl. exact awk for OFFLINE_FIT_PROGRESS — bare 'total' grep would match wm_total, a loss); M5 selfcheck mutant gaps (G1 conjunct masking, BH /m and rank mutants, no-op guarded legs, zero loader fixtures) → unit-tested activation/BH + temp-csv loader round-trips + unconditional legs; M6 milestone filter + pre-modal duplicate assert (a resubmitted ckpt450000 sibling could silently pick the primary's row); M7 occupancies now read from manifest.json occ_recomputed + sha256 recorded (hand-JSON dropped); M8 AXIS1_DOSE_TASK branch now REFUSES stale AXIS1_ARM/SIZE/BASE_CONFIG/WR/INIT_WM (task mode disables the sbatch's implicit arm guard); M9 registered literal submission command (defaults were cup+finger × seeds 1–5!) + KEEP fit glob broadened ax1wm_*_td[0-3]_seed* (cup-spill deletion hazard; machine-verified KEEP-precedence catches it); M10 D1-leak disclosure (dose buffers predate --holdout; per-level overlap counts registered into the artifact); M11 unit-of-inference scoped (ONE buffer per level ⇒ verdicts "on this grid realization"; amendment-1 demotion NOT overridden); M12 G1 power disclosed (~0.73 at +100, ~0.50 at +69; GRID-UNINFORMATIVE reworded "at this power"). Minors applied: BCa two_sample (house form), fail-closed domain filter, excluded_qc recorded, plain-python note, zero-weight stdout label, td-distinct runlist tag, protocol wording. Selfcheck re-PASS (6s). READY: [YOU] freeze-commit.
- **Goodhart D18: full-pool closure RETIRED-UNRECOVERABLE + partial re-test REGISTERED 08-11 (user GO)**: all 64 original adapt_* policy/evaluator run dirs destroyed (user inventory 08-10) ⇒ **the archived sprint (+0.153, top-1 regret 0.0-but-fragile) and p2elong (+0.209, top-1 regret 78.4%, "selection ≈ lottery") conclusions are PERMANENTLY WITHDRAWN** — no computation can adjudicate whether they were D18-bug artifacts (Midway non-determinism bars pool regeneration; #27-class). Full 32-refit+64-adapt regeneration REJECTED (fresh experiment, not closure; deferred to #28 prospective-curation scope if Paper-2 wants it). **REGISTERED instead** (`prereg/PREREG_goodhart_partial_20260811.md` + frozen `analysis/goodhart_partial_read.py`, selfcheck PASS; pool sidecar `artifacts/goodhart_partial_reg_20260811/pool_ids.json` sha c83c185d): fixed-code re-test on the SURVIVING pool — 106 finger turn_hard policies w/ live dirs (swave 88 + rif 8 + p2e 10; ter/rndte excluded = turn_easy, tm2 excluded = non-Dreamer; final10 spread 0–904.5 sd 171) × 2 evaluators (E1 = the SAME p2elong online model as the withdrawn leg, preflight; E2 = ax1wm_finger_q1s1_seed1 restored ORIGINAL weights) at the July protocol (20 starts × burn 16 × horizon 64, seed 0, INIT_REPLAY=pilot_goal_finger_seed1/replay), 212 scoring passes. Rules: P-GH1 RANKING-RECOVERED (p<.05 ∧ ρ≥0.4) / WEAK-RANKING / LOTTERY-REPLICATES / ANTI-RANKING; P-GH2 top-1 regret ≤ 0.5× exact random-pick mean; weaker-of-evaluators forward wording; gates = pinned-id identity + archived-final10 provenance (1e-6) + pool floor 95. DESIGN-SKETCH CORRECTION disclosed in the prereg: the 08-10 chat's "26 original-weight WMs vs archived AUCs" sketch confused the pool roles (members are POLICIES, not WMs) — replaced. Cleanup holds EXTENDED (swave/rif/p2e adapt dirs = the pool; release now conditions on this wave's read). No reviewer (skip disclosed). NEXT: [YOU] freeze-commit → E1 preflight → 212 scores + 2 collates → bundle → [ME] ONE read.
- **D1 CLOSURE COMPLETE 08-11** (`artifacts/d1_closure_20260811/`; payload bundle `paper1_d1_closure_payload_20260810_232932` sha-verified; labeled sensitivity, no verdict re-adjudicated): **Leg A proprio** — q1 task fits (restored ORIGINAL weights), h0 in-regime rew-NLL leak-excluded: s0 delta 0.0 EXACTLY (overlap-0 identity), s1 1.0226→1.0606 (+0.038 nats) ⇒ proprio member-band facts leak-insensitive (worst case +0.04); all full values reproduce E4 summaries bitwise. **Leg B X2 anchor** — pooled pxpx baseline 23.339 (≡ the rde/pe pin exactly) → leak-excluded 24.356 (+1.02, in-band) ⇒ rde PARTIAL-RELIEF + pe NO-RELIEF both insensitive. **Leg C** — payload rde/pe errors reproduce the 08-08 numbers to 0.0 (canonical chain). Row mapping independently verified vs the archived leaked_rows lists + npz source blocks; finger_v1 manifest sha ≡ the E4 probeset pin. D1 = fully discharged (pixel 08-08 + X2 + proprio); --holdout guard remains standing.
- **Goodhart partial AMENDMENT 1 (08-11, pre-execution, value-blind — inventory only)**: E1 (p2elong evaluator) MISSING ⇒ registered drop branch fires, E2 (restored-original q1s1_seed1 fit) sole evaluator; **18 pinned pool members have no loadable checkpoint** (rif 1–8 + p2eof/p2eou 1–5 — the rif/p2e adapt path didn't retain final ckpts, unlike axis1's) ⇒ loadable pool = the 88 s×w_r policies < floor 95 ⇒ `PREREG_goodhart_partial_amend1_20260811.md`: **floor 95→88 (the ONLY change)** — the 88 preserve the FULL final10 spread (0–904.5, sd 181; the lost 18 sat mid/low, max 342) so ranking power stands, but policy-CLASS diversity is lost ⇒ verdicts scoped "within-family policy pool"; cross-class wording (e.g. spotting an untrained policy) unlicensed. Regeneration of the 18 REJECTED (fresh adapts = new members not recoveries; ckpt-retention re-plumbing unjustified; diverse-class pool = future registration if #28 wants it). Reader floor re-pinned w/ dated comment + floor legs (87 refuses / 88 passes); selfcheck re-PASS. Scoring = 88 passes under E2 only. NEXT: [YOU] commit amendment + reader (ride-along or with the freeze-commit) → 88 scores + collate → bundle → [ME] ONE read.

## 2026-08-11 W1 + Goodhart partial reads

- **W1 ADJUDICATION READ (Paper 2) — BOTH FAMILIES: LEAK-WAS-ALL** (`artifacts/w1_read_20260811/`; bundle w1_labels_20260811_090727 sha-verified, 68/68 labels, bundled instrument code byte-identical to frozen commits, dup-null gates 2+2 PASS, ONE execution per family): **dv3** — P-W1a split-selected opportunity +0.0048 p=.898 (the archived +1.30 was winner's-curse noise, as the cross-fit collapse predicted), P-W1b actor harvest +0.024 p=.476, P-W1c ρ_full/ρ_value ≈ −0.01 ns (reward-only +0.138 p=.037 on 8 cells, below decisive form) ⇒ **the duplicate-leak witness accounted for the ENTIRE archived dv3 signal**. **tm2** — P-W1a FIRES +0.062* (small genuine max-over-candidates edge) BUT P-W1b deployed-planner harvest **−0.301 [−0.745,−0.123] p=.008 significantly NEGATIVE** (anti-harvest: the warm-started planner's action evaluates worse than the mean candidate — surprising structure fact, post-read flagged, own registration if pursued) + P-W1c null ⇒ LEAK-WAS-ALL. **Paper-2 consequence: the opportunity/achieved dissociation does NOT survive de-confounded measurement in either family — Route-B = construct invalidity + W1 clean negative + the leak mechanism.** NaN CIs on degenerate legs disclosed; permutation primary.
- **GOODHART PARTIAL READ — LOTTERY-REPLICATES / SELECTION-NOT-USEFUL** (`artifacts/goodhart_partial_read_20260811/`; bundle sha-verified 88/88 E2 scores; registered collate executed locally against the sha-verified swave scores, all 88 real-return provenance gates pass at 1e-6; E2 sole evaluator per Amendment 1; ONE execution): Spearman +0.165, perm p=.124; **top-1 regret 87.4% vs random-pick ~47% — trusting the evaluator's top pick is WORSE than random**. Fixed-code ρ sits exactly at the withdrawn-era levels (+0.153/+0.209) ⇒ the double-symlog bug was NOT the lottery story; WM imagined-return policy ranking fails with or without it (within-family scope; archived conclusions stay permanently withdrawn). For #28: documented instrument negative, two eras × two code versions.
- **Anti-harvest + evaluator-selection BATCH BUILT + REVIEWED 08-11 (user GO "explore all explanations"; ONE reviewer Opus 5, user-approved: 3 BLOCKING / 8 MAJOR / 10 minor-NIT — ALL applied, streak intact).** THREE registrations: **(1) `PREREG_antiharvest_zc_20260811`** (zero-compute on executed W1 tm2 labels) → `analysis/antiharvest_zc_read.py`: P-Z1 planner-vs-prior-MODE, P-Z2 Q-argmax-vs-mean (the value-error/planner-search dissociator), P-Z3 Q-disagreement-penalty repair (λ=1). B3 = the honesty block was FALSE (sel_q ≡ m_now ≡ plugin_choice — the executed P-W1a consumed ho(m_now) as its baseline, so Q-HARVESTS is the a-priori-expected branch) → corrected value-aware block + sel_q≡m_now load-gate witness + against-interest note. **(2) `PREREG_antiharvest_mech_20260811`** (34 new W2 passes, same 32 tm2r3 cells + 2 dup gates) → labeler `--w2` extension (4 planner first-action variants: warm/cold/iters-1/iters-12, Q-of per variant, CRN marks shared with candidates, iterations restored pre-rollout in finally) + `analysis/w2_mech_read.py` (G0 in-wave replication gate incl. SIGN-REVERSED branch; P-N1 curse w/ degenerate-state exclusion + 25% coverage floor [B2: tied-G states would manufacture CURSE — reviewer demonstrated a false PARTIAL-CURSE on a no-curse fixture]; P-N2 pressure slope over ln(iters); P-N3 inertia; verdict map CURSE/INERTIA/MIXED/PARTIAL/UNRESOLVED). M7 = W2 mark tags 100-102 COLLIDED with _w1_seed's t·101 term across labeled states → tags 10-12. M4 = labeler selfcheck missed 4/5 planted mutants → iters_log leak-counter + all-variant q_of closed-forms + w1/w2 candidate-panel bit-identity leg (leak mutant verified CAUGHT post-fix). M5/M6 = run_id↔filename + dial pins + iters_realized knob witness + planner meta gates + temp-npz round-trip legs. **(3) `PREREG_evaluator_selection_20260811`** (16 original-weight q1 evaluators × the 88-pool; 1320 new passes + E2 reuse) → `analysis/evaluator_selection_read.py`: E-R1 ensemble-mean (RESCUES/IMPROVES/FLAT w/ M11-corrected FLAT wording — no impossibility claim off the upward-biased max-single bar), E-R2 disagreement-penalized, singles panel, leg-3a pressure curve (value-aware descriptive), leg-3b shift law (SKIPPED-degrades, M9) + registered `make_action_stats.py` producer. **B1 = EVALUATORS[9] was q1s1_seed2 NOT E2** (the registered under-E2 legs would have silently run under the wrong evaluator) → E2_NAME by-name + unconditional legs. Cleanup: w2_labels/goodhart_partial/tm2r3 pinned (review #20: tm2r3 sat in the REVIEW bucket). All 4 selfchecks re-PASS. NEXT: [YOU] freeze-commit → [ME] ZC read (local, immediate) → cluster: W2 smoke+34 passes / 1320 scoring passes → [ME] ONE read each.
- **ANTI-HARVEST ZC READ — P-Z1 FIRES: PLANNER-BELOW-MODE; Q-side null** (`artifacts/antiharvest_zc_read_20260811/`; Amendment 1 `PREREG_antiharvest_zc_amend1_20260811.md` first — the initial execution CRASHED at the sel_q≡m_now load gate: the reader upcast float32 qfull to float64 before the head-mean argmax, flipping 61/6400 near-tie states vs the labeler's float32 plugin_choice (top-2 f64 gap ≤1.9e-5, tie-breaks only); zero estimand values output pre-crash ⇒ pre-outcome instrument repair: selection layer pinned to STORAGE precision, identity then exact 6400/6400, selfcheck gained a planted float32-near-tie round-trip leg; ONE execution after amendment): **P-Z1 d_mode = −0.261 [−0.612,−0.109] p=.0076 — the deployed planner's action is significantly worse than the UN-OPTIMIZED prior mode** (both domains: cup −0.309, finger −0.213; ordering mode > candidate-mean > planner). **P-Z2 NULL** +0.016 [−0.017,+0.051] p=.39 — the registered a-priori-expected branch (Q-HARVESTS) did NOT land; pooled Spearman(q,g) = +0.015 (25/32 finite) ⇒ the Q-ensemble carries NO candidate-ranking signal — neither curse nor harvest at the value head. **P-Z3 NULL** (penalty repair −0.0006 p=.97; λ 0.5/2 also null). Descriptive: penalty-selected candidate beats the planner +0.276* (anti-harvest restated). **Reading: the anti-harvest is PLANNER-SEARCH-SPECIFIC (value-error not demonstrated; candidate-level selection flat) ⇒ full mechanism weight onto the W2 arms P-N2 (pressure) / P-N3 (inertia); W2's iters→0 mode reference = live coherence check.** Portable lesson: identity witnesses vs stored labeler decisions must run in the labeler's precision — a float64 upcast before argmax is an instrument change. NEXT: [YOU] commit amendment+patched reader+artifacts (line in TODO) → cluster W2 wave unchanged.
- **Evaluator-selection AMENDMENT 1 (08-11, leg-3b producer repair, value-blind — covariate producer only, no scores/reader touched)**: `make_action_stats.py` failed on first invocation (`evaluator replay unreadable`) — TWO producer defects: registered usage pointed `--evaluator` at the FIT dir (offline fits retain no replay; E2's training buffer = `$RUNROOT/axis1_finger/q1/side1` per submit_all.sh `REPLAY=$root/side${side}`), and `stats_of` hardcoded a `replay/` subdir while axis1 side buffers are FLAT npz dirs ⇒ `PREREG_evaluator_selection_amend1_20260811.md` + patch (replay/-then-flat fallback, policy behavior byte-identical; corrected registered invocation w/ pool glob `adapt_ax1sw*_finger_seed*_ckpt500000`); producer verified locally on both layouts + missing-list disclosure; reader schema unchanged (SKIP-degrade M9 branch still in force). Legs 1–2 never at risk.
- **R1 THEORY ADDENDUM FROZEN 08-11 pre-dose-read** (`prereg/PREREG_theory_R1_gating_20260811.md`, sha 98015404…5bb8; frozen same-session BEFORE any dose-bundle file opened — timing disclosure in the doc + dose RECORD; no reviewer, predictions-doc precedent): **R1 = legibility-gating** (include(j) ⟺ a_j·s_j > θ_j; independent gating, NO competition; spectrum = readout not cause — P-SM1 kept). Part A registered out-of-sample predictions: #21 A1 G1-passes / A2 monotone-no-falling-limb / A3 step-must-not-win-decisively / A4 concave-lean-weak / A5 scope (#21 does NOT discriminate R1-vs-λ, interior) / A6 membership-rises; P-F1 TM2 attenuation (ratio<1, ≥0) + premise check; P-J1 LeWM ordering (task > LeWM > apt, strict LeWM>apt primary); P-D1 sparse-domain interaction present / P-D2 dense-domain attenuated. **Part B: λ-competition kept ALIVE in two-regime form** (user 11 Aug: "until decisive evidence") — decisive = two registered forced-regime trials with mediators-by-construction: B1 capacity-DESCENT (λ ⇒ interaction grows + membership reshuffles by λ-rank; R1 ⇒ proportional, composition stable) + B2 nuisance-injection via D0 distractor wrapper (λ ⇒ load×arm interaction; R1 ⇒ main effect only). Both-negative ⇒ λ decisively dead in-scope; either-positive ⇒ two-regime model adopted (three interior nulls become predictions — theory STRONGER than original).
- **EVALUATOR-SELECTION READ — ENSEMBLE-FLAT by the letter, but evaluator IDENTITY dominates** (`artifacts/evaluator_selection_read_20260811/`; bundle evaluator_selection_20260811_192338 sha-verified; 15 fresh collates + E2 = EXECUTED goodhart-partial metrics per registered reuse; action_stats via Amendment-1 producer; ONE execution): **E-R1 ensemble ρ +0.347 perm p=.0009, top-1 regret 13.4% vs random 78.7% — but verdict ENSEMBLE-FLAT** (ρ misses the 0.4 RESCUES bar by .053 and the max-single 0.493 IMPROVES bar; licensed wording = "did not clear the max-single bar", no impossibility claim); E-R2 penalized ≈ same. **Leg-2 singles panel (descriptive): heterogeneity is the story — q1s1_seed5 ρ .493/regret 1.7%, q1s1_seed4 ρ .419/regret 0.0% vs E2 .165/87.4%** — the "lottery" behaves like unlucky single-evaluator draws, not WM-evaluation per se (claim-grade version needs fresh-pool prospective registration — #28 candidate). **Leg-3a pressure curve (value-aware descriptive): E2 top-1 regret RISES with pool size 45%→58%→70%→79%→87%,** at/above random baseline — selection pressure monotonically worsens the pick. **Leg-3b shift law NULL** (ρ −.086 p=.43; (μ,σ) action-distance does not explain rank errors).
- **#21 DOSE READ — GRID-UNINFORMATIVE (power event, instrument clean)** (`artifacts/dose_task_read_20260811/`; bundle dose_task_20260811_203441 sha-verified; reader bitwise ≡ frozen 0251271c; occupancy gates 0.0536/0.1305/0.2490/0.3234 pass, dual fit-witness pass, modal n_ep 96, zero exclusions; leak overlap 0/11/5/… of 200 disclosed; ONE execution): **G1 +157.6 [+25.8,+309.5] perm p=.0663 — CI>0 but p-conjunct fails ⇒ gate FAILS at registered letter; P-DR1/P-DR2 zero decision weight** (interior: level2 +126.6 p=.0258 misses BH crit .025 by .0008; shape: step wins NON-decisively margin .036 < crit .079 ⇒ TIE-UNDISCRIMINATED). Everything within pre-declared M12 power limits (~0.73 at +100; observed point estimate +157.6). **P-DR3 membership descriptive: band transition between occ 0.130 and 0.249** (levels 0–1 out 1.52/1.62, levels 2–3 in 0.99/1.04; level-1 panel n=9 oddity noted). **R1 grading: A1 FAILS at the letter (scoreboard scratch, power context disclosed); A2–A4 UNADJUDICATED (zero weight); A6 descriptively coherent.** No dose wording licensed. RECOMMENDED (decision pending): P3-style power extension — seeds 9–16 × 4 levels (32 fits + 32 adapts), pooled n=16 gates.
- **11-Aug GO-SLATE BUILD BATCH + REVIEW (user GO "build 1 + other GO things, stop before LeWM"; ONE Opus 5 reviewer under the NEW standing auto-approval: 7 BLOCKING / 14 MAJOR / 8 MINOR — tm2_diag verdict NEEDS-REDESIGN, other two SAFE-AFTER-FIXES; ALL findings applied, streak intact).** THREE registrations built (B1 capacity-descent DEFERRED to next build turn — needs ctx-resized buffer pairs + sub-1m size presets; LeWM/bridge/third-domain stopped per user): **(1) `PREREG_dose_task_amend1_20260811` (#21 power extension, GO)** — seeds 9–16 × 4 levels on the same level buffers (registered command AXIS1_SEEDS="9..16"), frozen reader VERBATIM (verified n-agnostic in-review), Pocock α=.0294 on reader outputs w/ HONEST two-look accounting (look-1 .05 spent+failed ⇒ realized FWER ≤.0794, disclosed — review #13), registered 64-name dual-witness pre-read gate (frozen witness loops cover seeds 1–8 only — review B-3), pooled-integrity gate (modal n_ep pinned 96, excluded_submodal must be empty — the bincount tie-break could silently reduce the pooled read to look-1 rows, review B-4), e4 row filter (seed99 smoke row = the level-1 n=9 suspect), P-DR2 DEMOTED to descriptive-with-disclosure (per-look 5% × 2 looks, no persisted null draws — review #14), fresh-only replication = registered supplementary two_sample (the draft's second reader invocation was UNEXECUTABLE — witness refusal, review #12), corrected power basis (n=16v16 α=.0294: ~.93 @+100, MDE₈₀ ≈83), A1′ regrade registered. **(2) `PREREG_tm2_diag_20260811` REDESIGNED post-review** — the draft was killed 3 ways (B-1 reader consumed a nonexistent ridge_probe schema — selfcheck never exercised loaders; B-2 honesty block FALSE: the dv3 anchor was already READ 08-10 — finger_refit task/apt AUROC 0.93477/0.40107 ⇒ gap 0.53370 known at freeze; B-5 R²-ratio mixed two incommensurable unbounded estimators, biased TOWARD the registered prediction). Redesign: dv3 anchor PINNED to the executed AUROC values (zero dv3 compute), TM2 scalar = fold-centered pooled-OOF AUROC (fold-centering added after the selfcheck caught a train-mean fold-composition artifact manufacturing AUROC 0.33 on a leak fixture), thresholds on gap_TM2 directly (STRONG <0.267 / CONSISTENT <0.534 / VIOLATED ≥0.534 / INVERTED CI<0), side-balanced gap (review #20), per_fold witness + nan-exclude-disclosed + gate re-check (#17), house BCa (#18), audit-coefficient gates, loader round-trip selfcheck legs. `probing/tm2_legibility_probe.py`: random grouped inner alpha split + degenerate-inner REFUSAL (#26), AUROC output (#27). ≤32 probe passes total. **(3) `PREREG_horizon_20260811` REVISED** — B-6 fixed: BOUNDED-NULL now = CI-upper < δ (δ = 0.15× scratch late-mean, registered) with strict-inferiority SPLIT into APT-INIT-BELOW-SCRATCH-AT-4X (the draft would have labeled the a-priori-likely CI<0 outcome "≈ untrained" — an equivalence claim from inferiority evidence); B-7 fixed: selfcheck gained a TRUE-crossing fixture (bin localized at 3.5e5) + early-only-advantage rejection (the draft's monotone fixtures passed under ANY window — wrong-window mutants undetectable); #8 per-run config.yaml provenance gates (WM name + frozen_wm False + from_checkpoint_regex + expl mode; wrong-WM refusal tested) + donor dual-witness pre-submit command (existence-ls insufficient); #9 fail-closed inventory (unregistered-dir/missing-run/appended-file refusals); #10 walltime ≥1.5× extrapolation rule + registered truncation-recovery policy; #21 literal sbatch blocks; #11 gated descriptives; #24 early-ratio labeled non-matched; #28 substrate disclosure (all 8 hzf donors = restored July originals). Cleanup: adapt_ax1hz*/adapt_hscratch_*/tm2_diag added KEEP_PENDING (tm2wm already held). All four selfchecks PASS. NEXT: [YOU] freeze-commit (line in TODO) → cluster: #21 ext (32+32) + TM2 inventory→probes + horizon pre-gates→24 runs → [ME] ONE read each; [ME] next build turn = B1 capacity-descent.
- **11-Aug SECOND-PASS REVIEWS + B1/B2 BUILD (user GO "another reviewer on existing builds, then B1+B2"): THREE more Opus 5 reviews (verification pass on the revised first batch — 2 BLOCKING/7 MAJOR/11 minor; B1/B2 fresh batch — 1 BLOCKING+3 MAJOR on B1, B2 NEEDS-REDESIGN with 4 more BLOCKING; B2-redesign delta review — 1 BLOCKING/2 MAJOR, verdict SAFE + the whole injection chain RUN locally). ALL findings applied; 7/7 selfchecks PASS; both previously-masked mutants verified RED.** Headline catches: horizon's hzf expl-mode gate was UNSATISFIABLE (adapt stages never receive --agent.expl.mode — verified on executed U1 configs; arm identity = WM name only) + the donor-witness block couldn't parse OFFLINE_FIT_PROGRESS (key=value text, total_updates); tm2 wave's INPUT substrate (tm2wm_*/tm2_data) had NO KEEP protection (donor-dependency class); B2's replay-only nuisance key could NEVER reach the model (obs spaces are env-derived; every fit dies at update 1 on the space-equality assert) ⇒ REDESIGNED on the D0 distractor wrapper (dz1/dz2 dmc_proprio-inheriting configs w/ theta=1.0 iid emission; replay + env halves share the `distractor` key; delta reviewer RAN a real dz1 fit locally — the channels reach the decoder) + the vacuous obs-space config gate → recorded-knob gate (config.distractor.dim/basesd) + replay-side manifest σ gate (the mediator itself); the shared DID permutation was SELF-DEFEATING under a size/load main effect (power 0.69→0.04 at δ=250) ⇒ main-effect-aligned permutation (theta-invariant; type-I 0.050, power 0.74 at DID=150 independent of δ); B1's mediator confound (size presets also shrink the FRESH adapt heads) ⇒ readout64_frozen pinned-head adapt config + reader gate; dose-ext fresh-only block had incompatible CWDs + dropped 3 row filters ⇒ load_auc-reusing form; R1-gating Amendment 1 (PREREG_theory_R1_gating_amend1_20260811): A1 two-look grading + Part-B verdict→λ-negative mapping (INDETERMINATE/censored = neither) + instrument deltas disclosed. NEW REGISTRATIONS: PREREG_capdescent_20260811 (32 fit+adapt, sizes 300k/100k on ctx pairs, DID primary, floor gates, P-B1c legibility leg w/ apt-headroom gate) + PREREG_nuisance_20260811 REDESIGNED (32 fit+adapt, dz1/dz2, load-matched probesets from level-invariant holdouts, DID primary w/ informativeness gate apt(nz1)≥0.55 — a-priori-material censoring risk disclosed, anchor 0.401). NEXT: [YOU] freeze-commit (consolidated line in TODO) → cluster chains per preregs → [ME] ONE read each.
- **12-Aug FAMILY/DOMAIN GO-SLATE BUILD BATCH (user GO "proceed with all paper 1 GO remaining stuff" = the three items stopped 11 Aug; TWO Opus 5 reviewers, SEQUENTIAL build-review→fixes→verification per the 12-Aug standing policy — review 1: 5 BLOCKING / 12 MAJOR / ~20 MINOR, ALL applied; review 2 (verification): every fix FIXED-VERIFIED, ZERO fix-introduced defects, 5 new low findings (N1–N5) applied, verdict FREEZE-READY ×3).** THREE registrations + R1 Amendment 2 built: **(1) `PREREG_domains_20260812` (third domain)** — P-D1 sparse = dmc_reacher_hard (regime spec pre-existing), P-D2 dense = dmc_walker_walk (walker_run collides with the CONTROL naming convention; walker_walk reuses the existing CONTROL explorer pretrains as free collectors — deviation disclosed); NEW walker regime spec in probing/regimes.py (qvel[1] = rootx horizontal, NOT qvel[0] — verified vs the loaded model; threshold 1.0 'above'; proxy precision/recall vs the reward sensor measured 3× and quoted as RANGES); W0 estimand verbatim at n=16/domain, exact 2^16 sign-flip + BCa; P-D2 graded on d_z vs pinned FINGER_DZ 0.8284213656547132 w/ REVERSED guard (review A3); collectors via DECOUPLERS=" " (review A1 BLOCKING: "" re-fires the default and would have FORCE-resubmitted cup/finger over frozen substrate) + DRYRUN preflight; registered side-order normalization pre-build (review A2 BLOCKING — Option-C inversion precedent); index-first feasibility decision point + independent buffer_battery occupancy recompute + goal-vs-explorer proxy-separation gate; per-domain witness jsons (verification N1); TWO registered reads (one per leg), each ONCE. **(2) `PREREG_tm2_bridge_20260812` (P-F2 primary + powered P-F1)** — 3 same-era arms (aware/free/rec) ×2 sides ×8 seeds via NEW `probing/tdmpc2_recon_fit.py`: decoder MLP attached to the official pinned TD-MPC2 in EVERY arm (parameter parity), trained only in rec (recon MSE grads reach the encoder via accumulate-then-official-update — verified by source inspection: zero_grad only after step); decoder-stripped ckpts load strict into the executed adapt script; TM2_WAVE=bridge switch in tdmpc2.sbatch/submit_all (default path unchanged); P-F2 RECON-REOPENS/RECON-HELPS/INDETERMINATE on paired [free−rec]; P-F1 ratio vs pinned DV3_ANCHOR 108.33852499999999 (recipe pinned; NULL-CONTRAST = non-refutation per review B8); reader gates on the REAL bridge manifest key source_dir (review B2: the draft gated a key the pipeline never writes — fixture-agrees-with-reader class), 5 dead gates given fixture legs (review B3), STRICT modal n_ep (super-modal = scores-append signature, review X1/B4), SLURM_TIME pinned 12h. **(3) `PREREG_lewm_20260812` (P-J1, JEPA family)** — LeWM (arXiv 2603.19312, MIT, pin 8edfeb33…) trained per (side×seed 1–8) on the pxq1m pixel buffers via stable-worldmodel 0.1.1 HDF5 (NEW `probing/lewm_export.py`, schema validated against the vendored source incl. Dataset read-back leg); distill-graft: NEW `probing/distill_encoder.py` (JAX; fresh pixel_wm encoder regresses LeWM CLS embeddings; donor agent.pkl = the pe from_checkpoint contract, verified vs check_frozen_enc + elements + embodied loader; fidelity gate R²≥0.5; e2e micro selfcheck PASS locally); stage-2 = the pe loop verbatim w/ donor swapped (jppx names; jpsmoke smoke name); THREE legs: P-J1-rep primary (paired pe−jp in-regime rew-NLL, pe csv sha-pinned, cross-era exception disclosed), P-J1-emb distill-free (NEW `probing/embed_probe.py` = ridge_probe's estimator on raw LeWM embeddings vs fpxpx ridge AUROC, paired n=16), P-J1-beh floor-disclosed secondary; **review C-BLOCKINGs fixed: E4 command → pe direct form w/ COLLATE (the stage form would have OVERWRITTEN the committed swamping csv); literal jp_enc_checks.json producer registered; embedding preprocessing PINNED (--preproc required) + `verify` positive control vs the checkout's own preprocessor, reader refuses without it — the strengthened-negative P-J1 clause is control-conditional (reviews C3/C8)**; joint rule both/one/none → CONFIRMED/PARTIAL/NOT-CONFIRMED(objective-general null). **(4) `PREREG_theory_R1_gating_amend2_20260812`** — pre-outcome operationalizations: P-D2 d_z normalization + walker_walk instantiation, P-F1 level-contrast estimand + pinned anchor, P-J1 representation-leg carrier mapping (pixel behavioral floor is executed evidence). Shared statistical core (`one_sample` in domains_read) frozen jointly by all three registrations (review B10). All 7 selfchecks PASS post-fix. NEXT: [YOU] freeze-commit (line in TODO) → cluster chains (collectors→pairs→fits per domain; bridge selfcheck→48 jobs; lewm env→exports→trainings→embeds→distills→probes→grafts→E4) → [ME] ONE read per registration (two for domains, one per leg) — 12 Aug
- **12-Aug TM2-DIAG AMENDMENT 1 (mid-wave instrument repair, ZC-precedent, no reviewer)**: `tm2diag_aware_s1_seed1` crashed at the registered review-#26 fail-closed refusal — mechanism (operator diagnosis): CROSS-SIDE probing means s1 fits consume the side0 sample (positive-sparse, 535 rows, episode-clustered), and outer fold 2's seeded inner-test draw is zero-positive ⇒ constant labels ⇒ all inner alpha R² NaN ⇒ refuse; fixed seeding ⇒ ALL s1-fit cells fail identically (s0 cells all completed — they probe side1, 5770 positives). Repair (`PREREG_tm2_diag_amend1_20260812` + `probing/tm2_legibility_probe.py`): inner alpha split (training episodes only, outer folds untouched) redraws on constant inner-test labels, MAX 20, refusal preserved on all-degenerate; **RNG-stream identity** — valid first draw = byte-identical pre-amendment path, so the 16 completed s0 jsons remain valid (selfcheck pins the pre-amendment reference outputs to <1e-12 + a pinned sparse fixture that redraws [3,5,1,5] and completes where old code refused); new `inner_redraws` audit field; reader untouched, both selfchecks re-PASS. Zero estimands observed pre-repair (crashed cells wrote no json; completed jsons unread). NEXT: [YOU] commit amendment + re-run s1-fit passes only → bundle → [ME] ONE read — 12 Aug
- **12-Aug TM2-DIAG READ — PREMISE-CONSISTENT (ONE execution; bundle tm2_diag_20260812_162518 sha-verified 68/68; preregs frozen 8cd62845 + amendment b93c1e25 pre-outcome; reader selfcheck re-PASS pre-read)**: ratio = gap_TM2/gap_dv3 = 0.3365/0.53370 = **0.6305** ∈ [0.5,1.0) ⇒ P-F1's premise PASSES — TM2's aware−free legibility gap is genuinely compressed vs Dreamer's task−apt (pooled CI [0.244,0.427] entirely below the 0.5337 anchor; context-only, point rule governs). Arm means: aware **0.9993** (16/16 ≥.997 — TD-MPC2 reward-aware encoder near-perfectly reward-legible vs dv3 task 0.935), free **0.6629** — ABOVE chance on balanced probing while dv3 apt sits BELOW it (0.401): the consistency-only objective does include reward-legible support, mechanism-coherent with R1's family-boundary account. Side structure (why the scalar side-balances): side0 gap 0.152 vs side1 gap 0.521; free fits ≈ chance when probed on sparse side0 (0.43–0.54) vs 0.78–0.88 on side1 (descriptive lead only, n_data=2 sides). Amendment-1 provenance WITNESSED LIVE: 8 pre-amendment aware_s0 jsons valid via RNG-identity — free_s0 (post-amendment, same probe data) realized inner_redraws [0,0,0,0]; all 16 s1 runs realized the identical [1,0,0,3] (labels-only dependence, cross-seed uniformity = sanity witness). CONSEQUENCE: the bridge wave (PREREG_tm2_bridge_20260812, frozen BEFORE this read) proceeds as registered — no re-registration branch triggered (`artifacts/tm2_diag_read_20260812/`) — 12 Aug
- **12-Aug TM2-BRIDGE AMENDMENT 1 (selfcheck-only, pre-smoke)**: the registered CUDA selfcheck crashed at startup — the two selfcheck-internal `build_cfg(None, …)` calls (B7 reference-init replay + strict-load leg) lacked the env fallback the fit path has; repaired to `os.environ.get('TDMPC2_ROOT')` (2 lines inside `selfcheck()`; fit/checkpoint path byte-identical to freeze 7b51ad58; zero bridge runs exist). `PREREG_tm2_bridge_amend1_20260812` — 12 Aug

- **LeWM Amendment 1 — verify interface pin 08-13** (`prereg/PREREG_lewm_amend1_20260813.md`): all 3 registered verify smokes crashed at the checkout-preprocessor CALL (`get_img_preprocessor` located but called as `fn()`/`fn(tensor)`; it requires `(source='pixels', target='pixels', img_size)` and consumes a sample DICT — checkout train.py:59). Zero comparisons ran, no embed_control.json written, zero trainings/estimands exist — the exact contingency gate 2b reserved a dated amendment for. Repair (verify-scope only; embed path + reader byte-identical to 7b51ad58): construct as train.py:59 does with `--img_size` default 64 (= registered training pin), feed `{'pixels': torch uint8 [T,C,H,W]}` (byte-for-byte the swm-0.1.1 HDF5Dataset._load_slice yield — tensor input makes ToImage pass-through, so the numpy transpose(-3,-1) hazard is off-path), record `img_size`+`spt_version` (spt is an UNPINNED >=0.1.7 dep; audited vs 0.1.8), shape-mismatch = recorded pass:false + loud exit. Instrument expectation (not decisional): imagenet passes (ToImage ≡ scale+IN-normalize; Resize(64) identity on 64×64), raw/unit fail. Crash-repair precedent, no reviewer. NEXT: [YOU] commit + re-run the 3 verify smokes — 13 Aug

- **Vast XLA-flags infra fix 08-13** (`embodied/jax/internal.py`): distill queue aborts ("Unknown flags in XLA_FLAGS: --xla_gpu_enable_triton_softmax_fusion / --xla_gpu_graph_level") were NOT a queue-env leak — `internal.setup()` (called by every agent build via embodied/jax/agent.py:43, `jax.gpuflags: True` in all saved run configs) hardcodes a GPU perf-flag list and unconditionally OVERWRITES os.environ['XLA_FLAGS'] in-process, and the two flags are removed in the newer XLA that Blackwell (RTX 5060 Ti) requires; bare `import jax` never calls setup(), hence the misleading "clean direct test". Same crash would have hit stage-2 graft fits/adapts/e4 on that box. Fix: env-gated escape hatch `DREAMER_DISABLE_GPUFLAGS=1` skips the gpuflags block only (perf tuning; deterministic_ops + autotune preserved; default OFF ⇒ cluster behavior byte-identical; no registered file semantics change, no amendment — XLA perf flags were never registered, cross-hardware numerics already unpaired by standing rule). Operator: add the export to /root/.dreamer_vast_env AND restart queue workers (captured env). NEXT: [YOU] commit + re-queue distills — 13 Aug

- **HORIZON READ 08-13 (`artifacts/horizon_read_20260813/`)**: verdict = **INSTRUMENT-SUSPECT (P-H3 positive control failed) | APT-INIT-BELOW-SCRATCH-AT-4X (also bounded)**. hzt−hscratch late[4e5,5e5] = −19.9 [−41.9,+3.4] p=.136 (task-unfrozen does NOT beat scratch at 4× budget — scratch reaches ceiling ~933, the late window loses dynamic range ⇒ registered flag); hzf−hscratch = **−89.9 [−147.3,−40.3] p=.0057** (strict late inferiority; CI upper < δ=140 ⇒ also BOUNDED); P-H2: hzf NEVER crosses scratch in any 5e4 bin, early ratio 0.323, hzt's advantage is EARLY (+350 at 1e5, gone by 2.5e5). **The late-crossing attack ("reward-free payoff arrives later") receives no support**; quotable form limited by the flag to sign/ordering. 24/24 complete (496 eps uniform, zero exclusions) — 13 Aug
- **B1 CAPDESCENT READ 08-13 (`artifacts/b1_capdescent_read_20260813/`)**: verdict = **INSTRUMENT-LIMITED: both sizes FLOOR-CENSORED** (task auc100k means sk3/300k = 73.6, sk1/100k = 97.5, both < floor 113.7 ⇒ P-B1b not computed; descent overshot; shallower grid = new registration, NOT authorized). P-B1a per-size interactions tight nulls (−5.6/+4.1 ns); P-B1c APT-FLOOR-CENSORED as disclosed-likely (apt AUROC 0.413) — task legibility survives descent (0.847/0.697) while behavior floors (labeled lead). **λ mapping: counts as NEITHER** — 13 Aug
- **B2 NUISANCE READ 08-13 (`artifacts/b2_nuisance_read_20260813/`)**: verdict = **NEGATIVE-DID** — informativeness gate PASSED (apt nz1 AUROC 0.718 ≥ 0.55; the 0.401-anchor censoring risk did not materialize), DID = **−0.196 [−0.258,−0.096] aligned perm p=.0001** (~2× the 0.101 MDE): crowding hits the TASK arm hard (0.913→0.679, slope −0.234) and barely moves apt (0.718→0.681) — **outside BOTH models' registered predictions** (λ said apt loses more; R1 said no interaction). **Part-B adjudication (frozen mapping): B2 = λ-negative, B1 = neither ⇒ split/partial — reported as-is; λ UNRESOLVED-IN-SCOPE, two-regime NOT adopted, "decisively dead" NOT licensed; the both-sided miss goes on the theory ledger** — 13 Aug
- **#21 DOSE POOLED LOOK-2 READ 08-13 (`artifacts/dose_ext_read_20260813/`; LAST look)**: **G1-POOLED PASSES at the Pocock boundary** — +102.6 [+24.8,+200.2], perm p=.029297 < .0294 (by .0007 — reported plainly; family-wise ≤.0794 as registered), gates all green (modal 96, cells 16×4, zero exclusions, occupancies in-band). **Wave regains decision weight: P-DR1 at corrected BH ⇒ activation EARLY** (level1 +143.3 p=.0025, level2 +125.7 p=.0032, both survive, both CIs>0 — effect present by the 0.131 cell); P-DR2 descriptive verbatim: THRESHOLD-AT-0.092 step-wins (demoted, no confirmatory shape wording); fresh-only G1 +47.6 p=.302 sign-consistent underpowered (conformed access recorded, bit-identical to operator json); pooled means 141.7/285.0/267.4/244.3. **R1 scoreboard: A1′ PASSES (A1 recovers at look 2), A2 NOT-FALSIFIED (no decisive NON-MONOTONE), A3/A4 unadjudicated.** Instrument notes: 5 operator-flagged adapts BENIGN (uniform 112112 endpoint across all 64); e4 filter literal block has a seed9⊂seed99 substring defect — operator's realized filter enforced the intent, verified independently — 13 Aug

- **SIGMA-LADDER WAVE REGISTERED 08-14 (user GO; `prereg/PREREG_sigma_ladder_20260814.md` + frozen `analysis/sigma_ladder_read.py`, selfcheck PASS)**: discriminates the two accounts of B2's NEGATIVE-DID — (a) proportional-headroom compression vs (b) competition-with-swamped-rescue (spectral model, g-swamped regime; predictions FROZEN pre-outcome so a fire upgrades the paper story to predicted-then-confirmed). 2 new loads σ∈{2,8} (dzs2/dzs8 configs, q1_nzs2/q1_nzs8 buffers, own probesets) × 2 arms × 2 sides × 4 seeds = 32 fits+adapts, protocol-identical to B2. PRIMARY P-S1 = per-load contrast of HEADROOM-NORMALIZED drops vs pinned executed-B2 baselines (0.9133/0.7180), step-up BH m=2, floors 0.55; branches EXCESS-TASK-LOSS (incl. the (a)-IMPOSSIBLE task-collapse configuration) / EXCESS-APT-LOSS (statistical route only) / PROPORTIONAL-CONSISTENT (±0.25 equivalence, ~90% CI coverage disclosed) / INDETERMINATE / INSTRUMENT-LIMITED; value-aware S(4)=+0.163 anchor disclosed; does NOT reopen Part-B (adjudicates the ACCOUNT of the anomaly only). ONE Opus review (sequential): 2 BLOCKING caught — B1 the apt-side collapse branch fired a directional verdict on data the proportional null GENERATES (demoted to reported-only; task-side branch kept, its (a)-impossibility is derived: f-crossings .121 vs .229); B2 the holdout-equality gate was vacuous (rng-0 draw depends only on source listing) — replaced by pinned cross-era sha 0c760437…0e97a (computed from both committed B2 manifests, verified independently); + M4 per-rank BH → step-up (per-rank could flip MIXED into clean EXCESS), M5 literal replay-linkage block (runs.csv field 7), M3 honesty wording (σ=1 IS in the rule as the pinned baseline), 8 minor (NaN-auroc gate, manifest tool/source/16 gates, per-load RMS MDE, named-loads verdict strings, 7 new refusal legs). All applied; selfcheck re-PASS; Type-I of the directional rule verified 4.7% by the reviewer. NEXT: [YOU] freeze-commit → cluster chain — 14 Aug

- **W2 MECH READ 08-14 (`artifacts/w2_mech_read_20260814/`; bundle w2_mech_20260812_213627, 78/78 sha OK)**: verdict = **REPLICATION-FAILED — in-wave anti-harvest −0.128 [−0.354,+0.034] p=.217 misses the G0 gate ⇒ all three mechanism legs ZERO decision weight** (frozen rule; W1's own anti-harvest fact untouched). Honesty: the G0 CI CONTAINS W1's −0.301 — power event, not contradiction (realized MDE80 ≈ 0.31 ≈ the W1 magnitude). Zero-weight panels: P-N1 curse = **−0.794* WRONG-SIGNED** (planner action model-UNDER-valued on retained states; coverage 27.1% just above the 25% floor, finger heavily G-degenerate) — the search-pathology (not value-peak) signature, coherent with ZC's planner-search-specific reading but unregistered; P-N2 pressure flat (slope +0.003 p=.93; iters 1/6/12 means 40.18/40.18/40.19, prior mode 40.32 ABOVE all variants — echoes ZC P-Z1); P-N3 inertia null (+0.034 p=.46); Spearman(q,g)=+0.020 in-wave (ZC P-Z2 reproduced). All gates green (32 cells + 2 dup exact-zero, iters_realized witness, planner meta pins). **Consequence: anti-harvest mechanism = registered-compute CLOSED, unattributed; no mechanism wording licensed; any retry = new registration that must beat MDE 0.31 (design hint parked in RECORD: iters 0-vs-1 mode-vs-single-update cell)** — 14 Aug

- **PAPER-2 COMPUTE STOP (user decision 08-14)**: evidence base FROZEN for drafting — W2 seed-extension DECLINED (assessment: even a passing G0 yields UNRESOLVED since all three mechanism legs are known null/wrong-signed; cluster pool exhausted at 32 cells; robustness already carried by W1 −0.301* + ZC −0.261*); iters-0-vs-1 mechanism cell NOT registered (conditional: only if the mechanism section is thin at draft time). Evaluator wave (PREREG_evaluator_selection_20260811, frozen) stays REGISTERED with execution DEFERRED at user discretion — the draft does not wait on it; if its bundle ever lands the ONE read still applies as registered. [YOU] next: write Route-B draft ((a)+(b) composed framing per 08-09 decision) + trigger AI reviewers — 14 Aug

- **SE STAGE-1 REGISTERED READ 08-14 (`artifacts/se_stage1_read_20260814/`; bundle uncfield_se_stage1_20260813_193324, 76/76 sha OK, Vast queue failures=0, code shas bit-match frozen instruments, fit counters 8/8 ≥99.5%)**: verdict = **OUTCOME CELL 3, N-ONLY — P-SE1 FIRES on the distractor 8/8** (per-run perm p=.001 ×8, binomial .0039, BH survives; share-vs-real 0.611 [0.592,0.625]; **θ₁ = 5.26× the source key [5.02,5.46]** — the deployed p2e disagreement objective prices a by-construction zero-information OU channel at 5¼× a real key in its own accounting); **D-family = tight informative null at exact source-parity** (0/8 ×3, per-run p .90–.99 = BELOW-null; θ₁ dup0 = 1.0000 [0.9989,1.0004], dup1 1.000, dup2 0.973; ladder excess ratio −62 vs +100 ε²-prediction ⇒ neither redundancy nor ε-noise farming — fictitious pricing REQUIRES irreducible stochasticity); **P-SE2 NO-FIRE 1/8 with NEGATIVE delta** −0.0170 [−0.0277,−0.0064] (both families negative; OU pedestal inflates level, not ranking variance — accounting/ranking dissociation). Calibration max .049 (bar .5); const raw-normalizer exactly 0 → None as registered; instrument CLEAN; se_mask not needed (cell-6 only). Flagship NOT licensed. **TRIGGERED: D-only arm (§3 verbatim: N fires + all D at floor; 4 runs seeds 10–13) + Stage-2 behavioral (P-SE1 fired non-C; needs data-independent gate-pinning amendment first). M4 NOT EXECUTABLE from bundle** (ckpt_snapshots+replay not synced; weak form carried by primary at 100%; salvage from Vast instances if alive — snapshot probes MUST use --output se_probe_snap<pct> per #22-B1). Paper-A wording licensed: model-internal noisy-TV accounting exhibit at standard WM scale + duplicate-parity scoping null + planner-dissociation; NOT licensed: redundancy farming, cell-1 flagship, behavioral claims — 14 Aug

- **SE M4 + AMENDMENT-2 BATCH 08-14 (`artifacts/se_m4_read_20260814/`; bundle uncfield_se_m4_20260814_174131 64/64 sha OK)**: **M4 PERSISTENCE READ = WEAK FORM HOLDS 8/8 + misprice FULLY FORMED BY 25% AND FLAT** (distractor share 0.622→0.644→0.611 across 25/50/100%, indicator 8/8 at EVERY fraction, θ₁ 5.24×→5.81×→5.26×, non-decay BCa [−0.033,+0.009] FLAT-OR-MIXED; D-family 0/8 at every fraction — never farmed at any stage; all gates green: 16/16 VERIFIED-NEAREST snapshots, cross-fraction raw_norms bit-identity, 100% byte-identical to Stage-1, zero invalid cells) ⇒ strongest observable form of the §2 finite-time non-decay claim. **Amendment 2 registered** (PREREG_nfi_scale_exhibit_amend2_20260814: D-only read rules w/ θ₁ band [0.90,1.10] + 4/4-indicator arm — BOTH replacing forms my own fixtures proved degenerate (≥3/4 fires at .31/channel under parity null; CI-excludes-1.0 fires on 0.4% deviations); Stage-2 gate pinned position[0] > −0.13009691 = pre-Stage-1 smoke median (0.0 would be 2.96%-open degenerate), BOTH wrappers gated (distractor.py gains reviewed BITWISE-INERT gate — Paper-1 dz/Paper-2 D0/TM2 consumers verified unaffected), gated seeds 20–23 / ungated 24–27, M3 = full-buffer occupancy + exact C(8,4)=70 one-sided perm, fires iff p≤.05; M4 operationalization + Midway3 site disclosure). **3 frozen readers BUILT+REVIEWED** (se_m4_read/se_donly_read/se_m3_read; review #23 ONE Fable: 2B/5M/11m ALL 18 APPLIED — B1 superseded-map docstring, B2 unregistered M3 α, M3 skippable byte-identity gate, M4 missing fixed-eval witness, M5 degradation-asymmetric NO-INTERFERENCE, M6 unrecorded gate constants, M7 dropped ckpt_flag; reviewer independently reproduced the threshold pin + verified gate inertness for every consumer; 4 selfchecks re-PASS). Yield 23/23. **[YOU] freeze commit + 12 submissions (D-only 4 + Stage-2 8) — specs in TODO/amendment §4** — 14 Aug  **16 Aug: ARMS SUBMITTED via ops wave `se_arms` (commit 35a053ba) — spec transcription verified against amendment §4 (12 run_ids, deltas, threshold −0.13009691 exact); producer gained 4 inert env overrides (defaults = Stage-1 values byte-for-byte; DISTRACTOR_DIM:-8 note = Stage-1 hardcoded value, not the config default 0); dim=0 short-circuits the wrapper at main.py:252 with planted's 0x5E stream independent — D-only is exactly Stage-1-minus-N; verification addendum appended to Amendment 2 pre-outcome. Bundle reminders pinned: D-only needs post-training se_probe ×4; Stage-2 needs replay/ synced.**

- **SIGMA-LADDER READ 08-15 (`artifacts/sigma_ladder_read_20260815/`; bundle sgl_20260815_123342, 72/72 sha OK; freeze d5390c4a pre-outcome; Vast-provenance conformance disclosed per prereg clause)**: verdict = **EXCESS-TASK-LOSS at BOTH loads — competition-with-swamped-rescue CONFIRMED as predicted-then-frozen; proportional-compression REJECTED at σ=2 and σ=8** (nzs2 +0.339 [+0.134,+0.457] p=.0016; nzs8 +0.371 [+0.239,+0.513] p=.0006; both > their realized MDE80 ≈0.24/0.21; all floors passed, statistical route only; B2-era holdout sha MATCHED the pin). S(σ) curve {0, +0.140, +0.163, +0.153} at σ{1,2,4,8}: **knee sits BELOW σ=2** — task arm surrenders 38.5% of headroom at σ=2 (apt 4.6%), then flattens 0.754→0.679→0.679 while apt declines gently; B2's original dose sampled past the knee (its NEGATIVE-DID = the saturated form). **Frozen consequence mapping fires: the B2 anomaly gets its registered competition-mechanism account (spectral model, g-swamped regime — low-variance reward-relevant support excluded first despite the a² rescue); paper may claim the mechanism citing this wave as its predicted test; R1 scope note written from this account. Part-B stays split (not reopened). Theory-ledger synthesis: competition EXISTS and binds — against the opposite victim from the original λ prediction.** Parked conditional TRIGGERS: B1′ ~600k capacity-side convergent test now worth registering — [YOU] GO/NO-GO. [YOU] commit ride-along: `git add artifacts/sigma_ladder_read_20260815 manifests/sgl_20260815_123342.sha256 STUDY_LEDGER.md` — 15 Aug

- **B1' WAVE REGISTERED 08-15 (user GO executing the triggered conditional; `prereg/PREREG_b1prime_20260815.md` + frozen `analysis/b1prime_read.py`, selfcheck PASS)**: the capacity-side convergent test of the σ-ladder competition mechanism — size600k (deter 384/units 48, NEW preset + whitelists + ctx pair q1_ctxk6 + KEEPs), 16 fits + 16 readout64_frozen adapts. ONE Opus review = NOT-FREEZE-READY, 3 BLOCKING all applied: **B1 the executed capacity–legibility decline is ENTIRELY side-0** (1m/300k/100k side0 0.903/0.710/0.400 vs side1 RISING 0.967/0.984/0.994 — the pooled primary would have been blind to its own target) ⇒ primary now SIDE-STRATIFIED two_sample vs per-side pinned anchor runs, MECHANISM-CONVERGENT requires both sides, SINGLE-SIDE(side0) = the registered a-priori-likely qualified form, side-1 rise = benign expected note not anomaly; **B2 MDE was wrong** (assumed sd 0.03–0.05; executed sds 0.052/0.202/0.318 ⇒ MDE80 0.06–0.20, unpowered under 300k-like dispersion — disclosed plainly); **B3 anchor was treated as a constant** (SE 0.013 ≈ the new sample's) ⇒ two_sample against the 16 pinned per-run 1m values so anchor uncertainty enters the null. +3 MAJOR (M1 apt control direction-split — drift TOWARD chance is mechanism-consistent, not suspect; M2 knee-interval wording earned not asserted; M3 ctx-zeroing protocol asymmetry disclosed as carried caveat) + 9 minors (deter WALK gate, all-four-head pins, len asserts, literal ridge/collate commands arg-verified, sha-verify step registered). Selfcheck re-PASS (7 branches, 18 refusals). Consequence mapping: fire ⇒ two-lever mechanism sentence (σ+capacity); non-fire ⇒ ladder+B2 stands alone. NEXT: [YOU] freeze-commit → cluster chain — 15 Aug

- **RESCUE-UNDER-LOAD WAVE REGISTERED 08-15 (user GO; `prereg/PREREG_rescue_load_20260815.md` + frozen `analysis/rescue_load_read.py`, selfcheck PASS)**: the a²-term boundary test resolving the s×w_r inert-a² tension — 16 task fits at σ=2 (ALL substrate reused: dzs2 + q1_nzs2 + finger_nzs2_v1, nothing new built), w_r ∈ {1,100} via AXIS1_WR, PAIRED per-(side,seed) primary (exact sign-flip on 8 deltas). Branches: RESCUE-CONFIRMED (saturation-off-boundary account; β manipulable) / NO-RESCUE-DETECTED (gradient-geometric favored AGAINST FULL-MAGNITUDE rescues only — realized MDE inline in the verdict string) / RESCUE-INVERTED. ONE Opus review = NOT-FREEZE-READY, 1 BLOCKING + 6 MAJOR all applied: **B1 the read had NO binding buffer witness** (config.yaml records the env token, never --static_replay — an _nzs8 mis-submission passed every gate) ⇒ ruw_replay_linkage.json promoted to a fail-closed --linkage reader gate + literal producer block (M2); M3 MDE by SIMULATION of the conjunction (3.4× multiplier; paired MDE80 ≈0.15; half-rescue at 35% power — the pairing hedge was arithmetically unreachable, withdrawn; pairing buys ~8%, legitimate but modest); M4 MDE inline in the NO-RESCUE string; M5 pairing selfcheck leg made DIAGNOSTIC (paired fires / unpaired ns on the same side-gap data); M6 foreign-run_id leg; M7 full knee CI [0.42, 2.06] quoted + too-small-dose reading folded into the null branch; + 9 minors (literal smoke direct-form — the bundles route can't produce it; adapts run at rew=1.0 both cells, disclosed; ceiling censoring 0.954 anchor; per-fit values stored in read.json; manifest source gate; probeset equality). Selfcheck re-PASS (18 refusals). ALSO 08-15: **per-fit BOOTSTRAP of the parametric fit executed on user GO** (labeled; B=5000, 0 failures; artifacts/sigma_ladder_read_20260815/POST_READ_parametric_bootstrap.json): rescued knee < 2.06 in ≥97.5% of draws, shared knee censored >8 in 100%, **λ_corr/λ_rew_eff > 37 at 95% bootstrap confidence**. NEXT: [YOU] freeze-commit (consolidated w/ B1′) → cluster chains — 15 Aug

- **GPT PUBLISHABILITY REVIEW ADJUDICATED 08-15** (`research_notes/paper{1_wm_transfer,2_evpi}/reviews/Adjudication_GPT_Publishability_Claude_20260815.md`): the 14-Aug external review is one wave STALE — its Paper-1 kill-shot ("legibility = renamed intuition") and theory-wording advice ("mechanism unresolved") are superseded by the σ-ladder confirmation + parametric bound; the draft must be revised from the post-σ-ladder ledger, NOT the review's wording. ADOPTED: §7 draft compression + amended spine (mechanism arc inserted); §10 Paper-2 Route-1 (bias taxonomy + local sims — recommended, user GO, fits the compute stop); §13 appendix-centrality. DEFERRED: FB/successor baseline conditional on TM2-bridge + LeWM reads. REJECTED-AS-ROUTE / ELEVATED-AS-SEED: prospective anti-harvest replication (dominated for Paper-2; own-paper seed). Factual corrections carried: horizon quoted with the INSTRUMENT-SUSPECT flag; B2 anomaly no longer "outside both models" — 15 Aug

- **PAPER-2 ROUTE-1 EXECUTED 08-15 (user GO "proceed all along with Route 1"; non-registered writing-phase upgrade, zero cluster compute)**: `research_notes/paper2_evpi/route1/` — **T1+T2 taxonomy** (`Route1_Taxonomy_20260815.md`: four channels — A order-statistic pedestal σ_eff·E[max_k Z], B selected-sample reuse, C shared-stochasticity leakage, D wrong-null referencing; zero-bias conditions + corrected-estimator table; archived +1.30 decomposed as A+B+C+D w/ draft-§6 check→channel mapping; 9-shape in-the-wild pattern scan, ALL external citation keys TODO-VERIFY), **T3 simulation suite** (`route1_sims.py`, numpy-only, seed 20260815, deterministic, 8 s; selfcheck 12/12 PASS incl. the 2-Jul verified closed forms 0.22053/0.3990, planted-leak mutant power 0.98, determinism gate; results `route1_sims_results.json` + `Route1_Sim_Results_20260815.md`): pedestal matches σ/√m·E[max_k Z] every cell; reuse +0.712 vs fresh/split ns; leakage ρ 0.48 at 50% shared variance (archived duplicate-group +0.47 consistent-with); composed E4 at calibrated σ=0.876 reproduces pedestal +1.246 from TRUE ZERO, ×2.01 under doubled noise, cross-fit collapse −0.007; corrections all ~0 except analytic-debias residual +0.055 quoted honestly; **diagnostic battery measured: duplicate-null size .045/power .98, perm-null coverage .99/single-study power .43 (reference-fix not detector), cross-fit delta size .000/power .89 (parametric-bootstrap calibrated)**, **T4 protocol** (`Route1_Diagnostic_Protocol_20260815.md`: channel-organized pre-flight generalizing draft §10; channels A+D decidable from the loop declaration alone). T5 restructure = writing chat. NEXT: [YOU] T2 verification of TODO-VERIFY keys; decision on promoting route1_sims.py into the tracked repo for the repro package — 15 Aug

- **ROUTE-1 T2 CITATION VERIFICATION CLOSED 08-15 pm** (user pass: Crossref/EuropePMC/publisher/arXiv, all 18 works + quote anchors in `research_notes/paper2_evpi/route1/Route1_T2_Verification_Checklist_20260815.md`; Claude applied all 8 bookkeeping edits to the taxonomy + resolved the last open row same session): **no metadata fabrications; 3 SHAPE errors caught by the load-bearing check** — jannerEtAl2019 documents the OPPOSITE of the claimed exploitation shape (re-anchored kurutachEtAl2018 ME-TRPO), fenwick2008implementation has no estimator-bias content (DROPPED), strongOakley2014/2015 are efficiency methods not debiasers (reworded); **2 promotions** — brennan2007bias ("Maxima of Monte Carlo estimates of expectations are biased upward" — channel A in the EVPI estimand itself) + smith2006curse (optimizer's curse, Mgmt Sci: channels A+B w/ Bayesian debias) now lead the prior-art paragraph; **my "already in the Paper-2 bib and verified" claim was FALSE** (no .bib exists anywhere in the repo — struck; integrity lesson: never assert bib state without grep); colas = 2 conflated papers split remedy-side; patterson rekeyed JMLR 25(318) 2024; stiennon title trap (proceedings "with" / arXiv "from"); best new finds = machado §4.2 (published RL prior art for channel B + fresh re-eval, now cited in the corrected-estimator table) + patterson §3.2 (channel A stated as a law). **MPPI/CEM closed by Claude web pass (checklist §17)**: williamsEtAl2017 + chuaEtAl2018 verified as setting anchors (PETS quotes extracted from the proceedings PDF; honest nuance — PETS particle-averages BEFORE the max). Remaining before verbatim quoting: 2 publisher-PDF re-checks (jordan doubled-"only", machado §4.2 wording) — 15 Aug

- **ROUTE-1 T2 FINAL RE-CHECKS CLOSED 08-15 night (user, publisher PDFs) ⇒ T2 ZERO OPEN ITEMS**: jordanEtAl2020's doubled "only" ("By only reporting only the tuned performance…") is REAL in the PMLR camera-ready — quote with `[sic]`; machadoEtAl2018's channel-B passage is **§4.1.2, p. 531 (NOT §4.2 as I had recorded)** and the sentence ends "as reported by Wang et al. (2016)" — corrected throughout checklist (row 10, §8, §10, bookkeeping) + taxonomy (header, §4 channel-B row, §5 item 2). Route-1 citation base is now fully verified with quote anchors; the claim-freeze exhaustive lit audit remains a separate later pass — 15 Aug

- **GPU-PROVENANCE AUDIT IMPACT ADJUDICATED 08-16** (`artifacts/gpu_provenance_audit_20260816/RECORD.md` + lewm_probe_devcheck FINDING; ops fix = wavegen --constraint=v100 default, 225/225 selfchecks): **no registered verdict falls** — dose G1/P-DR1 ride Vast-side behavioral AUC (never touch the RCC V100/A100 split); td membership/band descriptives are nats-scale, device-stable ~1e-5 per devcheck (the 0.113 cross-device shift is specific to ridge-AUROC/R², where the ill-conditioned fit amplifies bf16 arch differences). REAL impacts: (1) **B1′ = the live hazard** — its primary compares fresh ridge AUROCs to pinned anchors (regen_ridge_e4_20260810_220849, device not positively established; A100 excluded by enumeration, V100-vs-Vast undecidable by sacct) while the device term (≤0.113) sits inside MDE80 0.06–0.20 ⇒ URGENT pre-ridge-stage amendment registered as TODO (device lookup → constraint pin or 32-anchor re-measure + tuple swap; ridge_probe device stamp folded into the same amendment; ops sidecar witness immediately); (2) td2/td3 s5,s6 (non-Slurm, device unknown) quarantined from per-cell E4 use; (3) draft gets a one-sentence td-E4 split disclosure; (4) arm-homogeneity lookups owed for orth_obj_b01 + axis1_bundle_20260812_001 (device at shard granularity matters only if arm-aligned; orthpos behavioral 8/8 robust regardless); (5) rescue-load clean by design (paired within-(side,seed), single constraint-pinned ridge stage; SGL cross-check already registered cross-hardware-descriptive) — 16 Aug

- **GPU-PROVENANCE CORRECTIONS + RESOLUTIONS 08-16 late (ops follow-up; supersedes parts of the 08-16 adjudication entry above)**: (1) **node map was wrong — THREE models**: 0277–0281 V100, 0282–0286 RTX6000, 0294 A100 (audit conclusion survives; td split is **A100 vs RTX6000**, not vs V100); (2) **cudnn 5003 is a Volta-architecture fact in this JAX build, not an RCC limitation** — pixel probe jobs separate perfectly (V100 9/9 fail, RTX6000 3/3 clean) ⇒ pixel legs CAN run on RCC pinned rtx6000; blanket --constraint=v100 default was itself a bug for pixel waves, fixed (lewm_probe_rcc pinned rtx6000, 228/228 selfchecks); (3) **B1′ anchor device RESOLVED = V100 midway3-0278, single node** (bundle's own producer logs, Slurm array 53243601_0..3) ⇒ cheap resolution: constraint v100 pin (recommend --nodelist=midway3-0278, which moots pending same-model-different-node job 53411135 for B1′), NO re-measure, NO amendment; B1′ fits 16/16 DONE, ridge = next step; (4) **LeWM pe re-measure recommendation WITHDRAWN (read-side verification done)**: lewm_read's jp↔pe primary is rew_nll_in (nats; effect gaps 0.1–10 nats vs ~1e-4 device term), AUROC legs already single-GPU (fpx unified on Vast; embed CPU), pe csv sha pin stays ⇒ step 9 = RCC --constraint=rtx6000, no Vast, no new pin; ADD 1-cell pe re-measure diff on rtx6000 (directly prices the cross-device nats term — devcheck bounded deter_std, not rew_nll itself; disclosed, non-gating). **NET: zero code-side amendments; all resolutions are ops routing + disclosed provenance** — 16 Aug

- **PE 1-CELL DEVICE-DIFF PRICED 08-16 (ops, rtx6000, non-destructive; supersedes the "~1e-4 nats device term" inference in the entry above)**: rew_nll_in moves **−0.0994 nats (−0.208%)**, rew_nll_all −0.215%, n_in_regime identical (numeric drift, not strata) — the drift is ~0.2% RELATIVE and is an UPPER BOUND bundling device + env + 2 weeks (single cell). **Executed reads: NO material impact** — (a) proportional scaling puts the term at ~0.003–0.006 nats at membership-band scale (tightest executed margin 0.09 nats = ~30×) and ~0.05 at X2-anchor scale (effects 1–16 nats); (b) design absorption: within-panel contrasts cancel the term; σ-ladder's cross-instance seeds put device variation INSIDE the CI as variance (bias-free by arm balance within instance), and it fired anyway. **ONE real exposure: pending lewm_read P-J1-rep** — paired [pe − jp] with historical pe vs fresh jp; a systematic 0.1-nats pe-side term shifts every delta identically (does NOT cancel) and whether it can tip the CI depends on unknown realized sd ⇒ **reinstated (narrow) amendment**: re-measure all 16 pe cells on rtx6000 (same device as jp panel; proven 1-cell machinery, side directory), dated pre-outcome amendment swaps the reader's pe sha pin, frozen BEFORE jp E4 runs; direction-disclosed (known pe shift is downward ⇒ disfavors the fire branch = conservative); bands/beh legs non-decisional or behavioral, emb leg already same-device — 16 Aug

- **FIT-WITNESS INSTRUMENT DEFECT FOUND 08-16 (ops; `artifacts/stale_fit_witness_20260816/RECORD.md` + `rcc_scan.json`; no verdict adjudicated)**: `OFFLINE_FIT_PROGRESS` on RCC is **not a completion counter — it is whichever copy was pulled last**. Six adapt cells failed the checker's fit-side dual witness while carrying a *completed* `ckpt/<ts>-000000500000/done` and a full 112-score adapt; a fit that stopped at 114,150 cannot have written a done ckpt at 500,000. Mechanism: the same run exists on several instances as unequal copies (instance 1 produced `ax1wm_finger_fsk6*` at `update=500000` matching its own ckpt dirname; instance 5 holds abandoned attempts stopped ~114k), and generated pull **pass 2** (`rsync -az -c --ignore-times` over fixed witness basenames) is checksum-forced with **no notion of which copy is newer**, so the last instance pulled wins. Two rsync sub-facts: pass 2 emits `--exclude='*'` BEFORE its wave-scoped `--include=/<run_id>` rules ⇒ those are dead and pass 2 syncs witnesses **repo-wide**, letting one wave's pull clobber another's; and `-a` preserves source mtime ⇒ RCC mtime cannot date the clobber. **SCOPE: full `$RUNROOT` scan = 63 fit dirs with `update < total_updates`, 63/63 carry a done ckpt at/above target, 0/63 genuinely short** — spans `cup_*`, `finger_{s12,s25,fs12,fs25,pxpx,fpxpx,swv*,swr*,bdq1d}`, `walker_fq1s0`; weeks old, today's all-waves×all-instances sweep only made it visible. **CONSEQUENCE FOR THE 08-07 STANDING RULE** ("verify fit counters in every future read"): as applied to RCC's `OFFLINE_FIT_PROGRESS` it now errs *opposite* to the 08-07 hole — it **understates** completion. Neither counter is self-sufficient: the witness is authoritative only on the producing instance; the done-ckpt is positive and forgery-resistant but silent about a stop *between* save points. Suggested (a read's call, not ops'): a done ckpt at `total_updates` is sufficient evidence of a full fit, and a witness sitting *below* a done ckpt it should dominate is evidence about the sync, not the training. **REPAIRED 4** (fsk6 witnesses re-pulled from producing instance 1 = authentic bytes, not hand-edit ⇒ b1prime fit stage 12/16→16/16; its 16 remaining pendings are the V100-blocked ridge panel). **NOT REPAIRED 2** (`ax1wm_walker_fq1s0_seed6/_seed8` — producer destroyed, only instance 5's abandoned partial survives; fits attested by done@500000 + consuming adapt) ⇒ **walker_domain reports 62/64 and was deliberately LEFT UNBUNDLED** — whether those cells count is the owning chat's call and bundling would imply an answer. **NOT TOUCHED 57** (producers gone; writing a plausible witness would be fabrication). **RECURRENCE: instance 5 still holds the abandoned fsk6 partials ⇒ any future `b1prime` pull from instance 5 re-plants all four stale witnesses; pull b1prime from instance 1 only** until pass 2 learns to prefer the newer witness — 16 Aug

- **[URGENT — PAPERS 1–3 CHAT] 08-07 TRUNCATION DISCLOSURE MAY BE THE SAME WITNESS ARTIFACT 08-16 (ops flag, NOT adjudicated; `artifacts/stale_fit_witness_20260816/` RECORD §"08-07 disclosure" + `capacity_ckpt_vs_witness.json` + `ckpt_vs_witness.py`)**: the 08-07 addendum's truncation ranges (12m 104,550–431,425; 25m 111,900–347,250) are **exactly** today's stale-witness scan endpoints for `s12/s25/fs12/fs25`. Run-by-run against `realized_training_sensitivity_20260807.json`: **27/27 addendum-"truncated" cells still read the identical below-total value today (same bytes), and 27/27 carry a completed `ckpt/<ts>-000000500000/done`.** The one alternative preserving the addendum — the 500000 ckpt being an earlier run's leftover admitted by the addendum's own `latest_ckpt()`+idempotent-skip hole — **predicts the ckpt PREDATES the frozen witness; it does not: 27 after / 0 before** (ckpt names read as UTC = conservative; min gap 0.99 h, median 4.97 h, max 59.05 h ⇒ fits kept checkpointing for hours after their progress files froze). Likely reading: **those 27 fits COMPLETED and the addendum measured a sync artifact, not walltime truncation** — in which case the "full-only" sensitivity re-ran the primary on an arbitrary subset and the C1/C3/C4 + figure-caption disclosure describes something that did not happen. **NOT checkable from ops, and the one way the addendum could still be right: whether each 500000 ckpt carries the capacity config its cell requires** — that is a read's job. Note the 08-07 STANDING RULE ("verify fit counters in every read") is the rule this defect attacks: against RCC's `OFFLINE_FIT_PROGRESS` it now **understates** completion, and the cells it drops are exactly the retried/moved ones (a selection effect, not noise) — 16 Aug

- **STALE-FIT-WITNESS FINDING TRIAGED 08-16 (ops commit 1816a7fd; artifacts/stale_fit_witness_20260816/)**: pull pass-2 clobbers progress witnesses with older instance copies (checksum-forced, no newest-copy logic; ops fixed) — 63 RCC fit dirs show counters below total, ALL 63 carry done ckpts at target, 0 genuinely short ⇒ **on RCC the fit witness now UNDERSTATES completion** (opposite direction from the 08-07 hole). ops repaired the 4 recoverable (b1prime fits 12/16→16/16, authentic producer bytes); 59 left untouched (producers destroyed; fabricating witnesses banned). **Executed-read triage: NO impact** — selection-effect audit clean: swave_read (only executed reader consuming OFFLINE_FIT_PROGRESS; swv/swr in the 63) is REFUSE-not-drop and executed cleanly 10 Aug ⇒ counters complete at its collate time, all-or-nothing semantics excludes selection; 10-Aug batch used ckpt-step witnesses (retroactively validated as the robust choice); realized-training stratification was sensitivity-only, primary unchanged both ways. **PENDING ADJUDICATION (the real tail): the 08-07 REALIZED_TRAINING truncation disclosure may be this same artifact** — 27/27 "truncated" capacity cells still read identical witness values AND carry done-at-500000 ckpts POSTDATING the witness (27 after / 0 before, median +5h; the foreign-leftover story predicts BEFORE) ⇒ truncation story near-dead; last escape = wrong-capacity foreign checkpoint. Decisive value-blind instrument BUILT + smoke-tested (capacity_ckpt_config_check.py: agent.pkl shape walk, deter 2048-vs-3072 discriminator, MATCH/MISMATCH/AMBIGUOUS + config.yaml cross-check) → [YOU/ops] run on RCC → [ME] adjudication: all-MATCH ⇒ disclosure RETRACTED-AS-SYNC-ARTIFACT (record + draft propagation; capacity waves were uniform — verdicts already invariant); any MISMATCH ⇒ disclosure re-scoped + arm-label investigation for those cells. Prospective reads insulated: no unexecuted frozen reader consumes OFFLINE_FIT_PROGRESS — 16 Aug

- **B1′ RIDGE PLATFORM DECISION 08-16 (`artifacts/b1prime_platform_decision_20260816.md`)**: ridge stage died at backend init on midway3-0278 — ops root-cause: `--platform` default `'gpu'` is an alias expanding to {cuda, rocm} in jaxlib 0.4.33 and the rocm member fatally calls the unexported `_xla.GpuAllocatorConfig` when the platform list is explicit (default path skips it; bare `import jax` fine; the one value no probe had tested). **Ruling: `--platform cuda` on the RCC ridge stage is NOT an amendment** — the prereg's registered per-fit form already carries the flag as a registered conditional (Vast-lane clause; its trigger condition now shown to hold on RCC), it selects the same cuda backend the anchor's successful 10-Aug default resolved to (backend selection, zero arithmetic surface), and the stage produced nothing (value-blind). Conditional on probe 53420572; disclosure chain = decision note → sidecar witness w/ actual command → read RECORD. **Open tail flagged: the env question** — the anchor ran the same default successfully 10 Aug, so if wheels changed since, same-node ≠ same-numerics; GATE = 3-anchor-cell re-measure on 0278/current-env vs pins (identical ⇒ proceed; different ⇒ narrow 32-anchor re-measure + pin-swap amendment, LeWM-pe pattern) — 16 Aug

- **[PAPERS 1–3 CHAT — ESCAPE HATCH CLOSED] CAPACITY-CONFIG CHECK RUN 08-16 (ops executed the instrument; adjudication + draft retraction remain the owning chat's)**: `capacity_ckpt_config_check.py` (value-blind shape/count walk over each disputed `agent.pkl`; no weights, no outcome quantities, no adapt scores) run on the RCC login node ⇒ `artifacts/stale_fit_witness_20260816/capacity_ckpt_config_check.json`. **67 dirs: 64 MATCH / 0 MISMATCH / 0 AMBIGUOUS / 3 NO-DONE-CKPT-AT-TOTAL** (the 3 = `s12q1s{0,1}_seed99` smoke + `speedtest_s12_inst6`, none carrying a witness or `total_updates` ⇒ outside the 64-cell panel). **ALL 27 disputed cells MATCH** — each 500000 ckpt's weight shapes are the capacity its cell name requires, other preset's `deter` absent. **Discriminator verified non-degenerate** (the positive control a uniformly-MATCH result needs): s12 n=32 → 32/32 contain 2048, 0/32 contain 3072; s25 n=32 → 32/32 contain 3072, 0/32 contain 2048. `config.yaml` agrees 64/64 (reported, not trusted — foreign-ckpt-under-authentic-config is the scenario under test; shapes are what rule it out). Param counts land on exactly 4 values in a **perfect 16/16/16/16 split** (s12 27,875,638 / 30,039,859; s25 62,456,758 / 67,177,267 = 2 sizes × 2 arms) — a foreign ckpt anywhere would show as an uneven count. ⇒ **the wrong-capacity-foreign-restore story is eliminated; the 27 fits ran to 500,000 updates at their required capacity, and the 08-07 addendum measured the sync artifact, not walltime truncation.** Pleasant inversion now live: capacity waves were UNIFORM after all — "22/32 full" caveat comes out of the drafts, full-only sensitivity becomes a footnote about a false stratification — 16 Aug

- **B1′ PLATFORM MECHANISM CONFIRMED 08-16 (ops; `artifacts/b1prime_platform_20260816/` RECORD + 5 producer logs; satisfies the condition in `b1prime_platform_decision_20260816.md`)**: probe 53420572 on midway3-0278 exercises the ridge probe's own call path (`jax.config jax_platforms=…` → `jax.devices()`) and separates the four values: **`gpu` FAILS** (`Unable to initialize backend 'rocm' … no attribute 'GpuAllocatorConfig'`), **`cuda` OK** `[CudaDevice(id=0)]`, **unset OK**, **`rocm` fails identically** — i.e. `'gpu'` expands to include rocm, whose init is fatal when the platform is requested explicitly. Chain: `ridge_probe.py:430` default `'gpu'` → `internal.py:34` → `agent.py:72`. **Ruled out**: the node (0278 V100 + 0284 rtx6000 both clean), the architecture (cc 7.0 ≡ 7.5), node-specific state (53411135 = byte-identical crash on 0279), and env drift (**all jax/jaxlib/cuda wheels 2026-05-09**, three months pre-anchor; only site-packages change since = `vastai` CLI + networking deps, 15 Aug, nothing numeric). **KEY INFERENCE FOR THE FLAG**: the anchor (53243601, 10 Aug, sacct all-4-tasks midway3-0278, logs `JAX devices (1): [cuda:0]`) succeeded on wheels identical to today's, and `jax_platforms='gpu'` is fatal on those wheels ⇒ **the anchor cannot have run with `'gpu'`; its effective platform was cuda-or-unset** ⇒ `--platform cuda` **reproduces** the anchor's backend and the bare default form (v1, 53411405, crashed 16/16 measuring nothing) is the one that would not have. The flag makes the panel MORE faithful to the pinned literals, not less — strengthening the "execution not amendment" ruling. Anchor-producer archaeology (`refit_ridge_panel.sbatch`, gone from repo, its log prints `ridge amended default ep_batch` ⇒ not a bare invocation; `internal.py:34` only fires on truthy platform, so an empty string leaves jax_platforms unset = sufficient but not singled out) **CLOSED BY USER DECISION 16 Aug as harmless + non-recurring** (every future ridge call passes `--platform cuda` explicitly) — 16 Aug

- **[GATE MUST RUN] ANCHOR USED A NON-DEFAULT `ep_batch` ON EVERY CELL 08-16 (ops; `artifacts/b1prime_platform_20260816/RECORD.md` §ep_batch)**: the anchor producer (`refit_ridge_panel.sbatch`, gone from repo) prints `ridge amended default ep_batch: <run>` **43× against 43 measured cells** — universal, not exceptional; the value is never logged and the script is gone ⇒ **unrecoverable**. This contradicts the assumption in the v1 producer header ("no --ep_batch; the anchor had neither") and in the registered per-fit form as previously read. `ep_batch` sets how many episodes go through the forward pass together, and under `compute_dtype: bfloat16` batching changes reduction order — the same amplifier behind the ≤0.113 cross-device ridge-AUROC shift ⇒ a plausible real source of pinned-vs-fresh difference that **cannot be matched by construction**. **CONSEQUENCE: the Phase-A anchor re-check is MORE necessary, not less.** Wheel-dating removed the software-drift rationale for the gate; this replaces it with a stronger one — the gate is now the ONLY empirical bridge between the fresh panel and the pinned literals. Phase A (job 53420696, 16 anchors → `$RUNROOT/b1p_anchor_recheck/`, our form: no --ep_batch + --platform cuda) resolves it: **match ⇒ ep_batch+platform inert, node/env terms zero, panel comparable as registered; differ ⇒ difference is COMMAND-FORM not hardware/env, and the narrow dated pin-swap amendment is the remedy with 16 of its cells already measured.** Do not consume the panel before this is adjudicated — 16 Aug

- **RESCUE-UNDER-LOAD READ 08-16 (`artifacts/rescue_load_read_20260816/`; bundle ruw_20260816_142429 3650 files sha-OK; freeze 9d8f50ec pre-wave; selfcheck PASS)**: verdict = **RESCUE-CONFIRMED** — paired primary +0.2144 [BCa +0.1389, +0.2639], perm p=.0078 (8/8 pairs positive, the exact-test floor at n=8), d_z 2.27; panel w1 0.7542 → w100 0.9686 (exceeds the registered FULL-rescue reference +0.16 and the unloaded 0.954 anchor). **Frozen consequence: s×w_r inertness = saturation-off-boundary; the loss-weight lever reaches the spectrum at the inclusion boundary; β experimentally manipulable ⇒ the competition mechanism now has causal confirmation on BOTH sides (σ-ladder g-side + rescue β-side).** All gates green (nzs2 manifest from the sha-verified sgl bundle copy; linkage 16/16 from the wave's runs.json cmd env — dv3ops-route conformance disclosed; counters 16/16; ckpts 16/16 @500000; rew both-directions config gates). **PROVENANCE DISCOVERY: w1-replication diff EXACTLY 0.0 — all 8 fresh w1 AUROCs bitwise-identical to the pinned SGL values despite fresh 16-Aug training (ckpt stamps + ridge run_logdir verified) ⇒ static-buffer offline fit + same-GPU ridge is END-TO-END BITWISE REPRODUCIBLE on the Vast stack (training included); zero instrument noise floor; strongest possible same-hardware replication of the σ-ladder task panel** — 16 Aug

- **THIRD-DOMAIN REACHER READ 08-16 (`artifacts/domains_reacher_read_20260816/`; bundle 13780 files sha-OK; freeze 7b51ad58)**: verdict = **P-D1 PRESENT** — within-collector interaction **+290.5 [BCa +160.2, +428.2], perm p=.00095, d_z 1.02, n=16**; cells task 48.2/336.6 vs apt 78.9/76.8 (de-confounded signature; level contrast +114.6; 14/16 seeds positive; occ gate 0.0/0.2795 passed). **The objective-conditioned interaction replicates in a THIRD domain — R1 Part-A P-D1 lands.** q1 manifest pulled from RCC via rcc-connect (bundle omitted it; disclosed) — 16 Aug

- **THIRD-DOMAIN WALKER READ ATTEMPT 08-16 = REFUSED AT FEASIBILITY GATE (ONE execution NOT consumed; `artifacts/domains_walker_read_20260816/`)**: side0 occ 0.0802 > frozen 0.05 bar (proxy-separation gate) — refusal fired BEFORE any outcome load; walker q1 buffer as built fails the registered decoupling spec (index-first gate should have blocked submission; ops chain gap; 64 fits+adapts spent on an infeasible buffer). **P-D2 UNADJUDICATED**; user decision = rebuild-to-spec vs carry-as-not-tested. Amendment 1 (PREREG_domains_walker_amend1_20260816, frozen pre-read) substituted ckpt-step witnesses for the TWO stale-witness cells (fq1s0 seed6/8: progress frozen at the identical second 08-14T20:47:40, done-ckpts @500000 postdating by 17–44h — the documented pull-clobber artifact; 10-Aug substitution precedent; authentic bytes preserved) — the user's mid-session note ("walker tasks with stale progress are actually done") confirmed-and-handled — 16 Aug

- **TM2-BRIDGE BUNDLE 08-16 = FITS-ONLY, READ NOT EXECUTABLE YET**: tm2_bridge_20260815_200027 (200 files sha-OK) carries stage fit 48/48 DONE (TM2_FIT_DONE + 500000/500000 progress, all three arms × 2 sides × 8 seeds) but ZERO adapt runs — the wave registered a single fit stage. The P-F2 primary is adaptation-AUC-based ⇒ read blocked on the adapt stage (48 adapts) + adaptation_auc collate + tm2b witness json + bundle — 16 Aug

- **LEWM READ HELD 08-16 (deliberate, value-blind)**: bundle lewm_20260816_121536 (124 files sha-OK) is read-ready EXCEPT the pe anchor: collates ship the HISTORICAL pe csv (sha matches the 9907d733… pin) — the 16-cell same-device pe re-measure (reinstated 16 Aug after the 1-cell devdiff priced the bundled drift at −0.099 nats) never ran. jp outcomes remain UNOBSERVED ⇒ holding the read keeps the anchor swap clean (null-side, direction-disclosed). NEXT: ops re-measures the remaining 15 pe cells on rtx6000 (1-cell machinery proven; probes already on rtx6000 per bundle NOTES; fpx panel already device-unified) → [ME] pin-swap amendment → ONE read. Bundle NOTES also archives the pe_e4_devdiff table — 16 Aug

- **B1′ PLATFORM CONFIRMED + GATE RETAINED 08-16 (ops; artifacts/b1prime_platform_20260816/, commits 6c005948/0952836f)**: probe 53420572 confirms the decision-note mechanism (gpu alias fatal via rocm's missing _xla.GpuAllocatorConfig; cuda + unset both → CudaDevice; node/arch/env ruled out — all jax wheels date 2026-05-09, predating the anchor) ⇒ `--platform cuda` is MORE faithful to the anchor (which cannot have used 'gpu' on these wheels). **NEW FACT: anchor used a non-default ep_batch, value unlogged, producer script gone ⇒ unmatchable ⇒ Phase-A gate retained**: job 53420696 (pinned 0278, PENDING) re-measures 16 anchors non-destructively → match ⇒ panel comparable as registered; differ ⇒ command-form difference ⇒ narrow pin-swap amendment (16 cells already exist) — 16 Aug

- **WALKER CURATION SCAN 08-16 (CPU-local on the registered index; `artifacts/walker_curation_scan_20260816/`)**: **compliance trivially achievable — the pool was never the problem.** 3430/7552 episodes at occ ≤ 0.05; unconstrained 200-episode side0 floor 0.0067 (7.4× under the bar; largest compliant N = 5877); high side headroom 0.336 top-200 vs built 0.1548. The built side0's 0.0802 (mid-strata mixture, 5 zero-bin episodes) = a SEARCH/CONSTRAINT artifact, not pool poverty ⇒ the second-refusal risk argument is dead; drop-vs-rebuild decision updated (rec now: side0-only rebuild as RCC background AFTER ops diagnoses why the search landed at 0.0802; side1 byte-identical ⇒ 32+32 only). Zero new compute (episodes.json pulled via rcc-connect) — 16 Aug

- **B1′ PHASE-A ANCHOR GATE = MATCH, 16/16 BITWISE 08-16 (ops; `artifacts/b1prime_platform_20260816/anchor_gate_result.md` + `anchor_recheck/` 16 jsons + 16 device sidecars)**: job 53420696 COMPLETED on midway3-0278 (V100, drv 535.216.03), `--platform cuda`, **Phase A ok=16/16, Phase B ok=16/16, exit 0** in 10m15s. Re-measured anchors vs pinned `ridge_finger_q1refit.csv` (job 53243601, 10 Aug, same node): **max |Δ| across auroc, r2 AND auroc_in_regime = 0.000e+00 — exactly zero, 16/16 bitwise**; collate's alpha is `alpha_0.001` and the pin reproduces at that alpha only. **Four terms discharged at once**: node (same 0278), env (wheel-dating confirmed empirically, not just by inventory), `--platform cuda` vs the anchor's unknown platform (backend selection INERT), and — the one the gate was kept for — **the anchor's unlogged/unrecoverable `ep_batch`: Phase A ran with NO `--ep_batch` and still reproduced the pins bitwise ⇒ inert for this instrument**. ⇒ **GATE VERDICT MATCH: ridge proceeds as registered; the narrow pin-swap amendment is NOT needed.** Phase B's 16 B1′ cells are measured (same allocation/GPU/flag) and **ops has not inspected their values** — 16 Aug

- **TM2-BRIDGE ADAPTS WERE ALREADY DONE; WAVE NOW 96/96 08-16 (ops)**: the other chat's "the adapt stage still has to be submitted" is superseded — **all 48 adapts exist on RCC complete (128 score rows + ADAPT_DONE each), produced 14 Aug**. The gap was the SPEC, not the compute: `tm2_bridge_fit2` was a fits-only transcription, so `check`/`bundle` could not see the adapts and the 15-Aug bundle shipped 48 fits with nothing to read. Adapt stage **transcribed from disk** (run_ids from the 48 dirs; cmd reconstructed from each run's audit `config.yaml` — pretrained/task/seed/steps 125000/eval_freq 16000/eval_episodes 16/seed_steps 5000/mpc true — cross-checked against `probing/tdmpc2_adapt.py`'s parser, where every value except pretrained/logdir/task/seed is the default; **NOT re-executed**, present so requeue is possible and the run table is one table). New kind `tm2_bridge_adapt` per_gpu 1. **`check` = fit 48/48 + adapt 48/48 = 96/96 DONE**; bundled `tm2_bridge_20260816_173625` (488 files) and pull-local VERIFIED. P-F2 primary now has its adapts; any collate instrument is the owning chat's — 16 Aug

- **LeWM pe PANEL RE-MEASURE SUBMITTED 08-16 (ops; job 53421265, `scripts/pe_e4_remeasure.sbatch`, `--constraint=rtx6000`)**: removes the pe-side drift term the held read would otherwise carry (1-cell devdiff priced it at **−0.0994 nats, −0.208%** on `rew_nll_in`; systematic and pe-side, so it shifts every paired [pe−jp] delta identically instead of cancelling). **ALL 16 cells, not the requested remaining 15**: one job = one GPU, and the instrument is bitwise-deterministic within a GPU but shifts across architectures, so 16-in-one-job gives a **device-uniform pe panel by construction**, where 15 + the earlier devdiff cell would leave a seam inside the amended panel (~2 min for the extra cell). Command is the step-9/devdiff form verbatim (same probeset, `--horizons 1 5 20`, `--ep_batch 4`); output to fresh side dir `pe_e4_remeasure/`, so **the registered `e4_fingerpx_v1_pe.csv` and its pinned sha 9907d733… are untouched** — the pin swap is the owning chat's dated amendment and nothing here presumes it — 16 Aug

- **B1′ WAVE COMPLETE + BUNDLED 08-16 (ops)**: `check --wave b1prime` = **32/32 DONE** (fit_adapt_task 8/8, fit_adapt_apt 8/8, ridge 16/16). Bundled `b1prime_20260816_174500` (3622 files, 32 run dirs = 16 fits + 16 adapts), pull-local VERIFIED against manifest. **Panel is device-uniform BY CONSTRUCTION and self-attesting**: all 16 `ridge_probe_finger_v1.json` carry a `.device.json` sidecar, and across the 16 there is exactly ONE host (`midway3-0278`), ONE gpu (`Tesla V100-PCIE-16GB`), ONE job (53420696) and `platform_flag: cuda` — the anchor's own node, the middle link of the platform decision note's disclosure chain, resolved without sacct archaeology. Ops has NOT inspected the ridge values — 16 Aug

- **pe PANEL RE-MEASURE COMPLETE + BUNDLED 08-16 (ops; job 53421265)**: **16/16 ok, 0 failed**, all on `midway3-0282` / ONE `Quadro RTX 6000` / one job ⇒ device-uniform pe panel as intended. Bundled `pe_e4_remeasure_20260816_174813` (49 files = 16 × {summary.json, summary.json.device.json, errors.npz} + producer log), rsync'd + `sha256sum -c` VERIFIED locally, manifest staged. **Ops deliberately did NOT compute the pe deltas**: unlike the 1-cell devdiff (scoped as a device probe), a 16-cell pe table against the pinned csv IS half the P-J1-rep primary — the comparison and the pin-swap amendment are the owning chat's. Registered `e4_fingerpx_v1_pe.csv` + sha 9907d733… untouched — 16 Aug

- **SUPERSEDED TM2 BUNDLE DELETED, WITH SALVAGE 08-16 (ops; `local_results/_superseded_provenance/tm2_bridge_20260815_200027/`)**: `tm2_bridge_20260815_200027` (197 files, 3.4 G) removed locally AND on RCC after `tm2_bridge_20260816_173625` (485 files) superseded it. **NOT a plain superset** — byte-comparison found 193/197 identical in the new bundle but **4 divergent**: `_cloud_logs/worker_shard{0..3}.out`, with **80 lines unique to old shard1 and 42 unique to old shard2** (old is a byte-prefix of new for shard0 only). Cause = **per-instance filename collision**: every instance writes its own `worker_shard0..3.out` and a pull flattens them into one runroot, so the last instance pulled wins; the 15-Aug bundle held a now-destroyed instance's queue-event history. Same shape as the `OFFLINE_FIT_PROGRESS` clobber (`artifacts/stale_fit_witness_20260816/`) — a per-instance file whose name does not carry the instance. Salvaged the 4 logs + the committed manifest, verified byte-identical, then a **pre-delete proof that all 197/197 old files are covered byte-identically somewhere (193 new + 4 salvage, 0 uncovered)** before removal. **SALVAGE SUBSEQUENTLY DELETED 16 Aug on user decision** (queue START/DONE event lines judged trivial and folder-cluttering) ⇒ those 80+42 unique event lines are GONE; the 193 science files remain byte-identical in `tm2_bridge_20260816_173625` and the old bundle's manifest `manifests/tm2_bridge_20260815_200027.sha256` is retained, so the deleted bundle stays attested even though the shard logs do not — 16 Aug

- **B1′ READ 08-16 (`artifacts/b1prime_read_20260816/`; bundle 3622 files sha-OK; anchor gate PASSED pre-read — 16/16 anchors bitwise-reproduced on 0278, max|Δ|=0, discharging node/env/platform/ep_batch in one comparison; panel 16/16 measured on the anchor's own node V100-0278 w/ --platform cuda + device sidecars)**: verdict = **DISSOCIATION-REPEATS** — task behavior FLOORS at 600k (AUC 101.07 < 113.682) while side-stratified legibility shows NO decline vs the pinned per-run anchors (side0 0.928 vs 0.903 ns; side1 0.989 vs 0.967 — the registered benign rise); apt CONTROL-OK; i600k descriptive ns. **Capacity-side convergence does NOT fire ⇒ per the frozen mapping the σ-ladder + B2 (+ rescue β-side) stands alone as the mechanism evidence; behavioral cliff bracketed (600k, 1m]; levers dissociate in erosion ORDER (capacity: behavior-first; noise: legibility-first) — descriptive, no ordering claim** — 16 Aug

- **TM2-BRIDGE READ 08-16 (`artifacts/tm2_bridge_read_20260816/`; fits + adapts bundles sha-OK; 48/48 witnesses)**: **P-F2 INDETERMINATE** — [free−rec] +16.0 [−8.1,+35.4] p=.175 (MDE80 33.9; closure 14.8%): decoder-parity reconstruction does NOT detectably re-open the family boundary; **P-F1 POSITIVE-NOT-RESOLVED** — aware−free +115.9* p=3e-05 d_z 1.50, ratio vs pinned anchor 1.069 [0.78, 1.45] (gap replicates full-size; attenuation unresolved); aware−rec +131.9*. The reward-aware objective carries the family's adaptation value — 16 Aug

- **LEWM READ 08-16 (`artifacts/lewm_read_20260816/`; Amendment 2 = same-device pe anchor swap, frozen pre-outcome, direction-disclosed conservative; full-panel device diff mean −0.172 sd 0.32 [−0.82,+0.26] — the hold + re-measure decision VINDICATED; fidelity 16/16 ≥0.5; selfcheck re-PASS post-pin-swap)**: verdict = **P-J1 PARTIAL (exactly one representation leg fires: emb)** — graft leg NO-SEPARATION (pe−jp −0.9 [−4.6,+3.3] ns; jp level 23.76 [21.9,25.9] AT the pixel anchor; inclusion NOT restored); **emb leg FIRES** +0.0297 [+0.008,+0.057] p=.032 (lewm 0.533 vs fpx 0.503 — small edge at near-chance levels); beh FLOOR-CONSISTENT (−15.4 ns). **Reward-free pixel null is objective-general at every consequential level tested (graft + behavior); predictive-latent objective retains an embedding-level vestige.** FB/successor conditional gets PARTIAL as input (neither branch fired clean) — user decision queued — 16 Aug

- **FB WAVE BUILT 08-16 pm (user GO; the predicted-ESCAPE registration; plan research_notes/paper1_wm_transfer/FB_Wave_Plan_20260816.md)**: reference implementation PINNED (facebookresearch/controllable_agent @ 5a9950b0…, cloned temp_files/fb_checkout; all integration claims verified against the checkout — FBDDPGAgent(**kwargs) direct construction, update(buffer, step), sample()'s unconditional physics indexing, infer_meta_from_obs_and_rewards, dmc.make states). Substrate: finger q1 proprio sides (stored env reward travels into the export — no relabeling needed; pinned 12-dim key order). 4 new instruments + frozen reader: probing/fb_export.py (selfcheck PASS local), probing/fb_fit.py + fb_zeroshot.py + fb_embed.py (torch-side selfchecks cluster-gated per TM2 precedent; fb_embed raw leg PASS local), analysis/fb_read.py (selfcheck PASS: 5 branch fixtures + 6 refusal legs incl. wrong-buffer linkage + mixed-device panel). PREREG_fb_20260817 drafted: P-FB1 zero-shot vs the house behavior-alive floor 113.682 (context pins rif 70.665 / apt 81.301 / scratch-AUC 147.682 from the randominit artifact — memory's "83.5±15.1" corrected), P-FB2 B-embeddings vs raw-proprio identity comparator (conservative bar, disclosed), directional side control, **both-directions clause: NO-ESCAPE = registered theory damage, never instrument-blame**; no graft leg by registration. ONE Opus reviewer (user pre-approved) TRIGGERED — freeze-commit only after findings applied — 16 Aug

- **[SPEC DEFECT FIXED] SE COMPLETION PREDICATE WAS UNABLE TO SEE TRUNCATION 08-16 (ops; `ops/waves/se_arms/spec.yaml`)**: instance-1 disk-full (overlay 50G at 100%, 3.6M free) killed all four D-only runs at **~50% (steps 248,240 / 258,480 / 258,640 / 259,632 of 5e5)** — and **all four carried `TRAINING_DONE`**. Cause: `scripts/uncfield_se.sbatch` does `TRAIN_RC=0; wait "$TRAIN_PID" || TRAIN_RC=$?; touch "$LOGDIR/TRAINING_DONE"` — the marker is touched **unconditionally, on the failure path too**. My spec's `jsonl_min_lines n: 100` would therefore have reported half-trained runs as DONE (they had 240–256 rows). **Corrected to n: 496 on all three stages** — the realized count, identical across all eight completed Stage-1 runs (`se_cheetah_seed10..17` = 496 rows each at 5e5). Textbook "markers lie; counters don't", caught before any read consumed it — 16 Aug

- **INSTANCE-1 DISK RECOVERY + SE RESUME 08-16 (ops)**: freed **27 G (100% → 48% used)** — 222 non-SE run dirs (**re-verified 222/222 covered on RCC with ≥ counters immediately before deletion**, deletion driven by that verified list with a guard refusing any `se_*` or path-bearing entry: 222 deleted, 0 refused), plus `/root/.cache/pip` (3.4 G, re-downloadable) and `/root/.cache/stable-pretraining` (6.9 G, the LeWM ViT training cache — deleted **only after confirming all 16 `lewm_finger_s*_seed*/lewm_weights.ckpt` are on RCC at the full 216,266,537 bytes**). **RESUME IS CLEAN AND VERIFIED**: `elements/checkpoint.py` writes `latest` only after a save completes, and `load_or_save()` loads from it, so the disk-full failure was benign — `latest` pointed at a `done` checkpoint in 4/4 while the mid-write partial (0-byte `agent.pkl`, no `done`) was unreferenced; partials removed, replay intact (608–624 chunks, ~290 M each), 40 M4 `ckpt_snapshots` per run intact. Resubmitted the identical lane commands; logs confirm **4/4 "Found existing checkpoint. Loading checkpoint: …"** at the pinned dirs ⇒ ~3.5 h × 4 lanes of compute preserved rather than restarted — 16 Aug

- **[BLOCKS TODO-132 STEP 4] WALKER side0=0.0802 ROOT-CAUSED 08-16 (ops; `artifacts/walker_curation_scan_20260816/DIAGNOSIS.md`; code+artifact reading only, zero compute, NOTHING SUBMITTED)**: two facts from `probing/gate0_compose.py` (the tool that wrote `axis1_walker/pairs.json`) fully explain it — (1) the acceptance test `if dcov <= cov_tol and docc >= occ_sep_min` (line 260; `gate0.py:189` identical) carries **NO absolute occupancy bound**, only coverage-*matching* + occupancy-*separation*; (2) among the 2715 accepted q1 candidates the selection is **argmax docc** (line 262), returning the widest gap with no preference for where it sits. Winner = side0 0.0802 / side1 0.1548, docc 0.0746 vs `occ_sep_min` 0.0250 (**3× more separation than required**). **The read's 0.05 absolute bar was never an input to the search** — not a misconfiguration but a spec gap between a curation step optimizing SEPARATION and a feasibility gate testing an absolute LEVEL. **A plain re-run is useless**: `default_rng(args.seed)` with the recorded `seed: 0` makes candidate generation deterministic ⇒ same 400 weight-sets, same 2715 pairs, same argmax winner, **bit-for-bit 0.0802**. ⇒ **TODO step (2) "re-curate targeting the low floor" is NOT a re-run but an edit to a registered instrument (absolute cap in the acceptance test, or a non-argmax-docc selection rule) — the owning chat's call, not ops'.** **The feasibility scan overstated the case**: `RECORD.md`'s "COMPLIANCE IS TRIVIALLY ACHIEVABLE" reads the MARGINAL occupancy distribution and omits the coverage-matching constraint, but occ and coverage are coupled in this pool — `random*` fam cov 1.121–1.131 / mean occ 0.034 / **0 episodes ≥ 0.1548** (family max 0.1079); `apt*` 1.214–1.280 / 0.077–0.090; `p2e*` 1.302–1.370 / 0.100–0.128; **Pearson r(source cov, source mean occ) = +0.522** over 18 sources, with `cov_tol` = 0.01246 ≈ 1% of the coverage level. Caveat stated in the record: search coverage is a set-level kNN quantity over the composed mixture, not a mean of source coverages, so this proves the constraint is LIVE and the scan omitted it — it does NOT prove a compliant pair is unreachable. **Cheap decider offered, deliberately NOT run by ops** (it is one line from being the selection-rule change itself): re-run `gate0_compose` enumerating every accepted pair's (side0 occ, side1 occ, docc, dcov) instead of the argmax — CPU, minutes, frozen index, side path. **32 fits + 32 adapts NOT SUBMITTED**: their input buffer does not exist, and building the registered fits on the CURRENT side0 would reproduce exactly the configuration the read already refused — 16 Aug

- **FB WAVE REVIEW APPLIED 08-16 late — FREEZE-READY** (ONE Opus reviewer: **NOT-FREEZE-READY, 7 BLOCKING / 14 MAJOR / 10 minor, ALL 31 APPLIED same session**; full inventory in PREREG_fb_20260817 §Review record): **B1 was the catastrophic one — the exporter omitted the dv3 post→prev action-convention shift** (reviewer empirically confirmed on a real chunk: corr(action, Δv_next) 0.78 vs 0.01 for the wrong pairing; the tdmpc2_bridge precedent existed in-repo) — FB would have trained F(s,a,z) on actions applied one step late, zero-shot would fail for an integration reason, and the wave would have recorded FAKE THEORY DAMAGE. Also: B2 fb_fit infinite loop (update() returns {} without use_tb — now a declared sole deviation + structural counting + real NaN guard), B3 1-D reward crash in infer_meta, B4 wrong probeset filename (now via load_e4 w/ FROZEN+sha gates), B5 MDE constant wrong 2.59× on the flagship, B6 damage-on-indeterminate (NO-CALL-UNDERPOWERED branch added; damage requires determinate flagship negative), B7 no harness positive control (in-protocol random-floor control added as fail-closed gate, band [35,110] — also retires the action_repeat caveat). Majors: discount 0.99, full-kwargs freeze + byte-identity gate, device-map exact-keyset + raw-cpu exemption, probeset-identity gate, updates cross-checks + deterministic FILL rule, zeroshot field gates, z-health + auditable side premise, heldout guard, near-ceiling raw disclosure (raw ridge ≈0.95 on a sibling probeset — P-FB2 null weakly informative, flagship weight on P-FB1), escape-hatch ledger + training-health witness, MUJOCO_GL=disabled, env-order guard. Post-fix selfchecks: fb_export PASS (5 refusals), fb_read PASS (7 fixtures + 11 refusals + literal pins), fb_embed raw PASS local; torch-side selfchecks = registered cluster smoke gates. NEXT: [YOU] freeze-commit → cluster chain — 16 Aug

- **FB WAVE VERIFICATION REVIEW APPLIED 08-16 night — FREEZE-READY (2nd pass)** (ONE Opus verification reviewer on the post-fix state: **NOT-FREEZE-READY, 3 BLOCKING / 9 MAJOR / 14 minor — ALL APPLIED**; full inventory in PREREG_fb_20260817 §Review record — second pass): the pass caught exactly the fix-introduced class it was sent for — **B-A the new B7 random-floor control CRASHED on invocation** (never imported the checkout; also moved into the smoke stage so an out-of-band result halts with zero fits spent); **B-B the registered tdmpc2 env (py3.11) provably cannot import the pinned checkout** (dataclass mutable-default rejected ≥3.11, reproduced) ⇒ dedicated py3.10 env `scripts/fb_env_setup.sh`, checkout unpatched; **B-C the device-map gate was vacuous** ('cuda' strings; no producer) ⇒ fb_embed records physical gpu_name, producer + semantics registered. Two frozen-text contradictions resolved by pre-outcome design decision: M-5 determinate-negative-flagship + P-FB2-fire now carries the behavioral damage half; M-6 P-FB1-only fire = BEHAVIORAL-ESCAPE (qualified) — the LIKELY fire path given the near-ceiling raw comparator. Also: fit-seed linkage, training-health witness bundled+read w/ --health_band, deterministic smoke rule (20000 updates, projected wall-clock), floor-band derivation registered, zeroshot↔emb ckpt-sha byte identity, n_infer/episode-length gates. It also VERIFIED all 31 first-review fixes correct (B1 action-shift direction walked index-by-index end-to-end incl. reward non-shift; B2 counting exact incl. step 0; all refusal legs reach their intended gates; constants sweep byte-exact). Selfchecks: fb_export PASS, fb_read PASS (8 fixtures + 14 refusals), fb_embed raw PASS; torch-side = cluster smoke gates. NEXT: [YOU] freeze-commit (git add now also includes scripts/fb_env_setup.sh) — 16 Aug

- **[SUPERSEDES THE ROW ABOVE — 0.05 IS UNREACHABLE, NOT MERELY UN-SELECTED] WALKER Q1 ENUMERATION 08-16 (ops; `artifacts/walker_curation_scan_20260816/DIAGNOSIS.md` + `enumerate_q1_pairs.py` + `q1_pairs_enumerated.json`; read-only, RCC login node, nothing rebuilt/submitted)**: **ATTRIBUTION CORRECTION FIRST** — `pairs.json` is written by **`probing/build_controlled_replay.py`** (acceptance line 359, argmax-docc selection line 361), NOT `gate0_compose.py` as the prior row said; the mechanism was right, the file/line were wrong. **ENUMERATION**: imports `build_controlled_replay` and calls its own `_candidate_pool`/`_overlap` (nothing edited/monkeypatched; acceptance copy-identical to `cmd_search`; keeps all accepted pairs instead of the argmax; selection UNCHANGED, argmax recomputed only as self-check). **Self-check REPRODUCED bit-for-bit** (2715 pairs, side0 0.080200, side1 0.154755, docc 0.074555 — script exits 2 without reporting otherwise). **RESULT: low-side occupancy over all 2715 accepted pairs = min 0.05318 / p5 0.08020 / median 0.09532 / max 0.13310; pairs with low side ≤ 0.05 = ZERO.** ⇒ **the prior row's framing is superseded: no selection-rule change can rescue this pool** — even a perfect argmin-low rule returns 0.05318, so the failure is not how the winner is CHOSEN but that no compliant pair EXISTS. Composition + cov-matching + separation + overlap raise the reachable floor **~8× above the scan's raw-episode floor (0.0067 → 0.05318)**, which is the quantitative form of the occ/coverage coupling (r=+0.522; `random` fam max occ 0.1079, zero episodes at side1's level). **NOT settled**: 0.05318 is the floor of THIS sampled pool (400 Dirichlet(0.3) weight-sets, seed 0, 388 feasible) — **the miss is only +0.32 pp (1.064×)**, so a different `--seed` / larger `--n_candidates` / less concentrated `--dirichlet` could plausibly clear it; that is a re-run with different SAMPLING knobs, touching no criteria (whether re-sampling until the bar is met is selection-on-the-gate is a REGISTRATION question, not ops'). Also newly noted: `build_controlled_replay` already ships `search-dose` (computes the Q1-feasible occupancy span) and `search-rpair` (`--target_docc`, non-argmax, pairs.json-shaped) ⇒ non-argmax selection was always a registered capability, it just cannot help this pool. **Caution for the decision**: the lowest-low pair (lo 0.05318 / hi 0.07859) carries docc **0.02541**, barely over the 0.0250 floor and ~⅓ of the built pair's 0.0746 — clearing the gate and being worth running are different questions. Options (owning chat's): (1) re-sample pool, (2) relax `cov_match_frac`/`max_overlap` = amendment weakening the matched-pair control, (3) carry P-D2 not-tested with reacher alone. **32 fits + 32 adapts REMAIN UNSUBMITTED** — 16 Aug

- **WALKER P-D2 DECIDED 08-16 night: CARRIED AS MEASURED-INFEASIBLE-AT-SPEC — no rebuild** (ops DIAGNOSIS in artifacts/walker_curation_scan_20260816/: root cause = the registered search optimizes SEPARATION with no absolute occupancy bound — the read's 0.05 bar was never a search input (spec gap, not a bug); constrained enumeration bit-reproduces the frozen pairs.json (2715 pairs) and shows the reachable low-side floor is **0.05318 — zero compliant pairs**, because occupancy⟂coverage FAILS in this pool (r=+0.522; the low-occ random family is the lowest-coverage material, so coverage matching drags side0 up ~8× off the marginal floor). **The scan's "trivially achievable" conclusion is SUPERSEDED (marginal-vs-constrained error — annotated in its RECORD).** Rescue routes rejected: pool re-sampling = selection-on-the-gate AND best-case docc 0.0254 (⅓ of built, ~11× under reacher's 0.279 dose ⇒ near-certain uninterpretable null); criterion relaxation re-admits the coverage confound the design kills. Paper disclosure UPGRADES: walker stopped by its own registered gate pre-outcome + archived proof no compliant pair exists in the pool + the domain-structural coupling as a constructibility observation; reacher alone = the third-domain leg. Walker chain CLOSED (steps 2/4 cancelled; 64 spent runs stay archived) — 16 Aug

- **[BLOCKS THE FB WAVE] FB HALTED AT THE REGISTERED SMOKE GATE 08-16 (ops, instance 12; `artifacts/fb_smoke_20260816/RECORD.md` + `fb_zeroshot_selfcheck_FAIL.log`)**: PREREG_fb_20260817's ops chain requires all three torch-side selfchecks to PASS cluster-side "before any fit". **`fb_env_setup.sh` OK** (pinned checkout `5a9950b0…`, py3.10, `torch 2.13.0+cu130 cuda True`, cuda forward OK on 5060 Ti); **`fb_fit --selfcheck` PASS**, **`fb_embed --selfcheck` PASS**, **`fb_zeroshot --selfcheck` FAIL**: `RuntimeError: Can't call numpy() on Tensor that requires grad` at `probing/fb_zeroshot.py:75` → `fb_ddpg.py:281 return action.cpu().numpy()[0]`. **DIAGNOSIS = ours, not the env's**: upstream's `act()` never detaches and relies on the caller; **every** upstream call site wraps it `with torch.no_grad(), utils.eval_mode(self.agent):` (pretrain.py:353/396/628, anytrain.py:91, train_online.py:86, play_behaviors.py:134 — 6/6), while our `rollout()` calls it bare. **TWO gaps, only the first errors**: (1) missing `torch.no_grad()` = the hard failure; (2) missing `utils.eval_mode(agent)` = **SILENT** — `utils.py:34` does `model.train(False)`, whereas our `eval_mode=True` arg is `act()`'s action-selection flag (`dist.mean` vs `dist.sample()`) and does NOT touch module training state, so fixing only (1) leaves (2) unable to announce itself. Materiality checked here: grep for BatchNorm/Dropout/LayerNorm in the pinned `fb_ddpg.py` returns **nothing** ⇒ (2) very likely numerically inert on this checkout (a grep, not a design guarantee); `AGENT_KWARGS additional_metric=False` so the eval branch's extra forwards don't apply. **OPS STOPPED**: the fix edits `probing/fb_zeroshot.py`, frozen with the prereg alongside `analysis/fb_read.py`, and that module produces BOTH the flagship P-FB1 zero-shot leg AND the `random_floor` control whose [35,110] band gates the wave ⇒ registered-instrument decision, Papers-1–3 chat's. Suggested fix = copy upstream verbatim (`with torch.no_grad(), ca_utils.eval_mode(agent):`), which closes both gaps without inventing a variant; re-validation is minutes since the env is built and 2/3 selfchecks pass. **NOT DONE, deliberately**: export ×2, random_floor, seed99 smoke fit + the `updates` FILL, 16 fits, and env install on instances 1/5/7 (the user's "if that works" precondition) — instance 12 idle with env ready, zero runroot writes — 16 Aug

- **FB WAVE SMOKE-GATE HALT + AMENDMENT 1 08-16 night (`artifacts/fb_smoke_20260816/` + `prereg/PREREG_fb_amend1_20260816.md`)**: the registered cluster smoke gate FIRED as designed — env setup OK (py3.10, pinned checkout imports, CUDA forward OK on 5060 Ti), fb_fit + fb_embed selfchecks PASS, **fb_zeroshot selfcheck FAIL**: upstream `act()` never detaches (relies on the caller; all six upstream call sites wrap in `torch.no_grad()`) and our rollout called it bare → "Can't call numpy() on Tensor that requires grad". ZERO exports/fits/estimands spent — the halt is the gate working. **Amendment 1**: `torch.no_grad()` wrapper on the act() call (calling-convention parity with upstream; eval mode already permanent via load_agent; no numerics change — no_grad disables graph construction only). Reader + prereg body untouched; the smoke sequence remains the gate (fb_zeroshot selfcheck must PASS cluster-side post-fix before the chain proceeds). NOTE: the two review passes checked act()'s signature and interface but neither traced upstream's grad-context calling convention — a third portable checkout-integration lesson (match the caller's context managers, not just the signature) — 16 Aug

- **FB FILLS PINNED BY DATED EDIT 08-16 night (PREREG_fb_20260817, both FILL slots; BEFORE any non-smoke fit — ops held the 16-fit submit for exactly this ordering)**: post-Amendment-1 chain progressed cluster-side (fb_zeroshot selfcheck PASS implied by preflight PASS; exports + random_floor + seed99 smoke done, disk 19%). **Updates FILL = 500000** by the registered deterministic rule: smoke 20000 updates in 652 s (rc=0; process wall 658 s) ⇒ projection 652×25 = 16300 s = 4.53 h (658 s → 4.57 h), ≤ 8 h ⇒ 500000; both wall measures give the same branch — zero judgement content. **Health-band FILL = [-455.1604080200195, -4.551604080200195]** from the smoke's fb_loss_tail_median = -45.51604080200195 (n_tail 100); band FORM chosen at the edit pre-outcome (the slot registered the anchor, not the width): symmetric ×10 log-magnitude band [10·m, m/10] — the witness catches scale anomalies (divergence/no-training) and must tolerate the registered 20000→500000 extrapolation, so order-of-magnitude is the honest width; reader arg `--health_band -455.1604080200195 -4.551604080200195`. Smoke dir fbwm_smoke_seed99/ removal (registered BY HAND step) authorized ONLY after archiving its fb_metrics.json (+ config) into artifacts/fb_smoke_20260816/ — the file is now the provenance of a registered pin (never-delete-without-archive rule) — 16 Aug

- **FB WAVE: SMOKE CHAIN CLEARED + 16 FITS SUBMITTED 08-16 (ops, instance 12; wave `ops/waves/fb_fits`)**: after Amendment 1 (`PREREG_fb_amend1_20260816`, `torch.no_grad()` on the `act()` call) the registered cluster gate passes — **`fb_fit` / `fb_zeroshot` / `fb_embed` selfchecks 3/3 PASS**; amendment's load-bearing claim independently verified by ops (`fb_fit.load_agent` does `net.eval()` on forward_net/backward_net/actor, so the second, SILENT gap flagged in `artifacts/fb_smoke_20260816/` — missing `utils.eval_mode` — is genuinely covered permanently, not merely inside a context manager). Chain: donors `axis1_finger/q1` synced (404 files / 411 MB incl. `manifest.json`) → **export ×2 OK** (200 eps × 1001 steps each, both manifests) → **random_floor (seed 0) mean_return 79.4, INSIDE the registered [35,110] band ⇒ no halt** → **timed seed99 smoke: 20000 updates in 652 s** (process wall 658 s, rc=0). **DETERMINISTIC FILL APPLIED**: 652 × (500000/20000) = 16300 s = **4.53 h ≤ 8 h ⇒ updates := 500000**; the 658 s measure gives 4.57 h, the SAME branch, so the input ambiguity never reaches the outcome. Health-band input handed over: `fb_loss_tail_median = -45.51604080200195` (n_tail 100). **Ops HELD submission until the user's dated prereg edit landed** (prereg: FILL "recorded by dated edit before any non-smoke fit starts") — commit `b280b57e` records updates=500000 + band [-455.1604080200195, -4.551604080200195]; only then submitted. **16 fits (sides 0/1 × seeds 1–8, run_ids verbatim from the prereg) at `per_gpu: 2`** (user decision from the solo smoke's ~500 MB VRAM / <50% util) — lanes pair side0+side1 of the SAME seed, so contention is symmetric across the side contrast. Live: 8 concurrent, 1035 MiB/card, **97–98% GPU util**. **Measured co-residency cost (provisional, first 2 min): ~2000 updates/2 min ⇒ ~8.3 h/fit vs the 4.53 h solo projection** — the card SATURATES at 2 fits, so 2×8.3 h ≈ 16.6 h beats 4×4.5 h = 18 h by only ~8%, not the ~2× the <50%-util premise implied; flagged to the user, their call. Completion gated on the COUNTER (`update=500000` AND `total_updates=500000`), though `fb_fit` writes `FB_FIT_DONE` only on the success path (no trap) unlike `uncfield_se.sbatch`. `fbwm_smoke_seed99/` LEFT IN PLACE (prereg: remove by hand) — 16 Aug

- **SE STAGE-2 + D-ONLY: ALL COMPUTE COMPLETE, INSTANCES RELEASED 08-16/17 (ops)**: 12/12 SE arms complete and archived; instances 1, 5, 7 pulled → counter-verified → destroyed (fleet now instance 12 only, FB fits). Coverage at destroy: **i5 125/125, i1 4/4, i7 86/86 dirs on RCC with ≥ counters**, plus **replay chunk parity checked run-by-run** (i5 1424/1424 ×4; i7 1376/1376/1360/1376 — the M3 occupancy input verified whole, not merely present). Pull volume ~37 GB total because the generated filter is `/<run_id>/***` (whole run dir incl. every ckpt), far above the prereg's narrower bundle spec — a scratch-headroom note, not a defect. **D-only `se_probe` passes EXECUTED (job 53424445, caslake CPU, `scripts/se_donly_probe.sbatch`): 4/4 ok**, each writing `se_probe.json` + `se_probe_dims.npz` per the amendment's bundle spec, all parsing, uniform 15-key structure, provenance `n_eval=512 seed=0` identical across the four. **caslake not gpu because `se_probe` defaults to `--platform cpu` ⇒ the D-only-on-rented-5060Ti vs Stage-1-on-Midway3 device question is moot (both sides CPU, device-independent by construction)**. Caveat recorded for the reader: the probe json echoes only `n_eval`/`seed` from the knob set — `rollouts`/`n_perm`/`horizon`/`topk`/`ep_batch`/`platform` are left at parser defaults by construction (the sbatch passes ONLY `--run_logdir`) but are NOT attested in the artifact, so a knob-equality gate against Stage-1 can check two of them and must trust the committed invocation for the rest. Ops did NOT inspect any probe value. **Stage-2 needs no compute at all** (M3 occupancy = deterministic CPU scan over `replay/`; `se_probe/` explicitly optional there, no registered attribution read) ⇒ both SE bundles now have every registered input in place — 16/17 Aug

- **SE COMPLETION PREDICATE CORRECTED TWICE 08-16 (ops; `ops/waves/se_arms/spec.yaml`)**: the post-disk-full `n: 496` bound was itself wrong — right instinct (counter over marker), WRONG counter. `scores.jsonl` is written in **bursts of 16 rows every ~16001 steps**, so 496 = 31 bursts and the burst count depends on where the stop step lands relative to an eval boundary. `se_donly_s10` resumed from an earlier checkpoint after the disk-full, ended at agent step **497744**, missed its 31st boundary ⇒ **480 rows (30 bursts) while FULLY TRAINED** (clean rc=0 on stop-file, all five milestone snapshots, scores monotonic with zero duplicates and NO interior gaps — the disk-full window shows no hole). **Replaced by the STEP**: the 500000-milestone snapshot must exist at realized step ≥ 490000. Validated across all 12 SE runs: realized **494240..499552**, with **s10 at 494704 MID-PACK (se_gate_s23 is LOWER at 494240)** while the disk-full truncation sat near 250000 ⇒ clean separation. Row count demoted to a loose sanity floor (464 = 29 bursts). Wave now **12/12 DONE with s10 surfaced by `n_ep_vs_modal` as a disclosed outlier** (n_scores 480 vs modal 496) — reported, not refused, per the 08-08 standing rule; strictly better than silently passing or falsely failing it — 16 Aug

- **SE AMENDMENT-3 GRADIENT-ARM BUNDLE BUILT + SYNCED 08-17 (ops; `uncfield_se_grad_20260817_101139`, manifest staged)**: **15,298 files / 7.2 G**, `sha256sum -c` VERIFIED on RCC (15298/15298) and again post-sync locally (15298/15298). **Built ON RCC from the full pulled runroot** — the FB wave had been bundled instance-side and that inversion is now a standing rule (`scripts/ops/CLAUDE.md`): a read bundle is a SUBSET, so bundling on the instance and shipping only the bundle strands everything outside the spec while every check still reads green. **Contents per §5**: all 12 runs carry `replay/` (1200–1376 chunks), `scores.jsonl`, `config.yaml`, `metrics.jsonl`, `ckpt/latest` (+DIR_LISTING); hetero ×4 and flat ×4 also carry `se_probe/` (8/8), **ungated2 verified 0/4 probes** (over-bundling would be a silent spec violation). Excluded: `ckpt_snapshots/` payload (~3.9 G/run), `plugins/`, `scope/`, all ckpts beyond latest — none in §5; kept per-run `ckpt_snapshots_nearest.json`, realized steps **493792–499792**, all clear of the 490000 bar. **PROBES: `se_grad_probe` job 53431914 COMPLETED 00:09:53 exit 0:0, ok=8 skipped=0 failed=0** — all 8 in ONE caslake CPU job, which is what satisfies §4's 'all probe passes on one device class' (se_probe defaults `--platform cpu`, frozen defaults, default output). All 4 HETERO probes valid ⇒ the §4 manipulation-check gate has its full complement (fewer than 3 valid = outcome cell iv, instrument failure not finding). **EXECUTION PROVENANCE THE READ MUST DISCLOSE: the 12 runs did NOT share one machine.** hetero+flat were split 2+2 / 1+1 / 1+1 across instances 13/16/17 so every machine carried EQUAL hetero and flat ⇒ device is a BALANCED nuisance factor inside the registered primary (hetero−flat, C(8,4)=70), not confounded with it; the 4 ungated2 ran on instances 14/15 ⇒ the two dose secondaries (hetero−ungated2, flat−ungated2) are CROSS-INSTANCE. All RTX 5060 Ti. Recorded in the bundle's own `BUNDLE_NOTE.md`. Between-machine variance now sits inside a low-powered 4-vs-4 test — a power cost, not a bias. All 5 instances destroyed after file-count + FILE-byte parity per run (`du -sb` on a tree compares filesystems, not data — it flagged every run spuriously) — 17 Aug

- **SE AMENDMENT-2 READ BUNDLE BUILT + SYNCED 08-17 (ops; `uncfield_se_arms_20260816_224710`, manifest committed)**: 11,290 files / **4.8 G**, rsync'd and `sha256sum -c` VERIFIED locally (11290/11290). **Built to the §1/§2 bundle specs, NOT by whole-dir copy**: `dv3ops bundle` would have shipped **51 G**, of which **~47 G is `ckpt_snapshots/` (3.9 G × 12) that NEITHER spec lists** — snapshots are the Stage-1 M4 input and the amendment records those probes as already executed. Excluded also `plugins/` (44 M/run) and `scope/` (20 M/run), unlisted in both specs; full ckpt history replaced by `ckpt/latest` + a `ckpt/DIR_LISTING.txt`. **Nothing deleted — snapshots remain on RCC scratch** if a later amendment wants a snapshot leg for these arms (rationale recorded in the bundle's own `BUNDLE_NOTE.md`). Kept from the snapshot tree: per-run `ckpt_snapshots_nearest.json`, the realized-step witness the corrected predicate asserts. **Contents verified post-sync**: 12 run dirs; D-only 4/4 with `se_probe.json`+`se_probe_dims.npz`; Stage-2 8/8 with replay **1424/1424/1376/1360/1424/1424/1376/1376** chunks (M3 occupancy input); 0 missing among the required per-run `config.yaml`/`metrics.jsonl`/`scores.jsonl`/`ckpt/latest`. Builder asserts counts and REFUSES pre-copy (a D-only arm silently short one se_probe, or a Stage-2 replay under 1000 chunks, is exactly what a read cannot see). COMPLETION.txt carries **12/12 DONE, 1 outlier** (s10 n_scores 480 vs modal 496, disclosed) — 17 Aug

- **SE ARM READS 08-16 (`artifacts/se_donly_read_20260816/` + `artifacts/se_m3_read_20260816/`; bundle uncfield_se_arms_20260816_224710, 11290/11290 sha OK, 12/12 DONE on Midway3 via se_arms wave)**: **(1) D-ONLY = NO-INTERFERENCE** (dup0 θ₁ 1.0001 [0.9991,1.0006] with N absent — indistinguishable from Stage-1's 1.0000 with N present; 0/4 indicators all channels; dup2 depression 0.982 replicates N-free ⇒ Stage-1 D-null stands UNQUALIFIED — no redundancy farming with or without stochastic competition; ckpt gate discharged manually vs shipped latest 4/4). **(2) M3 = NO DIVERSION, registered primary REFUTED AT THE TEST FLOOR** (Δ −0.0791 one-sided p=1.0000; score secondary null p=.37; descriptive COMPLETE REVERSAL — every gated run 0.467–0.493 below every ungated 0.521–0.625, unique minimum of 70 assignments, post-hoc-labeled). **THE SE DISSOCIATION CHAIN IS COMPLETE ACROSS ALL THREE LEVELS: objective massively+persistently misprices (5.3×, 8/8, formed-by-25%-and-flat) → planner ranking does not chase (P-SE2 negative) → behavior does not divert (M3 null-with-reversal)** — the naive noisy-TV behavioral prediction fails at scale while the accounting pathology is enormous; reversal mechanism unresolved (state-independent OU pedestal = level-not-gradient; flagged for design review before any follow-up). **ALL REGISTERED SE COMPUTE + READS COMPLETE** (Stage-1, M4, D-only, Stage-2/M3; reads via frozen reviewed readers ×4, yield 23/23 across #18–23). [YOU] commit ride-along: `git add artifacts/se_donly_read_20260816 artifacts/se_m3_read_20260816 manifests/uncfield_se_arms_20260816_224710.sha256 STUDY_LEDGER.md` — 16 Aug

- **SE GRADIENT ARM REGISTERED 08-16/17 (`PREREG_nfi_scale_exhibit_amend3_20260816` + `uncfield/se_grad_read.py`; user GO for the level-vs-gradient experiment; ICLR-version leg, read cutoff ~5 Sep)**: tests the M3 pedestal hypothesis with a followable amplitude ramp (distractor emission × m=clip((position[0]+0.32203162)/0.51914957,0,1); no boundary discontinuity). **Review #24 (ONE Fable, 1B/4M/9m ALL adopted): B1 = my contrast arithmetic was WRONG (registered 3.3×/11× matched neither measure; corrected to smoke-visitation-weighted 1.857× amplitude / 3.195× variance, reviewer-recomputed bit-exact); M2 dose confound (hetero also cuts mean amplitude ~2.8×) resolved the STRONG way — third arm added: flat amplitude-matched comparator `--distractor.scale 0.35891393` (= smoke mean m̄; constant-m-via-scale exact; incentive-immune), PRIMARY switched to hetero−flat one-sided exact C(8,4)=70 p≤.05, ungated2 (32–35) demoted to dose reference; M3 per-arm dim/scale/basesd identity pins; M4/m14 cell-iv honesty (removal wording needs ≥3 valid probes); M5 probe_ckpt_flag surfaced.** Delta verification pass: ALL 14 CONFIRMED-FIXED (every pin independently recomputed: median split 7464/7464 exact) + 1 NEW BLOCKING caught = launcher hardcoded scale 1.0 ⇒ flat arm inexpressible → DISTRACTOR_SCALE override (default 1.0, Stage-1-inert) + amendment sentence; all artifacts FREEZE-READY. Wrapper mod extension selfcheck exact over 3000 steps; se_grad_read 8 scenario groups PASS. Manipulation-check gate = distractor p<.05 in ≥3/4 valid hetero probes (validity gate on an 8/8-at-floor Stage-1 quantity, disclosed anti-conservative). Cells: i gradient-following (M3 null = channel-geometry) / ii dissociation-robust AT the registered dose / iii modulation-removed-misprice / iv validity. Reviews #18–24 + verification: yield 24/24. **[YOU] freeze commit + 12 submissions (hetero 28–31 / flat 36–39 / ungated2 32–35, specs in TODO)** — 17 Aug

- **FB READ 08-17 = BEHAVIORAL-ESCAPE (qualified) — THE PREDICTED ESCAPE LANDS (`artifacts/fb_read_20260817/`; registered ONE execution vs sha-verified bundle fb_fits_20260817_141845, 101/101)**: **P-FB1 ZERO-SHOT-ESCAPE** — mean return 179.79 vs house floor 113.682, excess **+66.11 [+21.04,+125.80] perm p=.0267 d_z 0.597 n=16**; random-floor control 79.4 ∈ [35,110] (fail-closed gate PASSES); FB sits 2.2–2.5× above the reconstruction-era reward-free pins (rif 70.7/apt 81.3). **First tested objective family to escape the reward-free null** — exactly the inclusion criterion's prediction (occupancy-factorized objective ⇒ reward readable by construction). Side-stratified (non-decisional): **carried by side1** (+128.6* p=.039; side0 +3.6 ns AT floor; side control +125.0* CONTROL-OK) — support-dependence extends to the escaping family, mechanism-coherent, NOT claimed. **P-FB2 B-LESS-LEGIBLE**: fb AUROC 0.795 vs raw 0.903, −0.108* p=3e-05 — B is LESS linearly legible than raw proprio (near-ceiling comparator, registered weakly informative; blocks only the bidirectional ESCAPE-CONFIRMED wording; behavior reads reward through F·z value structure, not linear decodability of B). **DISCLOSED: health witness uninformative as pinned** — all 16 tail-medians out-of-band (side0 −626…−1644 + seed8 SIGN-FLIPPED +1002; side1 −2826…−3970); my ×10 band under-anticipated the 20000→500000 loss-scale growth (~30–90×); witness's only registered role is the undertraining caveat AGAINST a NO-ESCAPE ⇒ moot for a fire (had it gone negative, the caveat would have been admissible — the fire direction is the one the witness cannot weaken); s0_seed8 outcomes unremarkable, descriptive anomaly only. All other gates PASS (updates 500000 ×16, kwargs byte-identical, sha linkages, single-GPU panel 5060 Ti, z_norm ≡ √50, side premise frac_pos 8/8). **Paper consequence: the 17-Jul family-scope limitation gets its positive complement — the boundary is not "non-reconstruction fails to inform" but exactly where the objective makes reward legible; headline claim "support must be legible to the objective" now has its predicted POSITIVE on the objective side** — 17 Aug
