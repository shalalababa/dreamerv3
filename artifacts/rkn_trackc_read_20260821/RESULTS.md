# Track-C RKN registered adjudication — 21 Aug 2026

**Prereg:** `PREREG_trackC_rkn_20260821.md` (review #33 applied,
frozen 554864ca). ONE adjudication pass over the job's emitted jsons
(manifest 12/12 OK; jsons copied here verbatim). Seed-0 (m0) rows
carry the registered PRE-COMPUTED label (review #33 executed the
recipe pre-freeze). **Pre-computation nuance, disclosed:** the
review pilot trained at batch=64 (pre-#33-F10); the frozen recipe
ran batch=32 — m0's categorical outcomes match the disclosure
exactly (CIG-ONLY, pbim=0, both=0) while the exploit count differs
(293 vs the review's 332).

## Adjudication vs §3

- **P-RKN2′ (primary): FIRES.** 4/4 members: n_exploit_pbim == 0
  AND n_exploit_both == 0; every verdict EXPLOIT-SURVIVES-CIG-ONLY.
  The coherent operator never reaches the realized-ΔH tier or the
  flagship conjunction — the periodic-covariance argument holds in
  all four trained members. Registered descriptive: n_exploit_cig =
  293 in ALL FOUR members — exactly the review's q̂-ladder count at
  q̂_static ≈ 2.5e-3 (the adam-budget bound), a clean confirmation
  of the optimizer-budget account of the coherent-liar cell.
  naive ≡ carried asserted true ×4 (the structural degeneracy).
- **P-RKN1 (consistency): HOLDS.** Across all 4×313 consistency
  rows (293 one-hot exploit rows per member, zero truncation):
  frac_inconsistent ≡ 0, licensed ≥ carried on every one-hot row,
  and **min-clip is a NO-OP on every row (clipped == carried)** —
  the coherent operator's exploits are FULLY LICENSED. Cross-link
  to today's min-clip read: this is the pure limit of the licensed
  residual class that kept 24/29 GRU models in tier.
- **P-RKN3 (comparability, descriptive — the sentence is NOT
  licensed):** RKN heldout obs-NLL 0.421–0.433 vs the pilot2 GRU
  members' 0.384–0.395 (floor 0.3006, reproduced to the registered
  1e-3); z-NLL 1.69–1.83 vs GRU 0.46–1.48 (diag floor −1.800).
  Coherence in this implementation carries a MODEST predictive cost
  (~+0.03–0.05 nats/read obs-side); "coherence didn't cost
  predictive quality" may not be claimed. σ_pi 0.9933–0.9995 (cap a
  no-op; no train/deploy divergence — the #33 F1 fix held).
- **P-RKN4′ (CEI bridge): FIRES.** All 20 audited sensor rows
  (5 static one-hot sensors × 4 members) PRICED with every
  registered criterion met: power 0.95–0.99, false-conviction
  0.04–0.08 (≤ .1), E[N] within band of the empirical-κ bound, Wald
  in band. Trained miscalibration γ̂ ∈ [+0.14, +0.29]; κ_emp
  0.062–0.073; **the audit-cost exhibit: E[N] ≈ 56–66 forced reads
  per sensor** (bounds 36–42). The price of verifying this coherent
  operator is now a measured table, not a formula.
- **C-channel disclosure (registered out-of-scope row):** ‖ĉ_s6‖ =
  0.198 / 0.078 / 0.176 / 0.146 across members on the zero-C TV
  sensor (computed from the frozen ensemble at adjudication; the
  audit jsons carry c_hat_norm for audited sensors only) — the
  r-only audit's blind spot remains exhibited. Overlap sensor
  ‖ĉ_s4‖ ≈ 1.37 ×4.
- Train losses 1.91–2.14; z3 q̂ row confirms the registered
  unidentifiability disclosure.

## Position

The Track-C headline lands as registered: **coherence removes the
conjunction-tier pathology structurally, converts all remaining
farming into fully-licensed honest-wrong promising, and the CEI
audit prices that residual at ~60 reads/sensor** — the
operator-level dichotomy (incoherent farmer vs coherent liar) with
both halves now measured. Cost side: modest predictive-quality
penalty vs the GRU bank, reported honestly. Standalone Track-C
paper has its complete empirical core; remaining work is writing.
