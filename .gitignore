.pytest_cache
dist
__pycache__/
*.py[cod]
*.egg-info
MUJOCO_LOG.TXT
.claude
academic-research-skills-3.9.2
/logs/
/results/

# Local research planning notes and drafts.
research_notes/
temp_files/

# Result bundles move by direct rsync, never by git (policy 2026-07-30).
# Bytes are pinned by tracked manifests/<bundle>.sha256 instead
# (scripts/bundle_manifest.sh). Pre-30-Jul bundles remain in git history.
local_results/

report/*.aux
report/*.log
report/*.out
report/*.bbl
report/*.blg
report/*.fdb_latexmk
report/*.fls
report/*.synctex.gz
report/*.toc

# dv3ops machine-local state (regenerated; never a record)
ops/state/
scripts/ops/VERSION
