# PREREG — Routed-repair PAIRED certification wave (rrp2; 23 Aug 2026)

**Status: FROZEN at user commit. ONE read execution.** Licensed by the
rrpctl NO-CALL (artifacts/rrpctl_read_20260823: E1 −103.2 [−213.5,
+8.9], MDE80 170 — the cross-cohort design could not decide) and its
measured cause: a **~+99 AUC cross-invocation shift on identical
(fit, protocol) cells** (rca 394.7 vs valuefree q1s1 295.5). User GO
23 Aug on cheap/slow lanes, background priority. Reviewer: ONE Opus
pass, user pre-approved, triggered at build. This is a CERTIFICATION
wave: honest priors ~5–10% SALVAGE, ~50–60% bounded
(refit-don't-finetune certified), ~25–35% ANTI-SALVAGE, ~10% NO-CALL.

## Design — seeds 1–8, THREE arms, ONE batch, invocation-paired

The rrpctl estimands re-run with the invocation offset REMOVED: all
three arms of a seed run in the SAME job on the SAME GPU (the
carrier/crowding pair-preserving pattern), and the read is paired
per-seed one_sample (exact 2^8 sign-flip + BCa).

| arm | source WM (seeds 1–8) | adapt run_id |
|---|---|---|
| repair′ | the EXISTING rrp refits `ax1wm_finger_rrpq1s1_seed{k}` (executed wave; counters 500000 witnessed) — RE-ADAPTED fresh | `adapt_ax1rp2rq1s1_finger_seed{k}_ckpt500000` |
| rca′ | rgo-from-random-init 500k at seeds 1–8 — source FILL below | `adapt_ax1rp2aq1s1_finger_seed{k}_ckpt500000` |
| rcb′ | NEW matched-budget refits `ax1wm_finger_rp2bq1s1_seed{k}` ([rgo → rgo-refit], the exact rrp recipe, init = the same-seed rca′ fit) | `adapt_ax1rp2bq1s1_finger_seed{k}_ckpt500000` |

- **rca′ source FILL (existence fact, recorded by dated edit BEFORE
  any adapt job; no outcome can inform it)**: (A) if ops attests 8/8
  surviving `ax1wm_finger_rgoq1s1_seed{1..8}` fits hold a done-ckpt
  at step 500000, they are the source; (B) else 8 FRESH fits
  `ax1wm_finger_rp2gq1s1_seed{1..8}` by the registered rgo recipe
  (`AXIS1_ARM=rgo` flags transcribed: `--agent.repval_grad False`
  with `--agent.expl.mode task`; `REPLAY=$RUNROOT/axis1_finger/q1/
  side1`, `UPDATES=500000`, random init — never writing into the
  historical rgoq1s1 namespace) via the registered vehicle
  `ops/waves/rrp2/rp2g_fits.sbatch` (review B1: path B previously had
  no instrument; its job log rrp2_fits_*.out feeds the builder's
  realized-replay scan). The witness records
  `_meta.rca_source_prefix` ∈ {'ax1wm_finger_rgoq1s1',
  'ax1wm_finger_rp2gq1s1'}; the reader validates it against exactly
  these two literals and refuses any per-seed mixing.
  **Source provenance gates (review B1 — E1′'s comparator)**: the
  saved `run.from_checkpoint` on every rca′-source fit must be EMPTY
  (the random-init property, gated in BOTH paths), and under path B
  the realized replay (from the vehicle's `offline_fit ... REPLAY=`
  log line) must end with `axis1_finger/q1/side1`; path A inherits
  replay provenance from the carrier registration (amend2-verified
  configs), disclosed. The sbatch's and builder's auto-detect both
  implement this FILL; the dated FILL line below is the artifact of
  record and prevails on any divergence.
  **FILL (ops, dated): rca′ source = A, attested 2026-08-23 by ops.**
  Tested as the prereg specifies -- the NEWEST ckpt dir carrying a `done`
  file, per seed, and its step suffix -- NOT `ckpt/latest` (the rrpctl
  precondition's explicit warning). Result: `ax1wm_finger_rgoq1s1_seed{1..8}`
  all 8/8 hold a done-ckpt at step 500000, so the surviving rgo fits ARE
  the rca′ source and path B (8 fresh rp2g fits, ~16 GPU-h) is not needed.
- rcb′ refit mirrors rrp/rcb byte-for-byte apart from the init
  source: `AXIS1_INIT_WM=<rca′ fit>`,
  `AXIS1_INIT_REGEX='^(enc|dyn|dec)/'`, `AXIS1_INIT_FROZEN_ENC=False`,
  `AXIS1_ARM=rgo`, UPDATES=500000 (fresh counter).
- All adapts: unfrozen_readout, STEPS=1.25e5, U1 protocol. Vehicle:
  `ops/waves/rrp2/bundle.sbatch` — **one SEED per job** (repair′ +
  rca′ + rcb′ ≈ 5+5+12 h ≈ 22 h against a 36 h wall), so every paired
  difference's ADAPT phase is same-job/same-GPU by construction (the
  source FITS come from earlier invocations — deliberately: the
  measured ~+99 shift is adapt-level); job logs
  `rrp2_bundle_*.out` feed the witness (completion evidence AND the
  invocation-pairing gate).
- Blindness disclosure: repair (original batch) and the rca CLASS
  levels (295.5 / 394.7 in two prior batches) are known; every
  quantity here is a fresh invocation and every threshold below was
  frozen before any rrp2 value exists.

## Estimands and decision rules (paired one_sample, exact 2^8
## sign-flip + BCa, α=.05; fixed-sequence gatekeeping; BOUND =
## G_PIN/2 = 100.5443, G_PIN = 201.0885625 — the same immovable
## look-0 constant as rrpctl, same justification)

- **E1′ (PRIMARY)**: paired [repair′_k − rca′_k], n=8.
  - FIRES POSITIVE ⇒ **INIT-ADVANTAGE** (E2′ adjudicated).
  - FIRES NEGATIVE ⇒ **ANTI-SALVAGE-500K** (the apt init hurts even
    under routing — reported as a finding; E2′ descriptive).
  - No fire, BCa CI ⊆ (−BOUND, +BOUND) ⇒
    **OVERWRITE-SUFFICIENT-BOUNDED**: the refit-don't-finetune
    certificate issues (the sunk trunk adds/subtracts less than half
    the routed effect); E2′ descriptive.
  - Else ⇒ NO-CALL-UNDERPOWERED (realized MDE80 reported; planning
    MDE80 ≈ 67 at the routedrepair paired sd 67.9 — a DIFFERENT
    pairing, disclosed as planning-only).
- **E2′ (CO-PRIMARY, verdict weight only on E1′ INIT-ADVANTAGE)**:
  paired [repair′ − rcb′]: SALVAGE-CONFIRMED / BUDGET-EQUIVALENT
  (bounded) / ANTI-SALVAGE-AT-BUDGET / NO-CALL — rrpctl semantics
  verbatim.
- **E3′ (descriptive)**: paired [rcb′ − rca′].
- **S1 (descriptive, no α — the instability instrument)**: per-seed
  [repair′_k − REPAIR_PINNED_k] on the SAME refits re-adapted — the
  cleanest direct measurement of the adapt-level invocation shift;
  reported mean/sd/range beside the ~+99 cross-batch estimate. The
  pinned constants are the routedrepair read's per-seed values
  (360.9792, 429.8125, 257.6875, 266.6667, 198.3542, 263.6042,
  214.6146, 339.8229). **Code-identity disclosure (review M3)**: the
  repo has moved since the pinned batch (agent.py/configs.yaml/
  explore.py — the diff is confined to expl-mode 'advd' branches and
  four new agent.expl.* config defaults; the task-mode adapt path is
  computationally unchanged, but saved configs are not byte-identical
  to the 22-Aug batch), so S1 is an invocation+environment-shift
  instrument, not a pure invocation one; the builder records the repo
  commit in `_meta.repo_commit` for the audit.

## Gates (refusal = read not consumed)

Witness (`build_rescue_bundle --wave rrp2 --job_logs <dir>`): 24 fit
entries (8 rrp + 8 rca′-source + 8 rp2b) counters == 500000; rgo-arm
cfg pins (task + reward_grad True + repval_grad False) on the
rca′-source AND rp2b fits; rp2b init-sha EQUALITY vs the same-seed
rca′-source fit's ckpt_sha + recipe pins (from_checkpoint_regex
'^(enc|dyn|dec)/', frozen_enc False) + realized replay endswith
axis1_finger/q1/side1; rrp fits: init containment in the same-seed
fq1s1 donor checked on the SAVED path string (donor dirs need not
exist — no sha) and refusing via the problems gate on mismatch;
rca′-source fits: `run.from_checkpoint` EMPTY in both paths +
(path B) realized replay endswith the registered buffer (review B1);
**24 adapt witnesses** counters == 125000 (four-signal evidence) +
adapt→source linkage; **INVOCATION-PAIRING GATE (the point of this
wave)**: each adapt's producing job log is recorded and, per seed,
all THREE arms' adapts must come from ONE job log file — refusal
otherwise, and an adapt whose OK/DONE lines appear in MORE than one
log is flagged ambiguous by the builder and refuses via the problems
gate (review M2: never silent last-wins); `_meta.rca_source_prefix`
valid; `_meta.repo_commit` recorded (review M3); bounded branches
additionally require perm_p ≥ α (review m5, free hygiene); STRICT
modal n_ep pinned == 96; `_meta.problems` empty; ONE-read guard.

## Ops

Background priority, cheap/slow lanes acceptable, ONE venue, never
blocking writing. (0) rca′-source FILL (existence attestation, dated
edit above) → (0b, path B only) 8 rp2g fits (~16 GPU-h) → (1) 8
seed-whole jobs via `ops/waves/rrp2/bundle.sbatch` — a seed that
crashes mid-job is unsalvageable by resume (the pairing gate refuses
split logs, by design); remedy = archive-then-delete that seed's
adapt dirs and re-run the seed whole (CELLS one seed
per job; ~22 h each; keep rrp2_bundle_*.out) → (2)
`build_rescue_bundle --wave rrp2 --runroot $RUNROOT --job_logs <dir>`
+ sha manifest → (3) **[ME] ONE read** via `analysis/rrp2_read.py`.
Crashed cells may resume into their logdir; a from-zero restart
doubles n_ep and refuses at the modal pin (disclosure carried from
rrpctl). Total ≈ 8×22 h ≈ **176 GPU-h path A / ≈192 path B** on the
slow lanes (UNVERIFIED; re-derive from the first measured job) —
background priority makes the figure a calendar note, not a cost
gate.
