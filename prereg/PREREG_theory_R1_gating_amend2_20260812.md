# AMENDMENT 2 to PREREG_theory_R1_gating_20260811 — operationalization of P-D2 / P-F1 / P-J1 — 2026-08-12

Frozen 2026-08-12, BEFORE any artifact of the three waves it governs
exists (no reacher/walker collector, buffer, or fit; no bridge-arm run;
no LeWM artifact). Amendment 1 (Part-A/#21 + Part-B mapping) is
untouched. This amendment ONLY pins measurement forms the R1 base text
left to wave registration; it adds no predictions and changes no
executed verdict. Grading happens at each wave's ONE registered read.

## P-D2 (dense domain, attenuation) — normalization + instantiation

- The base text's "point estimate below the finger interaction" is
  scale-confounded across domains (score ranges differ mechanically).
  Registered form: **standardized paired effect d_z** — d_z(dense
  domain) < d_z(finger) where the finger anchor is the executed W0
  value **0.8284213656547132**
  (`artifacts/w0_n16_interaction_20260713`, sensitivity.d_z).
  Directional (point comparison), per the base text, EXCEPT that a
  significantly NEGATIVE dense-domain interaction grades REVERSED
  (review A3) — a reversal must never count as R1-consistent
  attenuation; the dense domain's own CI/permutation are reported
  descriptively.
- Dense domain instantiated as **dmc_walker_walk** (the base text's
  parenthetical said "walker_run-class"; walker_walk is the same dense
  shaped-velocity locomotion family — deviation + rationale (collector
  reuse, naming-convention collision with walker_run) disclosed in
  `PREREG_domains_20260812`). This is a pre-outcome instantiation
  choice: no walker data of any kind has been touched.
- P-D1 unchanged (sparse domain named at registration: reacher_hard;
  interaction PRESENT = CI > 0, with the wave's permutation conjunct).

## P-F1 (TM2 attenuation) — estimand + anchor

- Estimand: the **adaptation-value LEVEL contrast** [aware − free]
  (paired per (seed, side) cell, auc100k) — the form the R1 mechanism
  sentence speaks to ("aware LEVEL ≈ 2.2× free", 17 Jul), NOT the
  occupancy interaction.
- Dreamer denominator pinned: **task − apt = +108.33852499999999**
  (finger q1, seeds 1–8, both sides pooled, qc-passed, computed
  2026-08-12 from committed `artifacts/p0_axis1_corrective_20260713`
  csvs). Ratio = contrast / anchor; "ratio < 1" graded on the ratio CI
  (numerator CI / fixed anchor).
- Branch vocabulary (frozen in `PREREG_tm2_bridge_20260812`):
  ATTENUATED-POSITIVE / POSITIVE-NOT-RESOLVED / NULL-CONTRAST /
  INVERTED (R1-violating). NULL-CONTRAST is NON-REFUTATION, not
  confirmation (review B8): a CI spanning zero does not establish the
  "≥ 0" clause, and only ATTENUATED-POSITIVE licenses the attenuation
  claim.
- The powered wave re-runs aware/free SAME-ERA alongside the bridge
  arm (Midway3 cross-era pairing ban); the 17-Jul cells become
  cross-era descriptives only.

## P-J1 (LeWM / JEPA arm) — substrate + carrier mapping

- Substrate: the finger PIXEL pair (`pxq1m`) — LeWM consumes pixels by
  construction; the study's proprio mainline buffers carry no images.
- Known constraint governing the mapping (executed evidence, not a
  choice): the pixel substrate is behaviorally FLOOR-CENSORED (pe
  P-PE2 null band; U4 protocol-independent floor). Therefore the base
  text's ordering "task-aware > LeWM > apt (strict LeWM > apt = the
  primary directional)" is carried as follows:
  - **"LeWM > apt" (the primary directional) = the two REPRESENTATION
    legs** of `PREREG_lewm_20260812`: P-J1-rep (graft: in-regime
    rew-NLL of stage-2 fits, JEPA-distilled vs the executed
    recon-pretrained pe anchor, paired) and P-J1-emb (distill-free: raw
    LeWM embedding vs fpxpx WM feature reward-AUROC under the identical
    ridge estimator, paired). Joint rule: both fire ⇒ CONFIRMED; one ⇒
    PARTIAL; none ⇒ NOT CONFIRMED (then the reward-free null is
    objective-GENERAL across recon and latent-prediction pretraining
    at this substrate — the registered strengthened-negative form,
    CONDITIONAL per reviews C3/C8 on the wave's embedding-pipeline
    positive control: the read refuses without it, so a
    mis-preprocessed instrument can never produce the claim).
  - **"task-aware > LeWM" = descriptive** (X2 task adapt cells vs jp
    adapt cells, floor-disclosed); the behavioral leg (P-J1-beh) never
    upgrades a verdict; a LIFTS outcome is a discovery requiring its
    own confirmatory wave.
  - "LeWM ≥ TM2-free-analog" stays unscored cross-family descriptive
    (base text).
- The distill-proxy caveat and fidelity gate are part of the wave
  registration (`PREREG_lewm_20260812`), not of R1.

## Discipline

Committed in the same freeze-commit as the three wave registrations,
before any of their compute. Each clause is graded ONCE, at its wave's
ONE registered read, by that wave's frozen reader. No reviewer
(predictions/operationalization doc, precedent Amendment 1).
