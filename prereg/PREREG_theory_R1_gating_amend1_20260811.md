# AMENDMENT 1 to PREREG_theory_R1_gating_20260811 — grading + Part-B operationalization (2026-08-11)

Pre-outcome for everything it touches (no #21-extension, B1, or B2
quantity exists). Three registered deltas, each forced by the 11-Aug
batch reviews:

1. **A1 second grading (reviewer-2 M7)**: the base document graded
   Part-A predictions "once, at the ONE registered read of its wave";
   `PREREG_dose_task_amend1_20260811` opens a registered pooled look.
   Recorded here on the theory side: **A1 is graded at BOTH looks** —
   look 1 (executed, n=8): A1 as written FAILED at the registered
   gate letter (scoreboard scratch stands); look 2 (pooled n=16):
   **A1′ = reader CI > 0 ∧ perm p < .0294** (the amendment's corrected
   alpha). A1's base-document sign-only phrasing ("level3 − level0 >
   0") is superseded by the gate-conjunction form it always referenced
   — disclosed, and conservative against R1 (the sign condition alone
   would already be satisfied by the executed +157.6). **A3/A4 are
   SUSPENDED at look 2** (P-DR2 demoted to descriptive there); they
   grade only at a future fresh single-look shape registration, if
   any.
2. **Part-B verdict mapping (reviewer-3 F11)**: the base adjudication
   ("both trials λ-negative ⇒ …; either λ-positive ⇒ …") is
   operationalized onto the wave readers' verdict vocabularies:
   **λ-positive** = `LAMBDA-SUPPORTED` (either wave). **λ-negative** =
   B1 `INTERACTION-SHRINKS` or B2 `NEGATIVE-DID`. **Neither** (does
   NOT count toward "both λ-negative") = any `INDETERMINATE*`,
   `*-CENSORED`, or `INSTRUMENT-LIMITED` outcome — an indeterminate
   or censored pair leaves λ UNRESOLVED-IN-SCOPE (no "decisively
   dead" wording licensed). This mapping is frozen BEFORE either wave
   runs.
3. **Part-B B1/B2 instrument deltas (reviewer-3 F11, disclosed)**:
   (a) B1's registered wave (`PREREG_capdescent_20260811`) carries the
   interaction DID as PRIMARY and the ridge-AUROC legibility DID as
   SECONDARY; the base text's "membership reshuffles by λ-rank
   (spectral-v2 rank shift)" leg is NOT registered as a rule in this
   wave (E4/spectral panels are descriptive) — the λ-positive branch
   therefore rests on the interaction leg alone, a WEAKER
   confirmation form than the base conjunction sketch, disclosed.
   (b) B2's instrument is the D0 **distractor wrapper obs key**
   (`distractor`, env-declared spaces + replay-carried channels),
   exactly as the base text names — the first B2 draft's replay-only
   `nuisance` key could never reach the model (env-derived obs
   spaces) and was killed in review before freeze.

No other change; the base document's predictions, honesty
disclosures, and adjudication logic stand.
